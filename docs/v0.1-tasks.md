# V0.1 Tasks

V0.1 구현을 위한 작업 단위 분할. 의존성 순서대로 정렬되어 있으며, 위에서부터 차례로 작업한다.

각 작업은 **검증 가능한 완료 조건**을 가진다 (Karpathy 원칙 4번 적용).

## 진행 상태 표기

- `[ ]` 미시작
- `[~]` 진행 중
- `[x]` 완료

---

## Stage 1: 프로젝트 셋업

### Task 1.1: Unity 프로젝트 생성
- [x] Unity 2021.3 LTS 이상으로 새 프로젝트
- [x] 폴더 구조 생성 (`coding-conventions.md` 참조)
- [x] Git 초기화, .gitignore 설정 (Unity 표준)
- [x] CLAUDE.md / docs/ 폴더를 프로젝트 루트에 복사
- **완료 조건:** Unity 에디터에서 빈 씬 열리고, 폴더 구조가 의도대로 잡혀있음

### Task 1.2: 외부 패키지 설치
- [x] Newtonsoft.Json (Unity Package Manager)
- [x] Modern UI Pack (Asset Store) — `Assets/Imported/` (gitignored) 로 임포트
- [ ] (V1.x에서 DOTween)
- **완료 조건:** `using Newtonsoft.Json;`이 컴파일됨

### Task 1.3: 폰트 셋업
- [x] 한국어 폰트 임포트 (NotoSansKR-VF, OFL 라이선스 — `Assets/_Project/Art/Fonts/`)
- [x] TMP 기본 폰트 에셋 생성 (`NotoSansKR-VF SDF.asset`, Dynamic Mode)
- **완료 조건:** TextMeshPro에 한국어 표시 가능

### Task 1.4: 네임스페이스 / 어셈블리 정의
- [x] FMLite.Core, FMLite.Domain, FMLite.Application, FMLite.UI, FMLite.Persistence, FMLite.Utils 폴더에 .asmdef 생성
- [x] Editor 폴더에 별도 .asmdef (Editor 플랫폼 전용)
- **완료 조건:** 네임스페이스가 컴파일됨

---

## Stage 2: 핵심 인프라

### Task 2.1: EventBus 구현
- [x] `Core/Events/EventBus.cs` 작성
- [x] Subscribe / Unsubscribe / Publish API
- [x] 타입별 핸들러 딕셔너리 관리 (`Dictionary<Type, Delegate>` + `Delegate.Combine/Remove`)
- [x] 빈 핸들러 목록 케이스 처리
- **완료 조건:**
  - 더미 이벤트 발행/구독 동작 (EditMode 테스트 통과)
  - 같은 핸들러 중복 구독 시 한 번만 호출 (EditMode 테스트 통과)
  - Unsubscribe 후 호출 안 됨 (EditMode 테스트 통과)

### Task 2.2: GameTime 구현
- [x] `Core/GameTime.cs` 작성
- [x] CurrentDate 프로퍼티
- [x] Advance(int days) 메서드 — 하루씩 N번 이벤트 발행 (design-decisions.md #23)
- [x] DayAdvancedEvent 발행 (`Core/Events/GameEvents.cs`)
- **완료 조건:** Advance(1) 호출 시 날짜 +1, 이벤트 발행 확인 (EditMode 테스트 5개 통과)

### Task 2.3: GameManager 싱글톤
- [x] `Core/GameManager.cs` 작성 (design-decisions.md #29 — Core Layer 결정)
- [x] Instance 패턴 (Awake에서 설정, 중복 destroy)
- [x] State 프로퍼티 (GameState) + `SetState` 진입점 + `UserClub` 헬퍼 — #76
- [x] DontDestroyOnLoad
- [x] **부수:** `Domain.asmdef` 미사용 Core 참조 제거 + `Core.asmdef` 에 Domain 참조 추가 (의존 방향 정통화)
- **완료 조건:** 씬 어디서나 GameManager.Instance 접근 가능 + State/UserClub 정상 동작 (PlayMode 테스트 6개 통과 — 기존 3개 + State/UserClub 3개)

### Task 2.4: 로그 시스템
- [x] `Core/GameLog.cs` (Debug.Log 래퍼) — `Logger` 명은 `UnityEngine.Logger` 와 충돌해서 `GameLog` 채택
- [x] 카테고리별 로그 (enum LogCategory: System / Match / Transfer / Youth / Season)
- [x] 디버그 모드 토글 (`GameLog.IsDebugMode` static — Stage 5 이후 GameBalanceSO로 이전)
- **완료 조건:** `GameLog.Log(LogCategory.Match, "...")` 형태로 호출 가능 (EditMode 테스트 4개 통과)

---

## Stage 3: 도메인 데이터 클래스

### Task 3.1: Enum / 값 객체 정의
- [x] `Domain/Enums.cs`: Position, Foot, CompetitionType, OfferStatus, PlayerOrigin, CupTarget
- [x] `Domain/Stats.cs`: Stats 컨테이너 + TechnicalStats(12) / MentalStats(12) / PhysicalStats(8) / GoalkeepingStats(10)
  - 각 카테고리에 ApplyToAll 헬퍼 포함 (테스트 4개)
- [x] `Domain/Contract.cs`
- [x] `Domain/PlayerState.cs`
- [x] `Domain/Facilities.cs`
- [x] `Domain/Finance.cs`
- [x] `Domain/PersonalInfo.cs`
- [x] `Domain/InjuryInfo.cs`
- [x] `Domain/MatchResult.cs` + PlayerMatchStat
- [x] `Domain/Standings.cs` + StandingEntry
- [x] `Domain/SeasonState.cs`
- [x] `Domain/SeasonStat.cs`
- **완료 조건:** 모든 클래스 [Serializable] 부착, 컴파일 OK, StatsTests 4개 통과

### Task 3.2: 핵심 도메인 클래스
- [x] `Domain/Player.cs`
- [x] `Domain/Club.cs`
- [x] `Domain/League.cs`
- [x] `Domain/Match.cs` + MatchEvent placeholder (V1.0 텍스트 이벤트 대비)
- [x] `Domain/TransferOffer.cs`
- [x] `Domain/YouthIntake.cs`
- **완료 조건:** `class-diagram.md`의 필드 모두 포함, 컴파일 OK

### Task 3.3: GameState
- [x] `Domain/GameState.cs`
- [x] 마스터 리스트 (allPlayers, allClubs, leagues, activeOffers)
- [x] 메타 필드 (currentDate, userClubId, rerollTokens, randomSeed)
- [x] [JsonIgnore] Dictionary 인덱스 (_playerById, _clubById)
- [x] BuildIndexes() 메서드
- [x] GetPlayer / GetClub 헬퍼 (lazy index build)
- [x] AddPlayer / RemovePlayer 헬퍼 + AddClub / RemoveClub (대칭, 인덱스 동기화)
- **완료 조건:** EditMode 테스트 7개 통과
  - AddPlayer → GetPlayer로 같은 객체 반환 ✓
  - BuildIndexes 후 동일하게 동작 ✓
  - Add/Remove 시 마스터 리스트와 인덱스 동시 갱신 ✓

---

## Stage 4: 직렬화 / 세이브 시스템

### Task 4.1: SaveSystem 골격
- [x] `Persistence/SaveSystem.cs` (static class)
- [x] `Save(GameState, slotName)` 메서드 + `Load(slotName)` + `LoadSlotMeta(slotName)`
- [x] 저장 경로: `Application.persistentDataPath/saves/{slotName}/`
- [x] state.json + meta.json (Newtonsoft.Json 직렬화)
- [x] AtomicWrite: .tmp 파일 + `File.Replace` 로 원자적 교체
- [x] GameLoadedEvent / GameSavedEvent 발행
- **완료 조건:** EditMode 테스트 6개 통과
  - 빈/채워진 GameState 라운드트립 ✓
  - 로드 후 BuildIndexes 정상 (GetPlayer/GetClub 동작) ✓
  - 같은 슬롯 재저장 시 깔끔하게 덮어쓰기 ✓
  - 존재하지 않는 슬롯 → null ✓
  - meta.json 생성 + LoadSlotMeta 로 조회 ✓
  - .tmp 파일 남지 않음 (atomic 확인) ✓

### Task 4.2: 세이브 슬롯 관리
- [x] `SaveSystem.ListSlots()` → `List<SaveSlotMeta>` (saves 폴더 스캔, meta.json 있는 슬롯만)
- [x] `SaveSystem.LoadSlotMeta(slotName)` (Task 4.1 에서 선반영)
- [x] `SaveSystem.DeleteSlot(slotName)` → bool
- **완료 조건:** 슬롯 목록 화면(Task 13.1)에 표시 가능 (EditMode 테스트 4개 통과)

---

## Stage 5: ScriptableObject 베이스

### Task 5.1: 핵심 SO 정의
- [x] `Domain/ScriptableObjects/GameBalanceSO.cs` — V0.1 밸런싱 17개 필드 (Debug/PlayerGen/Gacha/Match/Daily·Season/Transfer)
- [x] `Domain/ScriptableObjects/PositionSO.cs` — Position enum + emphasis flags
- [x] `Domain/ScriptableObjects/TraitSO.cs` — id/displayName/description/weight
- [x] `Domain/ScriptableObjects/CountrySO.cs` — code/displayName/flagColors
- [x] `Domain/ScriptableObjects/NamePoolSO.cs` — countryId + firstNames/lastNames
- [x] `Domain/ScriptableObjects/LeagueConfigSO.cs` — clubCount/relegationCount/playersPerClub
- [x] `Domain/ScriptableObjects/FacilityLevelSO.cs` — facilityType × level + youth effect
- [x] 각각 `[CreateAssetMenu]` 부착 (`FM-Lite/` 서브메뉴)
- [x] **부수**: `FacilityType` enum 신규 (Scout/Training/Youth) — Enums.cs, class-diagram.md
- **완료 조건:** Unity 메뉴(Assets → Create → FM-Lite)에서 7종 SO 에셋 생성 가능 ✓

### Task 5.2: GameDatabase
- [x] `Domain/GameDatabase.cs` (정적) — Core 가 Domain 을 참조 못 해서 Domain 으로 위치 변경
- [x] `LoadAll()`: `Resources.LoadAll<T>("")` 로 7종 SO 일괄 로드
- [x] `Get*(id)`: TraitSO / PositionSO / CountrySO / NamePoolSO / LeagueConfigSO 조회
- [x] `GetFacilityLevel(type, level)`: 복합 키 조회
- [x] `GameBalance` 프로퍼티: 단일 인스턴스
- [x] `Register/Clear`: 테스트·디버그용 in-memory 주입
- [x] `AllTraits/Positions/Countries`: 가챠 등 컬렉션 순회용
- **완료 조건:** EditMode 테스트 8개 통과
  - Register/Get 라운드트립 (Trait/Position/NamePool/FacilityLevel/GameBalance) ✓
  - 존재하지 않는 id → null ✓
  - Clear → 전체 비워짐 ✓
  - AllTraits 순회 ✓

### Task 5.3: 기본 SO 데이터 채우기
- [x] Editor 시드 스크립트 (`Editor/SeedData/SeedV01Data.cs`) — 메뉴 `FM-Lite/Seed/Generate V0.1 Data`
- [x] Position SO 14개 (GK ~ CF, 한글 displayName + emphasis flags)
- [x] Trait SO 6개 (늦깎이형 / 조숙형 / 부상 취약 / 멘탈 강자 / 빅매치형 / 만능형)
- [x] Country SO 10개 (ENG / FRA / GER / ESP / ITA / BRA / ARG / NED / POR / KOR + 깃발색)
- [x] NamePool SO 10개 (국가별 first/last 15 + 15 ≈ 300 이름)
- [x] LeagueConfig SO 1개 (EPL 모티브 — 20 클럽 / 강등 3 / 선수 25)
- [x] FacilityLevel SO 15개 (3 type × 5 level, 비용·기간 + Youth 효과)
- [x] **GameBalanceSO 1개** (V0.1 밸런싱 17 필드 — Task 5.1 기본값)
- **완료 조건:** `Assets/_Project/Data/Resources/` 아래 57 asset, `GameDatabase.LoadAll()` 정상 로드 가능 ✓

---

## Stage 6: 선수 생성

> 작업 전 `algorithms.md` #1 (Player Generation) 확인 필수

### Task 6.1: PlayerGenerator
- [x] `Application/PlayerGenerator.cs`
- [x] Generate(클럽명성, 연령범위, 시드, ...) → Player 인스턴스
- [x] 능력치 분포 (algorithms.md 명세대로)
- [x] 포지션 결정 + 키 스탯 가중치
- [x] 트레잇 부여 (확률 기반)
- [x] 이름 / 국적 분배 (NamePoolSO 활용)
- [x] 계약 정보 생성
- **완료 조건:**
  - 명성 90 구단용 선수 100명 생성 → 평균 CA, 분산이 명세 범위 ✓ (T5 통과)
  - 같은 시드로 생성 시 동일 결과 ✓ (T4 통과)
  - 포지션별 키 스탯이 우세함이 확인됨 ✓ (T1, T3 통과)

### Task 6.2: ClubGenerator

> Sub-PR 분할 진행 (PlayerGen 패턴 — Sub-A → Sub-B).

#### Sub-A: GameBalanceSO ClubGen 필드 + LeagueConfigSO.clubNames + 시드 (#79)
- [x] `GameBalanceSO` ClubGen 카테고리 ~36개 필드 추가
  - Reputation Tiers / Finance / Facilities / Squad Composition / Age Distribution / Foundation Year / Homegrown / Board
- [x] `LeagueConfigSO.clubNames: List<string>` 필드 추가
- [x] `SeedV01Data.GenerateLeagueConfigs` 에 EPL 풍 가상 구단명 20개 입력 (명성 내림차순)

#### Sub-B: ClubGenerator 본 구현 (#82)
- [x] `Application/ClubGenerator.cs`
- [x] LeagueConfig 기반으로 20개 구단 생성 (가변 clubCount 대응 — `AllocateTierCounts`)
- [x] 명성 분포 (4티어 계단)
- [x] 재정 / 시설 등급 초기화 (명성 약상관 + 노이즈)
- [x] 각 구단에 PlayerGenerator로 25명씩 생성 (포지션 분배표 + 연령/국적 추첨)
- [x] T1~T7 EditMode 테스트 통과 (T2b 가변 clubCount 포함)
- **완료 조건:** 20개 구단 + 500명 선수 정상 생성, T1~T7 통과 ✓

---

## Stage 7: 게임 시작 흐름

### Task 7.1: GameInitializer (#99)
- [x] `Application/GameInitializer.cs`
- [x] `NewGame(seed, seasonStart, leagueConfig, balance) → GameState`
- [x] data-flows.md #1 시퀀스 통합 (League + ClubGen + ScheduleGen + Standings)
- [x] `GameState.userClubId` 디폴트 `-1` sentinel (UI 가 선택 후 갱신)
- [x] `state.nextPlayerId` 자동 갱신 (ClubGen 500명 후 501)
- [x] T1~T7 EditMode 테스트 통과
- **완료 조건:** "New Game" 호출 후 완전한 초기 GameState 생성 ✓
  - 20 Club + 500 Player + 380 Match + 20 Standings + 인덱스 즉시 동작

### Task 7.2: ScheduleGenerator
- [x] `Application/ScheduleGenerator.cs` — Circle method (Berger tables 변형) 더블 라운드 로빈
- [x] 리그 일정 생성 (홈/원정 더블 라운드 로빈)
- [x] 가변 clubCount 대응 (n팀 → n(n-1) 경기, n=4/8/10/12/20 검증)
- [x] T1~T9 EditMode 테스트 통과
- [ ] 일정 표 League.schedule에 채움 (Task 7.1 GameInitializer 가 호출 시 처리)
- **완료 조건:** 20팀 시 38라운드 × 10경기 = 380경기 일정 생성 ✓

### Task 7.3: StartingSquadGacha

> Sub-PR 분할 진행 (PlayerGen/ClubGen 패턴 — Sub-A 명세 → Sub-B SO 필드 → Sub-C ClubGen 변경 → Sub-D GameState.nextPlayerId → Sub-E 본 구현).

#### Sub-A: 명세 작성 (#88)
- [x] `algorithms.md` #6 Starting Squad Gacha 신규 섹션 (4라인 / 평균 CA / 명성 대비 비율 / ACE / Reroll 정책)
- [x] `design-decisions.md` #28 갱신 (`FormationConfig` + 필수 23 + 랜덤 2)
- [x] `design-decisions.md` #30 신규 (Gacha 평가 정책)
- [x] `design-decisions.md` #31 신규 (Reroll 재생성 + 새 id)
- [x] `design-decisions.md` #32 신규 (V0.1 단일 포메이션 + V1.0 가챠 랜덤화 확장)

#### Sub-B+C: GameBalanceSO FormationConfig + ClubGen 동적 분배표 (#92, 묶음)
- [x] `GameBalanceSO` 에 `FormationConfig` nested `[Serializable]` 클래스 신규
  - name / gk / cbMin / lbMin / rbMin / dmCmGroupMin / lmLwGroupMin / rmRwGroupMin / stCfGroupMin / randomSlots
- [x] `GameBalanceSO.formation: FormationConfig` 필드 + Gacha 티어 컷 4개 필드 추가
- [x] **기존 `squadGK ~ squadCF` 13개 int 필드 제거** (FormationConfig 흡수)
- [x] ClubGenerator: `BuildSquadComposition(rng, balance)` 동적 분배 — 필수 23 + 랜덤 2자리, 그룹 균등 분배
- [x] ClubGeneratorTests T3 갱신 (조건 검증) + T3b 신규 (구단별 분배 다양성)
- [ ] 시드 자산 재생성 (`Balance/GameBalance.asset` 변경) — 머지 후 별도 chore PR

#### Sub-D: GameState.nextPlayerId (#94)
- [x] `GameState.nextPlayerId: int` 필드 신규 (디폴트 1)
- [x] GameStateTests: 기본값 + 갱신 라운드트립 (2개)
- [x] SaveSystemTests: nextPlayerId 직렬화 후 유지 검증
- [x] **ClubGenerator 시그니처 변경 안 함** — Stateless 원칙(#3) 유지. 호출자가 `state.nextPlayerId` → ClubGen `startPlayerId` 로 전달, 결과 후 `state.nextPlayerId = max(id) + 1` 갱신 (Sub-E + Task 7.1 GameInitializer 에서 사용)

#### Sub-E: StartingSquadGacha 본 구현 + 테스트 (#97)
- [x] `Application/StartingSquadGacha.cs`
  - `EvaluateSquad(club, state, balance)` → `SquadEvaluation` — 4단계 (4라인 분류 / 라인 평균 CA / 명성 대비 비율 5단계 / ACE 마커)
  - `RerollSquad(club, state, leagueConfig, balance, currentDate, rng)` → 25명 재생성 + 새 id (state.nextPlayerId 사용)
  - `LineOf(Position)`, `SquadEvaluation`, `TierGrade`, `Line` 값 객체/enum
- [x] `ClubGenerator.RegenerateSquad` public 메서드 추가 — Gacha 와 공용. Generate 도 이걸 호출하도록 리팩토링.
- [x] T1~T7 EditMode 테스트 통과
- **완료 조건:**
  - 다른 구단 영향 없이 한 구단 선수만 재생성 ✓
  - 같은 시드 → 같은 결과 (T5) ✓
  - 명성 대비 비율로 4라인 5단계 티어 정확 산출 (T2~T3) ✓
  - ACE 마커 정확 (T4) ✓

---

## Stage 8: 일정 / 시간 진행

### Task 8.1: EventScheduler (#103)
- [x] `Application/EventScheduler.cs` — Stateless `Run(state) → bool` (정지 신호)
- [x] 오늘 league.schedule 에서 매치 식별 → MatchDayEvent 발행
- [x] userClub 출전 여부로 isUserMatch + 정지 신호
- [x] `Core/Events/MatchEvents.cs` (MatchDayEvent 정의)
- [x] T1~T6 EditMode 테스트 통과
- [ ] (V1.0 / 해당 Stage) 이적창 / 인스펙션 / 보드 리뷰 / 시즌 종료 분기 — placeholder
- **완료 조건:** 특정 날짜 입력 시 예정 이벤트 목록 정확히 반환 ✓

### Task 8.2: DailyProcessor (#101)
- [x] `Application/DailyProcessor.cs` — Stateless `Run(state, balance)`
- [x] 매일 백그라운드 처리 (data-flows.md #2 [2])
- [x] fatigue 회복 (`max(0, fatigue - fatigueRecoveryPerDay)`) — 모든 선수
- [x] 부상 카운트다운 → expectedReturn 도달 시 `injuryTypeId = -1` 복귀
- [x] 계약 만료 카운트다운은 V1.0 placeholder (TODO 주석)
- [x] T1~T7 EditMode 테스트 통과
- **완료 조건:** 30일 진행 시 모든 선수 컨디션 회복, 부상 일수 감소 ✓

### Task 8.3: 시간 진행 통합 (#105)
- [x] `Application/GameLoop.cs` — Stateless `AdvanceDay(state, balance)` + `ContinueUntilStop(state, balance, maxDays)`
- [x] 호출 순서: GameTime.Advance(1) → state.currentDate 동기화 → DailyProcessor.Run → EventScheduler.Run
- [x] EventScheduler.Run 반환값으로 정지 신호 처리 (userClub 매치 등)
- [x] BackgroundSimulator (data-flows.md #2 [4]) 통합 — Task 9.3 (#119) 에서 GameLoop.AdvanceDay 갱신으로 처리
- [x] design-decisions.md #29 보완 — GameManager 책임 경계 명시 (State 보유만, 흐름 조율 X)
- [x] T1~T6 EditMode 테스트 통과
- **완료 조건:** `GameLoop.ContinueUntilStop` 호출 시 다음 정지 이벤트까지 자동 진행 ✓

---

## Stage 9: 경기 시뮬레이션 (V0.1: 스코어만)

> 작업 전 `algorithms.md` #2 (Match Simulation) 확인 필수

### Task 9.1: MatchSimulator ✅

> Sub-PR 분할 진행 (PlayerGen / ClubGen / Gacha 패턴 — Sub-A 명세 → Sub-B Poisson 헬퍼 → Sub-C 본 구현).

#### Sub-A: 명세 작성 (#109, PR #110) ✅
- [x] `algorithms.md` #2 Match Simulation 신규 섹션 (시드 고정 / starting11 선정 / 단순 CA 합 / Poisson + 홈 어드밴티지 / 라인 가중 득점자 / V0.1 PlayerMatchStat 범위)
- [x] `design-decisions.md` #33 신규 (V0.1 정책)
- [x] `design-decisions.md` #34 신규 (V1.0+ 이벤트 시퀀스 진화 경로 — 인터페이스 유지)
- [x] `data-flows.md` #3 비활성 구단 분기 V0.1 단일 메서드로 갱신 (SimulateLite 폐기)
- [x] `glossary.md` 포아송 분포 / λ / 베스트 일레븐 / 홈 어드밴티지 용어 추가

#### Sub-B: RngExtensions.NextPoisson 헬퍼 (#111, PR #112) ✅
- [x] `Utils/RngExtensions.cs` 에 `NextPoisson(double lambda)` 메서드 추가 (Knuth 알고리즘)
- [x] EditMode 테스트 5개 (T1 평균/분산 / T2 결정성 / T3 λ=0 / T4 λ<0 예외 / T5 작은 λ 분포)
- [x] PlayerGen 의 `NextNormal` 패턴 — 헬퍼 정확성 먼저 검증 후 본 구현 진입

#### Sub-C: MatchSimulator 본 구현 (#113, PR #114) ✅
- [x] `Application/MatchSimulator.cs` — Stateless `Simulate(match, state, balance) → MatchResult`
- [x] 6단계 알고리즘 (시드 / starting11 / 전력 / 골수 / 득점자 / playerStats) — `algorithms.md` #2
- [x] `GameBalanceSO` 필드 추가 — `homeAdvantageGoalBonus / scoringWeightByLine / strengthExponent`
- [x] **strengthExponent (k=1.5) 보강** — 단순 선형 ratio 의 결정력 부족 발견 → `pow(s, k)` 비선형 도입. V0.1 임시 변통, V1.0+ 매치 엔진 재작성 시 폐기 (`design-decisions.md` #33 보강 / #34 와 연동)
- [x] EditMode T1~T7 (+ T2b 부상자 / T2c 짧은 스쿼드) 통과
- [x] 시드 자산 갱신 (#115, PR #116) — 별도 chore PR
- **완료 조건 검증 통과:**
  - T1 결정성: 같은 매치 ID + 같은 시드 = 같은 결과 ✓
  - T3 강팀 승률: 강팀 홈 ≥65% / 원정 ≥50% (k=1.5 정규근사 기반) ✓
  - T5 골 분포: 평균 골수 / 무득점 비율 / 5골 이상 비율 명세 범위 ✓
  - T6 득점자 분포: AT 55~80% / MF > DF / GK = 0% ✓

### Task 9.2: MatchPostProcessor (#117)

> 단일 PR (명세는 `data-flows.md` #3 [4] 가 이미 정의, algorithms.md 신규 섹션 불필요).

- [~] `Application/MatchPostProcessor.cs` — Stateless `Process(match, result, state, balance)`
- [~] `Core/Events/MatchEvents.cs` 갱신 — `MatchFinishedEvent { matchId, result }` 추가
- [~] 결과를 Match.result 에 기록 (재처리 시 InvalidOperationException)
- [~] 선수 피로 갱신 — starting11 22명에 fatigueGainPerMatch (30) 가산, Clamp 0..100
- [~] 폼 / 사기 갱신 — **V0.1 미구현** (`design-decisions.md` #30 V1.0+ 보완 포인트, 평점 시스템 부재로 묶음 V1.0 도입)
- [~] 리그 순위 갱신 — League 만, EPL 룰 (승 3 / 무 1 / 패 0) + goalsFor/goalsAgainst
- [~] MatchFinishedEvent 발행
- [~] EditMode T1~T6 (+ T3b 무 / T3c 원정승)
- **완료 조건:** 
  - 1매치 시뮬 후 양 팀 StandingEntry 정확 갱신 (T3a/b/c)
  - 피로 starting11 22명만 +30, 벤치 0 변화, Clamp 100 (T2)
  - MatchFinishedEvent 1회 발행 + payload 정확 (T4)
  - 결정성 / 재처리 차단 (T5/T6)

### Task 9.3: BackgroundSimulator + GameLoop 통합 (#119)

> 단일 PR. 명세는 `data-flows.md` #2 [4] / #3 [6] 이 정의 (algorithms.md 신규 섹션 X).

- [~] `Application/BackgroundSimulator.cs` — Stateless `SimulateDay(state, balance)`
  - 모든 `league.schedule` 의 `state.currentDate` 매치 일괄 처리
  - 각 매치: `MatchSimulator.Simulate` → `MatchPostProcessor.Process`
  - `match.result != null` 매치 스킵 (이미 처리)
  - V0.1: 활성/비활성 구분 X, `MatchFinishedEvent` **모든 매치 발행** (옵션 A, UI 없으니 구독자 0)
- [~] `GameLoop.AdvanceDay` 갱신 — `EventScheduler.Run` 다음에 `BackgroundSimulator.SimulateDay` 호출 (Task 8.3 비고의 "Stage 9 Task 9.3" 통합)
- [~] `data-flows.md` #3 비활성 발행 생략을 V1.0+ 트리거로 이동 + V0.1 모든 매치 발행 명시
- [~] EditMode T1~T5 통과
- **완료 조건:**
  - T1 1라운드 일괄 — 4팀/2매치 currentDate 시 SimulateDay 1회로 모든 매치 result + 순위 갱신
  - T2 결정성 — 같은 시드 → 두 별개 GameState 같은 결과
  - T3 이미 처리된 매치 스킵 — preset result 매치는 재처리 안 됨, 다른 매치만 처리
  - T4 빈 날짜 — currentDate 매치 없을 때 no-op
  - T5 GameLoop 통합 — AdvanceDay 호출 시 currentDate 매치 자동 처리

---

## Stage 10: 유스 인스펙션

> 작업 전 `algorithms.md` #4 (Youth Pool Generation) 확인 필수

> Stage 10 의 3개 Task (10.1/10.2/10.3) 는 같은 파일 (`YouthSystem.cs`) 의 3 메서드 — Sub-PR 패턴은 **명세 → 구현 단일 PR → chore** 흐름.

### Task 10 Sub-A: 명세 작성 (#123)
- [~] `algorithms.md` #4 Youth Pool Generation 신규 작성 (V0.1 전체 명세 + V1.0+ Migration Notes 10개 항목)
- [~] `design-decisions.md` #35 신규 (V0.1 정책 — PA 진실값 / CA derived / 스타 픽 / 시드 옵션 2+3 / 시설 통합 / 미영입 V0.1 단순화 / 나이 가중치 / 자국 78%)
- [~] `design-decisions.md` #36 신규 (`GameState.nextIntakeId` 단조증가)
- [~] `data-flows.md` #4 갱신 (시드 공식 / 트리거 시점 6/15·1/15 / V0.1 단순화 / V1.0+ 트리거)
- [~] `event-bus-catalog.md` Youth Events V0.1 구현 상태 명시

### Task 10.1: YouthSystem.GenerateIntake (Sub-B 일부)
- [ ] `Application/YouthSystem.cs` — Stateless `GenerateIntake(club, state, balance, db, leagueConfig)`
- [ ] `Core/Events/YouthEvents.cs` 신규 (3 이벤트)
- [ ] `GameState.nextIntakeId` 신규 필드 (디폴트 1) + SaveSystem 라운드트립 검증
- [ ] `GameBalanceSO` 신규 13개 필드 (youthStarPickProbability / youthStarPaBonus / youthPaStdDev / youthPaGapStdDev / youthIntakeMinAge/MaxAge / youthIntakeAgeWeights / youthPrimaryNationalityRatio / youthIntakeMainMonth/Day / youthIntakeSecondMonth/Day)
- [ ] `EventScheduler` 갱신 — 6/15 / 1/15 도래 시 `YouthSystem.GenerateIntake` 호출 + 정지 신호
- [ ] 시드 = `state.randomSeed ^ currentDate.Ticks ^ userActionHash ^ club.id ^ intake.id ^ rerollsUsed`
- **완료 조건:**
  - 시설 Lv5 구단의 평균 PA > Lv1 구단 (T8)
  - 같은 newgame seed + 같은 행동 = 같은 풀 (T1 결정성)
  - 자금 1원 차이 → 다른 풀 (T2 직플 공유 방어)
  - 스타 픽 5% (T4)

### Task 10.2: YouthSystem.UseRerollToken (Sub-B 일부)
- [ ] `YouthSystem.UseRerollToken(intake, club, state, balance, db, leagueConfig)`
- [ ] `state.rerollTokens` 감소 + `intake.rerollsUsed += 1`
- [ ] 미영입 기존 후보 제거 + 새 풀 생성 (시드 자동 변경)
- [ ] 토큰 0 시 `InvalidOperationException`
- [ ] `YouthRerolledEvent` 발행
- **완료 조건:** T9 통과 (토큰 차감 + 풀 변경 검증)

### Task 10.3: YouthSystem.SignPlayers (Sub-B 일부)
- [ ] `YouthSystem.SignPlayers(intake, playerIds, club, state)`
- [ ] 영입: `currentClubId` 설정 / `club.youthSquadIds` 추가 / `intake.signedPlayerIds` 추가
- [ ] **미영입 V0.1: 모두 GameState 제거** (`intake.rejectedPlayerIds` 에 ID 만 보관)
- [ ] `YouthSignedEvent` 발행
- **완료 조건:** T10 통과 (영입 후 currentClubId 갱신 / 미영입 ID 만 보관)

### Task 10 Sub-C: chore (Sub-B 머지 후)
- [ ] `GameBalance.asset` 시드 자산 갱신 (Sub-B 의 신규 13 필드 반영) — 별도 chore PR

---

## Stage 11: 이적 시스템 (V0.1: 단순)

### Task 11.1: TransferSystem
- [ ] `Application/TransferSystem.cs`
- [ ] CalculateMarketValue(player, state) → int (algorithms.md #3)
- [ ] SubmitOffer(playerId, fromClubId, toClubId, amount, contract, state) → TransferOffer
- [ ] ProcessOffers(state): 매일 호출, AI 응답 처리
- [ ] CompleteTransfer(offerId, state): 최종 체결 처리
- **완료 조건:** 오퍼 제출 → AI 응답 → 체결까지 전 흐름 동작

### Task 11.2: 이적시장 검색
- [ ] TransferSystem.SearchPlayers(filter, state) → List<Player>
- [ ] 단순 LINQ 필터링 (V0.1 충분)
- **완료 조건:** 포지션 / 연령 필터로 정확한 결과 반환

---

## Stage 12: 시즌 사이클

### Task 12.1: SeasonEndProcessor
- [ ] `Application/SeasonEndProcessor.cs`
- [ ] data-flows.md #6 시퀀스 구현
- [ ] 시상 / 사기 정산 / 계약 만료 / 은퇴 / Match 데이터 압축
- **완료 조건:** 시즌 종료 호출 시 다음 시즌 정상 시작 가능

### Task 12.2: 신규 시즌 시작
- [ ] 6월 1일 도래 시 자동 처리
- [ ] 새 일정 생성 / 토큰 지급 / 보드 목표
- **완료 조건:** 2시즌 연속 진행 시 데이터 일관성 유지

---

## Stage 13: UI (최소 기능)

> UI는 "예쁘게"가 아닌 "동작하게"가 V0.1 목표.

### Task 13.1: 메인 메뉴 씬
- [ ] New Game / Load Game 버튼
- [ ] 시드 입력 옵션
- **완료 조건:** 새 게임 / 세이브 로드 시 메인 게임으로 전환

### Task 13.2: 구단 선택 화면
- [ ] 리그 내 구단 목록 표시
- [ ] 선택 시 GameState.userClubId 설정
- **완료 조건:** 구단 선택 후 다음 단계 진행

### Task 13.3: 스타팅 가챠 화면
- [ ] 4라인 티어 표시
- [ ] 리롤 버튼 + 남은 횟수
- [ ] 확정 버튼
- **완료 조건:** 리롤/확정 흐름 정상 동작

### Task 13.4: 메인 대시보드
- [ ] 현재 날짜 / 다음 경기 / 사기 등 요약
- [ ] "Continue" 버튼
- [ ] 사이드 메뉴 (스쿼드, 유스, 이적, 일정, 시설)
- **완료 조건:** 모든 메뉴 접근 가능, Continue로 시간 진행

### Task 13.5: 스쿼드 화면
- [ ] 1군 / 유스 명단 표시
- [ ] 선수 클릭 시 프로필 화면
- **완료 조건:** 선수 목록 / 프로필 표시 정상

### Task 13.6: 선수 프로필
- [ ] 능력치 / 트레잇 / 계약 / 이력
- **완료 조건:** 데이터 모두 표시

### Task 13.7: 유스 풀 화면
- [ ] 인스펙션 이벤트 발생 시 자동 표시
- [ ] 후보 목록 + 시설 등급별 정보
- [ ] 영입 / 리롤 버튼
- **완료 조건:** 인스펙션 전 흐름 UI로 동작

### Task 13.8: 이적 검색 화면
- [ ] 필터 + 결과 목록
- [ ] 오퍼 제출 화면
- **완료 조건:** UI로 이적 흐름 가능

### Task 13.9: 일정 / 결과 화면
- [ ] 캘린더 또는 라운드별 일정
- [ ] 경기 결과 표시
- **완료 조건:** 라운드별 결과 확인 가능

### Task 13.10: 리그 순위표
- [ ] 순위 표시
- **완료 조건:** 정확한 순위 표시

### Task 13.11: 시설 화면
- [ ] 현재 등급 / 업그레이드 버튼
- **완료 조건:** 업그레이드 발주 가능

---

## Stage 14: 디버그 도구

### Task 14.1: 디버그 윈도우
- [ ] `Editor/DebugWindow.cs` (EditorWindow)
- [ ] 시간 강제 진행 (1일 / 1주 / 1개월)
- [ ] 자금 추가
- [ ] 토큰 추가
- [ ] 강제 부상 / 회복
- **완료 조건:** Window 메뉴에서 호출 가능, 모든 버튼 동작

### Task 14.2: 디버그 모드 토글
- [ ] GameBalanceSO에 isDebugMode 필드
- [ ] 디버그 모드 시 잠재력 노출 등
- **완료 조건:** 토글에 따라 UI 정보 다르게 표시

---

## Stage 15: 통합 테스트

### Task 15.1: 한 시즌 완주 테스트
- [ ] 새 게임 → 시즌 진행 → 종료까지 한 번 플레이
- [ ] 발견된 버그 / 어색함 기록
- **완료 조건:** 한 시즌이 끝까지 진행됨 (크래시 없음)

### Task 15.2: 세이브 일관성 테스트
- [ ] 임의 시점 저장 → 종료 → 로드 → 같은 상태인지
- **완료 조건:** 세이브/로드 전후 GameState 일치

### Task 15.3: 시드 결정성 테스트
- [ ] 같은 시드로 두 번 시작 → 모든 데이터 동일한지
- **완료 조건:** 시드 결정적 동작 확인

---

## Stage 완료 후

V0.1 완성 시 상태:
- ✅ 한 시즌 끝까지 플레이 가능
- ✅ 세이브/로드 정상 동작
- ✅ 유스 인스펙션 + 리롤 토큰 시스템 동작
- ✅ 스타팅 가챠 동작
- ✅ 이적 시스템 기본 동작
- ✅ 디버그 도구 사용 가능
- ❌ 경기 텍스트 이벤트 (V1.0)
- ❌ 부상 / 카드 (V1.0)
- ❌ UI 폴리싱 (V1.x)

---

## 작업 시 주의사항

### 의존성 순서 지키기
위에서 아래로 작업. 건너뛰면 다음 작업에서 막힘.

### 각 Task 완료 조건 검증
완료 조건이 "동작 확인"인 경우 실제로 돌려보고 확인. 통과 못 한 채 다음으로 넘어가지 않기.

### 명세 참조 의무
- Stage 6 (선수 생성) → `algorithms.md` #1 필수
- Stage 9 (경기 시뮬) → `algorithms.md` #2 필수
- Stage 10 (유스) → `algorithms.md` #4 필수
- Stage 11 (이적) → `algorithms.md` #3 필수

알고리즘 명세 없이 진행하면 추측 발생 → 다음 세션에서 충돌 위험.

### 발견 사항 기록
- 새 결정 필요 → `design-decisions.md` 추가
- 명세 미비 → `algorithms.md`에 QUESTION 추가
- TBD 해결 → 해당 문서 갱신

---

## Change Log

| Date | Change |
| --- | --- |
| 2025-05-15 | V0.1 작업 분할 초안 작성 |
| 2026-05-19 | Task 6.2 ClubGenerator 명세 작성 (algorithms.md #5, design-decisions.md #27/#28). 가변 clubCount/playersPerClub 대응 + V1.0+ 보완 포인트 분리 명시. |
| 2026-05-19 | Task 2.3 마무리 (#76). GameManager 에 State/SetState/UserClub 추가. design-decisions.md #29 — GameManager 는 Core Layer 결정. 의존 방향 정통화 (Domain 독립 + Core→Domain). 세 문서 정합 통일. |
| 2026-05-19 | Task 6.2 Sub-A (#79) — GameBalanceSO ClubGen 필드 ~36개 추가, LeagueConfigSO.clubNames 추가, SeedV01Data 에 가상 EPL 구단명 20개 입력 (명성 내림차순). |
| 2026-05-19 | Task 6.2 Sub-B (#82) — ClubGenerator 본 구현 + T1~T7 EditMode 테스트 통과. Task 6.2 완료. |
| 2026-05-19 | Task 7.2 (#86) — ScheduleGenerator 구현 + T1~T9 EditMode 테스트 통과. Circle method 더블 라운드 로빈, 가변 clubCount 대응. |
| 2026-05-19 | Task 7.3 Sub-A (#88) — Starting Squad Gacha 명세 작성 (algorithms.md #6, design-decisions #28 갱신 / #30~32 추가). 4-4-2 단일 포메이션 + FormationConfig 도입으로 V1.0 가챠 랜덤화 확장 경로 준비. |
| 2026-01-12 | Issue Type 마이그레이션 (#90) — `type:*` 라벨 60개 이슈 → GitHub Issue Type 필드. github-workflow.md 갱신. |
| 2026-01-12 | Task 7.3 Sub-B+C (#92) — GameBalanceSO 의 `squadGK~squadCF` 13개 필드 제거 + `FormationConfig` nested + Gacha 티어 컷. ClubGenerator 가 `BuildSquadComposition(rng, balance)` 으로 동적 분배 (필수 23 + 랜덤 2자리). ClubGeneratorTests T3 → 조건 검증 + T3b 다양성. |
| 2026-01-12 | Task 7.3 Sub-D (#94) — GameState.nextPlayerId 필드 추가 (단일 진실의 원천). GameStateTests + SaveSystemTests 라운드트립 검증. Stateless 원칙 유지 — ClubGen 시그니처 미변경. |
| 2026-01-12 | Task 7.3 Sub-E (#97) — StartingSquadGacha 본 구현 + T1~T7 통과. EvaluateSquad (4라인/명성 대비 비율/ACE) + RerollSquad (재생성 + 새 id + 토큰 차감). ClubGen.RegenerateSquad public 메서드 추출하여 공용. Task 7.3 완료. |
| 2026-01-12 | Task 7.1 (#99) — GameInitializer.NewGame 구현 + T1~T7 통과. data-flows.md #1 시퀀스 통합 (League + ClubGen + ScheduleGen + Standings). GameState.userClubId 디폴트 -1 sentinel 변경. **Stage 7 완료.** |
| 2026-01-12 | Task 8.2 (#101) — DailyProcessor.Run 구현 + T1~T7 통과. fatigue 회복 + 부상 카운트다운. Stateless 시스템 (`#3` 원칙). |
| 2026-01-12 | Task 8.1 (#103) — EventScheduler.Run 구현 + T1~T6 통과. 오늘 매치 식별 + MatchDayEvent 발행 + 정지 신호 반환. Core/Events/MatchEvents.cs (MatchDayEvent) 신규. |
| 2026-01-12 | Task 8.3 (#105) — GameLoop.AdvanceDay / ContinueUntilStop 구현 + T1~T6 통과. design-decisions.md #29 보완 (GameManager 책임 경계 — State 보유만). **Stage 8 완료.** |
| 2026-05-19 | Task 9.1 Sub-A (#109) — Match Simulation 명세 작성. algorithms.md #2 (V0.1 전체 명세 + V1.0+ Migration Notes). design-decisions.md #33 (V0.1 정책 — 단순 CA 합 + Poisson + 홈 어드밴티지 + 라인 가중 득점자) / #34 (V1.0+ 이벤트 시퀀스 진화 경로 — 인터페이스 유지). data-flows.md #3 비활성 구단 분기 V0.1 단일 메서드로 갱신 (SimulateLite 폐기). glossary.md 용어 추가. Sub-B (`NextPoisson` 헬퍼) / Sub-C (본 구현) 는 별도 이슈로 분리. |
| 2026-05-19 | Task 9.1 Sub-B (#111, PR #112) — RngExtensions.NextPoisson (Knuth 알고리즘) 구현 + EditMode 테스트 5개 (평균/분산/결정성/λ=0/λ<0 예외/약팀 분포) 통과. |
| 2026-05-19 | Task 9.1 Sub-C (#113, PR #114) — MatchSimulator 본 구현 + T1~T7 (+ T2b/T2c) 통과. 구현 검증 중 단순 선형 ratio 결정력 부족 발견 → **strengthExponent (k=1.5) 보강** (V0.1 임시 변통, V1.0+ 매치 엔진 재작성 시 폐기). design-decisions.md #33 보강 (strengthExponent 섹션 + V1.0+ 폐기 트리거). algorithms.md #2 4단계 의사코드 + Balancing + T3 임계치 + V1.0 Notes 갱신. **Task 9.1 완료.** |
| 2026-05-19 | Chore (#115, PR #116) — GameBalance.asset 시드 자산 갱신 (homeAdvantageBonus 제거 + homeAdvantageGoalBonus / strengthExponent / scoringWeightByLine 신규). |
| 2026-05-20 | Task 9.2 (#117) — MatchPostProcessor 구현 + T1~T6 통과. data-flows.md #3 [4] 명세 그대로 (algorithms.md 신규 섹션 X). MatchEvents.cs 에 MatchFinishedEvent 추가. **폼/사기 갱신은 V0.1 미구현 (#30 V1.0+, data-flows.md 명세 갱신 — 평점 시스템 부재로 묶음 V1.0 도입)**. event-bus-catalog.md MatchFinishedEvent V0.1 구현 표시 갱신. 작업 시작 전 머지된 4 브랜치 (feature/28-player-generator, docs/109, feature/113, chore/115) 일괄 정리. |
| 2026-05-20 | Task 9.3 (#119) — BackgroundSimulator 구현 + GameLoop.AdvanceDay 통합 + T1~T5 통과. data-flows.md #2 [4] / #3 [6] 명세 그대로. **옵션 A 채택 — V0.1 단순화** (활성/비활성 구분 X, 모든 매치 MatchFinishedEvent 발행, UI 없으니 구독자 0 → 비용 ~0). data-flows.md #3 비활성 발행 생략을 V1.0+ 트리거 (publishEvent 옵션 도입) 로 이동. Task 8.3 비고 갱신 (BackgroundSimulator 통합 완료). **Stage 9 완료** ✅. |
| 2026-05-20 | Docs (#121) — github-workflow.md PR 메타데이터 규칙 명시. §2 PR Metadata 섹션 신규 + §3 Projects 항목에 FM-Lite 보드 (#50) 명시 + §9 PR gh CLI 예시. PR 부터 이슈와 동일하게 label/milestone/assignee/Projects 채움. 사용자 요청 ("앞으로 PR 넣을 때 label, assignee, milestone, projects 칸도 다 채워줘"). memory `feedback_pr_metadata.md` 동기화. |
| 2026-05-20 | Task 10 Sub-A (#123) — Youth Pool Generation 명세 작성. algorithms.md #4 신규 (V0.1 전체 + V1.0 Migration Notes 10개 항목). design-decisions.md #35 (V0.1 정책 — PA 진실값+CA derived 역방향 / 스타 픽 5% PA bonus / 시드=`currentDate.Ticks`+`userActionHash` 결합 / 시설 통합 / 미영입 단순 제거 / 나이 가중치 16=40·17=40·18=20 / 자국 78%) + #36 (`GameState.nextIntakeId`). data-flows.md #4 갱신 (시드 / 트리거 6/15·1/15 / V0.1 단순화 / V1.0+ 트리거). event-bus-catalog.md Youth Events 상태 갱신. Stage 10 Sub-A/B/C 분할 명시 (Task 10.1/10.2/10.3 같은 파일이라 코드는 Sub-B 단일 PR). |
