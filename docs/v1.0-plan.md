# V1.0 계획서 (Plan)

> **상태:** 보강본 (2026-05-29 갱신). V0.5 마무리 빌드 + 플레이테스트 피드백 + 이월 (Cup) + UX 폴리시 전면 + **UI 일관성 / Options / 사운드 / 통화 / Unity MCP / 매치 대시보드 / 훈련 / 비교 도구** 확장.
>
> **목적:** V1.0 작업 시작 전 단일 광범위 계획서. **이 문서가 합의되면** → 영역별 algorithms / data-flows / design-decisions 명세 갱신 + `v1.0-tasks.md` 작업 분할 + GitHub 이슈 일괄 생성으로 이어진다.
>
> **선행 문서:** `docs/v0.5-build-notes.md` §2 / `docs/v0.5-build-report.md` §5.2 §7 / 각 `docs/design-decisions.md` V0.5+ 보완 포인트 / `docs/algorithms.md` V0.5 Migration Notes / `docs/unity-mcp-setup.md` (Stage 0).

---

## 0. 문서 사용 방법

1. **§2 V0.5 피드백** = 사용자가 직접 짚은 항목 — V1.0 우선순위 최상.
2. **§3 범주별 명세** = 각 영역별 V0.5 동작 / V1.0 변경 / 영향 범위 / 검토 필요 결정. 본 문서가 단일 진실의 원천.
3. **§4 Stage 분할** = 의존성 순서 기준 작업 단위 — `v1.0-tasks.md` 의 골격.
4. **§5 Out of Scope** = V1.0 에서 명시적으로 안 함 (V1.x 또는 영구 제외).
5. **§6 영향 매트릭스** = 도메인 / 시드 / 세이브 마이그레이션 / 기존 docs 갱신 추적.
6. **§7 Open Questions** = 본 문서 작성 중 발견한 미결 항목. **모두 해결 후** Stage 분할로 진입.

---

## 1. V0.5 → V1.0 비전

### V0.5 (마무리 — 2026-05-29)
- "한 시즌이 재미있게 진행" — 매치 시뮬 / 사기 / 트레잇 / 시설 / 시즌 시상 작동.
- 단점: UX 자유도 침해 (강제 씬 전환), UI 폴리시 부족, 매치 디테일 단순, 일부 씬 미완.

### V1.0 목표 단어
> **"UX 가 매끄럽고 (인박스 / 자유도 / 시각화), 매치가 자세하고 (신체 / 멘탈 / 옵션), 시장이 정교하다 (협상 / 컵 / 자동 저장)."**

### 비전 vs V0.5 차이 한 줄
- V0.5: "이벤트 발생하면 강제로 그 화면에 던져진다."
- V1.0: "이벤트는 피드에 쌓인다. 사용자가 골라서 처리한다. 기한 있는 것만 추궁한다."

---

## 2. V0.5 플레이테스트 피드백 (2026-05-29)

> 사용자가 V0.5 빌드 플레이 후 직접 제공. **V1.0 우선순위 최상 항목들** — 본 문서 §3 의 각 영역 결정의 기반.

### 2.1 Dashboard 자유도
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 강제 전환 | 유스 인스펙션 / 매치 / 보드 약속 / CounterOffer 발생 시 예고 없이 씬 전환 또는 패널 노출. 자유도 침해 | §3.1 — FM 식 피드/인박스 (알림만, 클릭해 처리). 기한 있는 항목만 추궁 |

### 2.2 Player Profile + Stats UI
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 색상 코딩 | 모든 stat 동일 색상 → 강약 한눈에 안 보임 | §3.2 — stat 값 → 색상 등급 (예: 80+ 진녹 / 50-80 보통 / 50- 빨강) |
| 한 화면 | 스크롤 필요 → 가독성 ↓ | §3.2 — 한 화면 레이아웃 |
| 신체 조건 | 키 / 몸무게 등 미반영 | §3.2 + §3.17 — Player.physical 신규 + 경기 영향 (헤더골 / 민첩성 / 등) |

### 2.3 Squad 화면
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 검색 동작 안 함 | N.6 머지 후 회귀로 추정. 작동 안 함 | §3.3 — bug fix |
| 성장 동향 | 시각화 부재 | §3.3 — 화살표 각도 / 색으로 성장 추세 표시 |

### 2.4 유스 관리 분리
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| Dashboard 유스 버튼 | 인스펙션 직행 = 평시 의미 X (정해진 날짜만 발동) | §3.4 — **YouthManagementScene 신규** — 다음 풀 예고 (수준 / 특징 / 수) + 현 유스 성장 동향 |

### 2.5 Transfer 검색
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| CA 검색 | 스카우트 시스템 무력화 — CA 정확히 알 수 있는 검색은 모순 | §3.5 — CA 필터 폐기 |
| 세부 stat 검색 | 특정 stat ≥ N 형태 | §3.5 — 세부 stats 필터 추가 |
| 주급 필터 | 미존재 | §3.5 — 주급 필터 추가 |
| 필터 패널 분리 | 인라인 필터 → 옵션 클릭 → 패널 → 적용 → listing | §3.5 — UI 패러다임 변경 |

### 2.6 Schedule
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 컵 대회 탭 | 컵 추가 시 리그 / 컵 / 통합 탭 | §3.12 + §3.14 |

### 2.7 Facility
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| MUIP 갈아엎기 | 현재 UI 단순 | §3.11 — 전 씬 MUIP 폴리시 일환 |
| 효과 와닿지 않음 | 시설 투자 동기 ↓ | §3.11 — 효과 밸런싱 + 시각화 (예: 부상 회복 일수 차트 / 성장률 표시) |

### 2.8 Standings
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| UI 폴리시 | 기능 OK, 시각만 | §3.12 — MUIP 갈아엎기 |

### 2.9 Save
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 파일명 사용자 지정 | 같은 시드 여러 시점 세이브 구분 어려움 | §3.13 — 사용자 입력 가능 + 자동 저장 옵션 |

### 2.10 Tactic
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| Dropdown 문제 | 가림 / 영역 침범 + 중구난방 | §3.7 — 싹 뜯어고치기 |
| 포메이션 도식 + 드래그 | 도식 위에 선수 드래그 배정 | §3.7 |
| 전술 상성 | 전술 종류 (포메이션 / 역할) 간 상성 | §3.7 + §3.6 (매치 엔진 입력) |
| 선수 조합 시너지 | 빅앤스몰 / 빠른윙+타겟포워드 등 | §3.7 + §3.6 |
| 피지컬 / 멘탈리티 반영 | 매치 결과에 더 큰 영향 | §3.6 + §3.17 |

### 2.11 Lineup
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| Tactic 과 통합 | FM 시리즈 식 | §3.7 — Tactic ↔ Lineup 통합 |
| 매치 전 점검 | 라인업 / 전술 확인 화면 | §3.7 — pre-match 화면 |
| 상대 라인업 표시 | pre-match 시점 | §3.7 |

### 2.12 Mentoring
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 동작 불안정 | UI 싹 뜯어고치기 | §3.8 |

### 2.13 Match Simulation
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 너무 단순 | 매치 결과 디테일 부족 | §3.6 |
| 보기 옵션 | 핵심만 / 골만 / 전체 옵션 | §3.6 — viewMode |
| 전체 옵션 | 패스 / 턴오버 / 스로인 / 프리킥까지 모두 표시 | §3.6 + §3.9 |
| 신체 / 멘탈 반영 | 결과 정밀화 | §3.6 + §3.17 |

### 2.14 MatchTextScene (V0.5 미완)
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 가속 / 결과 | ×1/×2/×4 / 스킵 + 결과 화면 | §3.9 |

### 2.15 PromiseInbox (V0.5 미완)
| 항목 | 피드백 | V1.0 매핑 |
| --- | --- | --- |
| 진행률 시각화 | 출전시간 35% / 목표 50% 형태 | §3.10 |

---

## 3. 범주별 명세

### 3.1 UX 패러다임 — Dashboard 인박스 / 자유도 (사용자 피드백 2.1)

**V0.5 동작:**
- 특정 이벤트 (유스 인스펙션 / 매치 / 보드 약속 / CounterOffer / Happiness < 20 TransferRequest / 1군 콜업) 발생 시 강제 씬 전환 또는 자동 패널 노출.
- 사용자가 "이벤트를 처리하지 않으면 진행 불가" 같은 압박.

**V1.0 변경:**
- **FM 식 피드 / 인박스 패러다임** — 이벤트 = 알림만 누적. 사용자가 클릭해 확인 / 처리.
- 강제 처리 (씬 강제 전환 / 모달 잠금) 항목 = **기한 있는 결정만** (예: CounterOffer 7일 응답 / 보드 약속 시즌 시작 즉시 수락/거절).
- 기한 없는 항목 (Promise 일반 진행 / 어워드 알림 / 사기 변동 / 1군 콜업 권장) = 인박스 누적, 사용자가 골라 처리.
- **Dashboard 인박스 확장**: 카테고리 탭 (전체 / 매치 / 이적 / 사기 / 보드 / 유스), 읽음/안읽음 표시, 만료 임박 강조.

**영향 범위:**
- `DashboardController.OnContinueClicked` — CounterOffer 자동 라우팅 제거 (인박스 알림으로 대체)
- `Inbox` 도메인 신규 또는 기존 in-memory inbox 확장 + 카테고리 / 기한 / 우선순위 필드
- `EventBus` 모든 UI-routing 이벤트 → InboxItem 변환 미들웨어
- `data-flows.md` Dashboard 진입 시퀀스 갱신

### 3.2 Player Profile + Stats UI (사용자 피드백 2.2)

**V0.5 동작:**
- PlayerProfile = 스크롤 형식. Stats 49 필드 동일 색상. 신체 조건 미반영.

**V1.0 변경:**
- **한 화면 레이아웃** — 4 카테고리 (Technical / Mental / Physical / Goalkeeping) 2×2 또는 4-column 그리드. 49 필드 한 눈에.
- **색상 코딩** — stat 값 기준 5단계 색상 (외부화 임계치):
  - 80+ 진녹색 (Elite)
  - 65-79 녹색 (Good)
  - 50-64 회색 (Average)
  - 35-49 주황 (Weak)
  - 34- 빨강 (Poor)
- **신체 조건 표시** — 키 (cm) / 몸무게 (kg) / 우세 발 / 약발 정도. PlayerInfo / Player.physical 신규 필드.
- **§3.17 신체 조건 → 경기 영향** 연동.

**영향 범위:**
- `PlayerProfileController` 전면 재작업
- `Player.physical: PhysicalAttributes` 신규 (height / weight / preferredFoot / weakFootAbility)
- `PlayerGenerator` 신체 조건 추첨 (포지션별 평균 + 편차)
- `MatchSimulator` 일부 이벤트 (헤더 / 민첩성 / 속도) 분기 추가

### 3.3 Squad 화면 (사용자 피드백 2.3)

**V0.5 동작:**
- 자기 구단 + 유스 탭. 검색 기능 작동 안 함 (N.6 회귀).

**V1.0 변경:**
- **검색 fix** — 이름 / 포지션 / 나이 / CA 범위 (자기 구단은 정확) 정상 동작.
- **성장 동향 시각화** — 각 선수의 최근 1개월 / 3개월 stat 변화량을 화살표 (각도) + 색상 (녹/회/빨) 으로 표시.
- 데이터 — `Player.career` 의 직전 시즌 stat 평균 vs 현재 stat 비교 또는 별도 `Player.growthHistory: List<StatSnapshot>` (월 1회 스냅샷).
- **유스 탭은 §3.4 신규 씬으로 이관**.

**영향 범위:**
- `SquadController` 검색 회귀 fix
- `Player.growthHistory` 신규 (월 1회 GrowthSystem 에서 스냅샷)
- UI 화살표 / 색상 위젯
- SaveMigration 신규 필드

### 3.4 Youth 관리 씬 신규 분리 (사용자 피드백 2.4)

**V0.5 동작:**
- Dashboard "유스" 버튼 → YouthScene (인스펙션 화면). 평시 클릭해도 후보 없음.

**V1.0 변경:**
- **YouthManagementScene 신규** — Dashboard "유스" 버튼 = 이 씬으로.
- 표시 항목:
  - 다음 인스펙션 예고 (날짜 / D-N + 시설 등급 기반 풀 사이즈 예측 / 평균 PA 예측)
  - 현 유스 스쿼드 목록 (Squad 유스 탭 이관) + 각 선수 성장 동향 (§3.3)
  - 1군 콜업 후보 (자동 트리거된 18세+CA70% 선수 목록)
  - Mentoring 그룹 요약 (현재 그룹 수 / 효과 진행률)
- **YouthScene (인스펙션 화면)** = 6/15 / 1/15 도래 시에만 진입 (이벤트 트리거).

**영향 범위:**
- `YouthManagementController` 신규 + 씬 신규
- `DashboardController.OnYouthClicked` 분기 (정해진 날짜 = YouthScene / 평상시 = YouthManagementScene)
- 인스펙션 예고 표시 헬퍼 (시설 등급 → 예상 풀 사이즈 / PA)

### 3.5 Transfer 검색 (사용자 피드백 2.5)

**V0.5 동작:**
- 인라인 필터 (포지션 / CA 범위 / 연령 / 국적 / 트레잇 / 시장가 / 계약 잔여). CA 검색 가능 → 스카우트 시스템 모순.

**V1.0 변경:**
- **CA 필터 폐기** — 스카우트 시스템 (정확도 차등) 과 모순. 자기 구단 선수는 정확 표시되지만 검색 필터로는 X.
- **세부 stats 필터** — "Pace ≥ 75", "Finishing ≥ 70" 등. 단 Not-Scouted 선수는 검색 결과 = 정성적 라벨만 표시 (정확 수치 노출 X 유지).
- **주급 필터** — 주급 범위.
- **필터 패널 UI 분리**:
  - Transfer 화면 = 검색창 + [필터] 버튼 + 결과 listing
  - [필터] 클릭 → 패널 슬라이드/모달 → 모든 옵션 (포지션 / 나이 / 국적 / 트레잇 / 세부 stats / 주급 / 시장가 / 계약 잔여 / 스카우트 여부)
  - [적용] → 패널 닫힘 + listing 갱신

**영향 범위:**
- `TransferController` UI 재작업 (필터 패널 분리)
- `TransferSearchFilter` 도메인 확장 (statThresholds: Dictionary / wageRange: (min,max))
- Not-Scouted 선수 검색 결과 라벨링 (`ScoutingVisibility` 활용)

### 3.6 Match Simulation 디테일 (사용자 피드백 2.13 + 2026-05-29 V1.0 합의)

**V0.5 동작:**
- 5-Zone Markov + 49 stat / 이벤트 종류 (Shot / Pass / Tackle / Foul / Card / Injury / Sub / Cross / Save). 텍스트 이벤트는 일부 (~40 키).
- **플레이테스트 발견 문제 (2026-05-29)**:
  - **골이 너무 많이 나옴** — 11대 5 같은 비현실적 스코어 자주 발생. EPL 평균 (2.7) 대비 과다.
  - 텍스트로 노출되는 이벤트가 일부 (~5종) 만 → 골/키패스/선방/카드 등 핵심 이벤트가 안 보임.

**V1.0 변경:**

#### 3.6.1 5-Zone Markov 골 빈도 재밸런싱 (P0 — V0.5 플레이테스트 hotfix 격상)
- `GameBalanceSO.avgGoalsPerMatch` 기존값 검토 → 2.7 ± 0.3 으로 강제 수렴.
- 조정 후보 파라미터 (`algorithms.md` V0.5-2 / V1.0-2):
  - **Shot success rate (`attEff / (attEff + defEff)` 분모 가중)** — GK reflexes/handling 가중치 ↑
  - **Box 진입 확률** — AttackingThird → Box 전이 확률 ↓ 검토
  - **action / 분 분포** — 매 분 1~3 action 의 평균값 ↓ 검토
  - **homeAdvantage** 곱셈 ↓ (현 1.1 → 1.05)
  - **strengthExponent** (V0.1 변통, 폐기됨 — Markov 라 영향 X)
- **검증 시나리오**: 시즌 1회 완주 후 380 매치 평균 골수 측정 → 2.4~3.0 범위 진입까지 반복 튜닝.

#### 3.6.2 이벤트 종류 확장
- 패스 (성공/실패), 턴오버 (Interception / BallLost), 스로인 (LongThrow 외 일반), 프리킥 (Direct / Indirect), 코너킥 (이미 SetPieceResolver — 텍스트 강화), 오프사이드, 골키퍼 펀칭 / 캐치.

#### 3.6.3 신체 조건 / 멘탈리티 반영 (§3.17)
- 헤더 이벤트 → 키 × 점프 + `jumpingReach`
- 민첩성 (드리블/회피) → `agility × (180/height)` (키 작을수록 보너스)
- 속도 (스프린트) → `pace + weakFootAbility × 0.5`
- 빅매치 / 페널티 → `pressureHandling` (Hidden) + `composure`

#### 3.6.4 선수 조합 시너지 (§3.8 합의 — 10+ 카탈로그)
- 빅앤스몰 (큰 ST + 키 작은 윙) / 윙+타겟포워드 / 빠른 윙+크로스 / 온볼이마이웨이 / 골니아 / 서프-스테파 / 등.
- `TacticImpact` 확장. 자세한 카탈로그는 `algorithms.md` V1.0-3 명세.

#### 3.6.5 전술 상성
- Formation × Formation 매치업 행렬 (외부화 SO). 예: 4-3-3 vs 3-5-2 → 4-3-3 측 strength 보너스.
- `FormationMatchupSO` 신규.

#### 3.6.6 viewMode 폐기 — 단일 모드 (V1.0 변경, 2026-05-29 사용자 결정)
- V1.0 초안의 `viewMode (KeyOnly / GoalsOnly / All)` 폐기.
- **모든 매치는 단일 모드** — 모든 핵심 이벤트 (Goal / KeyPass / Save / Card / Injury / SubstitutionAI / Cross / Foul / Penalty / Free Kick / Corner) 가 텍스트로 노출.
- **사소한 이벤트** (성공 패스 / Midfield 점유 갱신 등) 는 통계만, 텍스트 비기록.
- `collectEvents` 플래그는 유저 매치 / 비활성 매치 분기에만 사용 (텍스트 수집 여부).

**영향 범위:**
- `MatchSimulator` zone resolution 확장 (이벤트 종류 추가)
- `Player.physical` 활용
- `TacticImpact` 조합 시너지 계산
- `FormationMatchupSO` 신규 (전술 상성표)
- `Match.events` 확장 (MatchEventType 신규 항목)
- `algorithms.md` V1.0-2 갱신 + 시너지 / 매치업 명세 신규

### 3.7 Tactic + Lineup 통합 / 도식 (사용자 피드백 2.10 + 2.11)

**V0.5 동작:**
- TacticScene (Formation + Role + Duty + Mentality + SetPieces) + LineupScene (11 슬롯 배정 + SetPieces 키커) **분리**.
- Tactic dropdown 가림 / 영역 침범 문제.

**V1.0 변경:**
- **TacticLineupScene 통합** (FM 시리즈 식) — 단일 화면에 다 들어옴.
- **포메이션 도식 + 드래그 배정**:
  - 4-4-2 / 4-3-3 / 등 포메이션 도식 (피치 + 11 점)
  - 우측 패널에 선수 목록 (Squad)
  - 드래그 & 드롭으로 슬롯 배정
  - 각 슬롯 클릭 → Role / Duty 선택 팝업 (dropdown 폐기)
- **매치 전 점검 화면** — 매치 시작 5분 전 (Dashboard Continue → MatchPreviewScene) — 라인업 + 전술 + Mentality + 상대 라인업 표시 + [확인 후 시작] / [수정].
- **전술 상성 / 선수 조합 시너지** §3.6 와 연동 — pre-match 시 시너지 표시 (예: "빅앤스몰 시너지 +5%").

**영향 범위:**
- `TacticScene` + `LineupScene` 폐기 → `TacticLineupScene` 신규
- `MatchPreviewScene` 신규
- 도식 위 드래그 UI (UIElements 또는 MUIP 드래그 컴포넌트)
- Role/Duty 팝업 위젯 (dropdown 대체)

### 3.8 Mentoring UI 재작업 (사용자 피드백 2.12)

**V0.5 동작:**
- MentoringScene 코드 동작. UI 불안정 (씬 wiring 미완).

**V1.0 변경:**
- **씬 전면 재작업** — MUIP 활용.
- 그룹 만들기 / 해체 안정화.
- 진행률 표시 — Mentor → Mentee 각 Hidden Attr 수렴 진행률.
- Mentor 추천 (leadership / age 기반 자동 추천).

**영향 범위:**
- `MentoringController` UI 로직 정비
- 씬 prefab 재작업
- Localization 키 보완

### 3.9 MatchTextScene 완성 (V0.5 미완 + V1.0 단순화)

**V0.5 동작:**
- 씬 진입 OK. Match.events 분 순서 표시. ×1/×2/×4 가속 / 스킵 / 결과 화면 미완.

**V1.0 변경 (2026-05-29 합의 — viewMode 폐기):**
- **가속만 유지**: ×1 (실시간) / ×2 / ×4 / 즉시 결과 (스킵). 단축키 + UI 버튼.
- **보기 옵션 폐기** — §3.6.6 일관. 모든 매치는 단일 모드, 모든 핵심 이벤트 텍스트로 노출.
- **결과 화면 = §3.23 매치 후 결과 대시보드 통합** — 스코어 + 득점자 + 어시스트 + 평점 표 + 카드/부상 + (V1.0 신규) 히트맵 / 슛맵 / 통계 시각화.
- **상대 라인업** §3.7 와 연동 표시 가능 (pre-match 에서 노출되므로 결과 화면에선 선택적).
- **텍스트 생동감 강화** — V0.5 ~40 키 → V1.0 ~150 키 확장. 다양한 표현 (예: 골 한 종류 → 5종 변형 — "환상적 슈팅" / "행운의 굴절" / "PK 침착" 등). 같은 이벤트도 시드 기반 표현 회전.

**영향 범위:**
- `MatchTextController` 가속 / 결과 화면 (대시보드 통합) / viewMode 제거
- LocalizationSO 매치 이벤트 키 ~150 확장
- §3.23 MatchResultDashboard 연동

**영향 범위:**
- `MatchTextController` 가속 / 보기 옵션 / 결과 화면
- `Match.events` 의 viewMode 필터링 헬퍼

### 3.10 PromiseInbox 진행률 (V0.5 미완)

**V0.5 동작:**
- 씬 진입 OK. active promises 목록 표시. 진행률 표시 미완.

**V1.0 변경:**
- **진행률 시각화** — Promise 종류별:
  - PlaytimeAgreement: "출전시간 N% / 목표 M%" 진행률 바 + D-day
  - TransferIn: "해당 포지션 영입 0/1" + D-day
  - Renewal: "재계약 협상 X 일 남음"
  - TransferOut: "이적 협상 X 일 남음"
- **카테고리 필터** — Active / Fulfilled / Broken.
- §3.1 Dashboard 인박스와 연동.

**영향 범위:**
- `PromiseInboxController` 진행률 위젯
- `Promise.GetProgress(state)` 헬퍼 (종류별)

### 3.11 Facility UI + 밸런싱 (사용자 피드백 2.7)

**V0.5 동작:**
- 8 시설 × 10단계 동작. 효과 와닿지 않음. UI 단순.

**V1.0 변경:**
- **UI MUIP 갈아엎기** — 시설별 아이콘 / 등급 시각화 (스타 / 바) / 업그레이드 진행률.
- **효과 시각화**:
  - Training Lv1 vs Lv10 → "성장률 +X%/y" 표시
  - Medical → "부상 회복 평균 N일 → M일" 비교
  - Scout → "명단 ~N 명 / CA 정확도 ±M" 비교
  - 등
- **효과 밸런싱** — 등급 차이가 더 의미 있게:
  - Training: Lv1 = 거의 효과 X, Lv10 = 평범 선수도 ace 성장 가능
  - Medical: Lv10 = 부상 회복 50% 단축
  - Scout: Lv10 = 사실상 전체 정확 / Lv1 = 자기 리그 일부만
- 외부화 곡선 재산정.

**영향 범위:**
- `FacilityScene` UI 전면
- `GameBalanceSO` 시설 효과 곡선 재산정
- `FacilityEffectFormatter` 시각화 데이터

### 3.12 Standings + Schedule UI (사용자 피드백 2.6 + 2.8)

**V0.5 동작:**
- Standings 기능 OK, 시각만 단순. Schedule 단일 일정.

**V1.0 변경:**
- **Standings UI MUIP 갈아엎기** — 행 색상 / 강조 / 변동 화살표.
- **Schedule 탭 분할** — 컵 도입 후 (§3.14):
  - 리그 일정 탭
  - 컵 일정 탭
  - 통합 (날짜순 모두) 탭
- 매치 결과 인라인 표시 (스코어 + 클릭 시 MatchReport 모달).

**영향 범위:**
- `StandingsController` / `ScheduleController` UI
- 탭 위젯 (MUIP)

### 3.13 Save 파일명 + 자동 저장 (사용자 피드백 2.9)

**V0.5 동작:**
- 슬롯명 = 자동 (`slot_<구단명>_<yyMMdd_HHmm>`). 자동 저장 X.

**V1.0 변경:**
- **사용자 지정 슬롯명**:
  - [신규] 클릭 → 슬롯명 입력 다이얼로그 (기본값 = 자동 형식)
  - 같은 시드 여러 시점 세이브 구분 가능
- **자동 저장 옵션** (V0.5+ design-decisions 보완 포인트):
  - 옵션 ON: 시즌 종료 / 새 시즌 시작 / 매치 종료 (단 유저 매치만) 시 자동 저장
  - 슬롯 = `autosave_<구단명>_<season>_<event>` 형식
  - 최근 3개만 보관, 나머지 자동 삭제

**영향 범위:**
- `SaveSystem.SaveAs(slotName)` 시그니처 확장
- `DashboardController.OnSaveClicked` 다이얼로그
- `EventScheduler` / `MatchPostProcessor` 자동 저장 훅

### 3.14 컵 대회 (Stage Q 이월 — V0.5 원안 스코프)

**V0.5 동작:**
- 미구현. I.11 연장/승부차기 인프라만 V0.5 에 구축.

**V1.0 변경:**
- **단판 녹아웃 단일 컵** (FA Cup 모티브):
  - 시즌 시작 시 대진표 생성 (20 구단 → 1라운드 10 매치 → ... → 결승)
  - 라운드 일정 = ScheduleGenerator 가 리그 일정 사이 삽입 (예: 매월 마지막 주 평일)
  - `match.type = CompetitionType.FACup` + `allowsExtraTime = true`
- **녹아웃 진행** — 승자 진출 / 패자 탈락. 결승 후 우승 클럽 `League.history` + managerReputation 가산.
- **AI 로테이션** (선택) — AI 컵 매치 라인업 다양성.
- **UI** — 대진표 화면 (라운드별 진행) + Dashboard 다음 컵 매치 정보 + Schedule 탭 분할 (§3.12).

**영향 범위:**
- `Cup` / `CupRound` / `CupTie` 도메인 신규
- `ScheduleGenerator` 컵 라운드 삽입
- `MatchSimulator` allowsExtraTime 분기 (이미 I.11 구축됨)
- `CupBracketScene` 신규
- `algorithms.md` Cup 섹션 신규
- 명세 Stage Q.1~Q.5 (V0.5 plan §Q 유지)

### 3.15 통합 테스트 (Stage O 이월)

**V0.5 동작:**
- 단위 EditMode 50+개 통과. 통합 시나리오 미작성.

**V1.0 변경:**
- O.1 한 시즌 V1.0 시스템 통합 시나리오 (매치 + 사기 + Promise + 시상 + 컵)
- O.2 SaveMigration V0.5 → V1.0 라운드트립 (신규 필드 — Player.physical / growthHistory / Inbox 등)
- O.3 매치 텍스트 이벤트 결정성
- O.4 사기 / 약속 / 컵 시나리오

### 3.16 Localization 추가 언어 (선택)

**V0.5 동작:**
- KO / EN 다국어. ~250 키.

**V1.0 변경:**
- 추가 언어 (일본어 / 중국어 / 스페인어 — 선택). KO/EN 만 유지해도 무방.
- 누락 키 보완 (특히 매치 텍스트 이벤트 신규 종류 §3.6 / 컵 §3.14).

### 3.17 신체 조건 + 경기 영향 (사용자 피드백 2.2 + 2.13)

**V0.5 동작:**
- 신체 조건 미반영.

**V1.0 변경:**
- **Player.physical: PhysicalAttributes** 신규:
  - `height: int` (cm — 평균 178, σ 8, 포지션별 평균 차)
  - `weight: int` (kg — 평균 75, σ 10)
  - `preferredFoot: Foot` (Left / Right / Both)
  - `weakFootAbility: int` (1-5 별점, 약발 정도)
- **포지션별 평균 차** (외부화):
  - GK / CB → 키 큰 편 (평균 188, σ 6)
  - ST / Target → 키 큰 편 (평균 184)
  - LW / RW / 윙백 → 작거나 평균 (평균 175)
  - CM / AM → 평균
- **경기 영향**:
  - 헤더 (Cross 결과 → 골) → `heading × jumpingReach × (height/180)`
  - 민첩성 (드리블 / 회피) → `agility × (180/height)` (키 작을수록 보너스)
  - 속도 (스프린트) → `pace + weakFootAbility × 0.5`
  - 페널티 키커 약발 → preferredFoot 일치 시 보너스
- 외부화 — `GameBalanceSO.physical*` 계수.

**영향 범위:**
- `Player.physical` 도메인 신규
- `PlayerGenerator` 신체 조건 추첨
- `MatchSimulator` 이벤트 분기 (헤더 / 민첩성 / 속도)
- SaveMigration (신규 필드)
- §3.2 UI 표시

### 3.18 Inbox 도메인 신규 (§3.1 기반)

**V1.0 변경 (구체화):**
- `Inbox` 도메인 신규:
  ```csharp
  public class InboxItem {
      public int id;
      public InboxCategory category;     // Match / Transfer / Morale / Board / Youth / Cup / Award
      public InboxPriority priority;     // Low / Medium / High / RequiresAction
      public DateTime createdAt;
      public DateTime? deadline;         // null = no deadline
      public bool isRead;
      public string titleKey;
      public Dictionary<string, string> titleArgs;
      public string bodyKey;
      public Dictionary<string, string> bodyArgs;
      public InboxAction action;         // OpenScene / OpenDialog / None
      public string actionTargetSceneOrDialogId;
  }
  ```
- `GameState.inbox: List<InboxItem>` 신규.
- 모든 V0.5 EventBus 이벤트 (Promise / Award / Board / Mentoring / TransferRequest / YouthPromotion / OfferResponded / Injury / 등) → InboxItem 변환 미들웨어 (`InboxRouter.cs`).
- Dashboard 인박스 UI = InboxItem 카테고리 탭 + 정렬 + 읽음 처리.

### 3.19 글로벌 네비게이션 표준 (V1.0 신규 — 2026-05-29 합의)

**V0.5 동작:**
- 뒤로가기 / 저장 / 옵션 등 기본 버튼이 씬마다 위치·형태 제각각.
- 저장 = Dashboard 만, 옵션 = 부재, 메인 메뉴 = Dashboard 의 SideBar 만.
- 사용자 피드백: "씬이 전환되더라도 저런 기본적인 버튼들 (뒤로가기, 저장, 옵션 등)은 고정된 위치에 있도록 해야해."

**V1.0 변경 — TopBar + SideBar 영구 레이어:**

`GlobalNavPrefab` 신규 — 모든 컨텐츠 씬에 자동 포함되는 prefab. Canvas 별도 (Override Sorting, sortingOrder = 100) 로 항상 최상위 표시.

#### 3.19.1 TopBar (상단, height = 80px)

```
┌─────────────────────────────────────────────────────────────┐
│ ◀  2026-08-15  £12.5M  토큰3  📥(3)         ⚙ 💾 🏠       │
└─────────────────────────────────────────────────────────────┘
좌(고정)                                            우(고정)
```

| 영역 | 위치 | 컴포넌트 | 동작 |
|---|---|---|---|
| 좌상단 뒤로 | x=20, anchor top-left | MUIP Button Basic-Outline Minimal | 직전 씬으로 (PlayerPrefs `PreviousScene` 기반) |
| 날짜 | x=80, anchor top-left | TMP_Text | `currentDate` 표시 |
| 자금 | x=320, anchor top-left | TMP_Text | `clubName + £X.XM` |
| 리롤 토큰 | x=520, anchor top-left | TMP_Text + 아이콘 | `토큰 N` |
| 인박스 (§3.1/§3.18) | x=720, anchor top-left | MUIP Button + 배지 | 클릭 → 우측 슬라이드 패널 (어디서든 동일) |
| 옵션 | x=-180, anchor top-right | MUIP Button Basic-Outline Minimal | OptionsScene 추가 (스택 진입) |
| 저장 | x=-120, anchor top-right | MUIP Button | 저장 패널 모달 |
| 홈 (메인 메뉴) | x=-60, anchor top-right | MUIP Button | "정말 메인 메뉴로 ?" 확인 모달 |

#### 3.19.2 SideBar (좌측, width = 200px)

```
┌─────────┐
│ 대시보드│
│ 스쿼드  │
│ 전술    │
│ 라인업  │
│ 이적    │
│ 일정    │
│ 순위    │
│ 시설    │
│ 유스    │
│ 멘토링  │
└─────────┘
(높이: TopBar 아래 ~ 화면 끝)
```

- MUIP Button Basic-Outline 9개. 현재 씬 강조 (배경 #4A90D9 + 글자 흰색).
- 현재 씬 클릭 = 무동작 (이미 그 씬).
- Dashboard 만 SideBar 강조 = "대시보드".

#### 3.19.3 컨텐츠 영역

- TopBar (80px) + SideBar (200px) 제외한 영역에 각 씬 컨텐츠.
- 컨텐츠 시작 좌표: `(200, -80)` (anchor top-left).
- 폭 / 높이: `(Screen.width - 200, Screen.height - 80)`.
- 모든 V1.0 씬 prefab 작업이 이 영역 안에서 이루어짐.

#### 3.19.4 예외 — GlobalNav 가 없는 씬

- **MainMenuScene** — 게임 시작 전 (GameState 없음). 자체 메뉴.
- **ClubSelectScene** — 게임 시작 직후 (GameState 있으나 미초기화). 자체 메뉴.
- **GachaScene** — 가챠 흐름 중. 자체 메뉴.
- **SeasonSummaryScene** — 시즌 자동 진입 시. TopBar 만 (SideBar 없음, [다음 시즌] 버튼만).
- **MatchTextScene** — 매치 시뮬 중. TopBar 만 (가속 / 스킵 버튼 우선).
- **OptionsScene** — 모달 형태. TopBar 의 [뒤로] + [저장] 만.

#### 3.19.5 영향 범위
- `UI/GlobalNavController.cs` 신규 (싱글톤, DontDestroyOnLoad)
- `GlobalNavPrefab` (Assets/Imported/FMLite UI/Prefabs/) 신규
- 11+ 컨텐츠 씬에 GlobalNavPrefab 인스턴스 자동 주입 (Awake)
- 각 씬 컨트롤러의 자체 [뒤로] / [저장] 버튼 제거 (중복 회피)
- `DashboardController.savePanel` 등 통합 → `GlobalSavePanel` (모달, 한 곳)

### 3.20 Options 시스템 (V1.0 신규)

**V0.5 동작:** 전무.

**V1.0 변경:**

#### 3.20.1 OptionsScene 신규

```
┌─────────────────────────────────────────────┐
│  ◀ 뒤로                          💾 저장   │  ← TopBar (특수)
├─────────────────────────────────────────────┤
│                                             │
│   사운드                                    │
│   Master   ████████░░  80%                  │
│   SFX      ██████████ 100%                  │
│   BGM      ██████░░░░  60%                  │
│                                             │
│   언어      ◀  한국어   ▶                    │
│                                             │
│   통화      ◀    £     ▶                    │
│                                             │
│   UI Scale  ◀  100%   ▶                     │
│                                             │
│   자동 저장  ON  / OFF                       │
│                                             │
│   단축키 안내 →                              │
│                                             │
└─────────────────────────────────────────────┘
```

#### 3.20.2 옵션 항목

| 항목 | UI 컴포넌트 | 값 | 저장 |
|---|---|---|---|
| Master 볼륨 | MUIP Slider Standard 0-100 | 0~100 | PlayerPrefs `FMLite.Options.Master` |
| SFX 볼륨 | Slider 0-100 | 0~100 | `FMLite.Options.SFX` |
| BGM 볼륨 | Slider 0-100 | 0~100 | `FMLite.Options.BGM` |
| 언어 | MUIP HorizontalSelector | KO / EN | `FMLite.Options.Language` |
| 통화 | HorizontalSelector | £ / $ / € / ₩ | `FMLite.Options.Currency` |
| UI Scale | HorizontalSelector | 90% / 100% / 110% / 125% | `FMLite.Options.UIScale` |
| 자동 저장 | MUIP Switch | ON / OFF | `FMLite.Options.AutoSave` |
| 단축키 안내 | MUIP Button → ModalWindow | 정적 표 | (저장 X) |

#### 3.20.3 자동 저장 트리거 (§3.13 갱신, Q10 합의)
- ON 시: 매월 1일 / 시즌 종료 (5/15) / 새 시즌 시작 (6/1) 자동.
- 슬롯명: `autosave_<클럽명>_<YYYY-MM>` 또는 `autosave_<클럽명>_<season>_<event>`.
- 순환: 최근 3개만 보관, 나머지 자동 삭제.

#### 3.20.4 UI Scale
- Canvas Scaler.scaleFactor 동적 조정.
- 90% / 100% / 110% / 125% 4단계. 즉시 반영.

#### 3.20.5 단축키 안내 (정적 표)
- 매치 가속: 1/2/3/4 = ×1/×2/×4/스킵
- 저장: Ctrl+S
- 뒤로: Esc
- Continue: Space
- (V1.0 추가 단축키)

#### 3.20.6 영향 범위
- `UI/OptionsController.cs` 신규
- `Application/OptionsManager.cs` 신규 (static, PlayerPrefs 어댑터)
- AudioMixer 도입 (`Assets/_Project/Audio/MasterMixer.mixer`) — §3.21 과 짝
- LocalizationSystem.SetLanguage 연동 (기존 API 활용)
- GlobalNavController 의 [옵션] 버튼 → OptionsScene 진입

### 3.21 사운드 시스템 (V1.0 신규)

**V0.5 동작:** 전무.

**V1.0 변경 — 무료 라이센스 에셋 (사용자 합의):**

#### 3.21.1 AudioMixer 도입
- `MasterMixer.mixer` 신규:
  - Master (Master 볼륨)
    - SFX (SFX 볼륨)
    - BGM (BGM 볼륨)
- Options 슬라이더 → AudioMixer.SetFloat 변환 (`Mathf.Log10 × 20`).

#### 3.21.2 BGM 카탈로그 (3곡)

| 곡 | 사용처 | 길이 | 소스 후보 |
|---|---|---|---|
| BGM_MainMenu | MainMenu / ClubSelect / Gacha | ~2분 loop | Pixabay / freesound CC0 |
| BGM_Dashboard | 모든 컨텐츠 씬 (Dashboard / Squad / 등) | ~3분 loop | 같음 |
| BGM_Match | MatchTextScene | ~2분 loop, 긴장감 | 같음 |

`AudioSource` 싱글톤 (`SoundManager.cs`) DontDestroyOnLoad. 씬 이동 시 crossfade.

#### 3.21.3 SFX 카탈로그 (~12종)

| SFX | 사용처 | 소스 |
|---|---|---|
| sfx_button_click | 모든 버튼 | CC0 click |
| sfx_button_hover | 호버 (선택적) | 같음 |
| sfx_inbox_received | 새 인박스 알림 | 같음 |
| sfx_goal | 매치 골 이벤트 | 환호 |
| sfx_card_yellow | 옐로 카드 | 휘파람 |
| sfx_card_red | 레드 카드 | 휘파람 (긴) |
| sfx_injury | 부상 발생 | 알림 톤 |
| sfx_substitution | 교체 | 알림 톤 |
| sfx_match_kickoff | 매치 시작 | 휘파람 |
| sfx_match_fulltime | 매치 종료 | 휘파람 |
| sfx_save_complete | 저장 완료 | 짧은 ding |
| sfx_season_summary | 시즌 종료 | fanfare |

#### 3.21.4 크레딧
- `Assets/_Project/Audio/CREDITS.md` 신규 — 각 파일의 라이센스 / 출처 / 작가 명시.
- README.md V1.0 갱신 시 사운드 크레딧 섹션 추가.

#### 3.21.5 영향 범위
- `Application/SoundManager.cs` 신규 (Stateless 아님, DontDestroyOnLoad — `GameManager` 패턴 일관)
- `Assets/_Project/Audio/` 신규 폴더 + Music + SFX + CREDITS.md
- 모든 UI 컨트롤러 — 버튼 OnClick 에 `SoundManager.PlaySFX(SfxId.ButtonClick)` 호출
- MatchTextController — 매치 이벤트 SFX 트리거
- OptionsController — AudioMixer 볼륨 조정 wire-up

### 3.22 통화 시스템 (V1.0 신규)

**V0.5 동작:** £ (파운드) 단일. 모든 금액 `£X.XM` 포맷 하드코딩.

**V1.0 변경:**

#### 3.22.1 4 통화 지원, 환율 고정 (사용자 합의)

```csharp
public enum Currency { GBP, USD, EUR, KRW }

// GameBalanceSO 추가 필드
public static readonly Dictionary<Currency, float> ExchangeRates = new() {
    { Currency.GBP, 1.00f },   // base
    { Currency.USD, 1.27f },
    { Currency.EUR, 1.16f },
    { Currency.KRW, 1700f }
};

public static readonly Dictionary<Currency, string> Symbols = new() {
    { Currency.GBP, "£" },
    { Currency.USD, "$" },
    { Currency.EUR, "€" },
    { Currency.KRW, "₩" }
};
```

#### 3.22.2 포맷 헬퍼 — `CurrencyFormatter`

```csharp
public static class CurrencyFormatter {
    public static string Format(int gbpAmount) {
        var cur = OptionsManager.Currency;
        float converted = gbpAmount * GameBalanceSO.ExchangeRates[cur];
        string sym = GameBalanceSO.Symbols[cur];
        // 단위 (M/K) 자동
        if (converted >= 1_000_000) return $"{sym}{converted / 1_000_000:0.0}M";
        if (converted >= 1_000)     return $"{sym}{converted / 1_000:0.0}K";
        return $"{sym}{converted:0}";
    }
}
```

#### 3.22.3 적용 범위
- TopBar 자금 표시
- Transfer 검색 / 오퍼 패널 (시장가 / 입찰가 / 주급)
- Facility 비용 / 자금
- SeasonSummaryScene 재정 결산
- MatchReport 의 결과 영향 X

#### 3.22.4 도메인 영향 0
- GameState 내부는 항상 GBP base 저장. 표시 시점에만 변환.
- SaveMigration 영향 없음.

#### 3.22.5 영향 범위
- `Utils/CurrencyFormatter.cs` 신규
- `GameBalanceSO.exchangeRates` / `currencySymbols` 필드 추가
- 11+ UI 컨트롤러의 `£X.XM` 직박 → `CurrencyFormatter.Format(amount)` 교체

### 3.23 매치 후 결과 대시보드 (V1.0 신규, 사용자 추가 요청)

**V0.5 동작:** MatchReport = 신문기사 + 단순 수치표.

**V1.0 변경 — 시각화 대시보드:**

#### 3.23.1 MatchResultDashboard 탭 구조

```
┌──────────────────────────────────────────────────────┐
│ TopBar                                                │
├──────────────────────────────────────────────────────┤
│ Manchester United  2 - 1  Liverpool                   │
│ [개요] [평점] [통계] [히트맵] [슛맵] [이벤트]          │
├──────────────────────────────────────────────────────┤
│                                                       │
│ (탭별 컨텐츠)                                          │
│                                                       │
└──────────────────────────────────────────────────────┘
```

#### 3.23.2 탭 내용

| 탭 | 내용 |
|---|---|
| 개요 | 스코어 + 득점자 + 어시 + 결정적 이벤트 (카드 / 부상 / 교체) |
| 평점 | 11명 + 교체 출전자 별점 (color-coded) + Man of the Match |
| 통계 | 점유율 / 슛 / 유효슛 / 파울 / 코너 / 패스 성공률 / 태클 (양 팀 막대그래프) |
| 히트맵 | 5-zone Markov 상의 ball 점유 분포 색상 (Home red / Away blue) — V1.0 신규 |
| 슛맵 | 슛 위치 점 (Goal = 큰 점 / 빗나감 = 작은 점 / 선방 = 회색) — V1.0 신규 |
| 이벤트 | 분 단위 Match.events 시계열 — MatchTextScene 의 회고 버전 |

#### 3.23.3 히트맵 / 슛맵 데이터
- `MatchResult.zoneOccupancy: int[5]` 신규 — 각 zone 의 home/away 점유 분 합산.
- `MatchResult.shotMap: List<ShotPin>` 신규 — `{x, y, outcome (Goal/Saved/Off)}`.
- MatchSimulator 가 5-zone 전이 시 누적.

#### 3.23.4 진입 경로
- MatchTextScene 종료 시 [결과 보기] 버튼 → MatchResultDashboard.
- Schedule / Standings 에서 완료된 매치 클릭 → 같은 대시보드.

#### 3.23.5 영향 범위
- `Scenes/MatchResultDashboard.unity` 신규
- `MatchResultController.cs` 신규
- `Domain/MatchResult` 필드 확장 (`zoneOccupancy`, `shotMap`)
- `MatchSimulator` 통계 누적 추가
- SaveMigration: V0.5 → V1.0 시 신규 필드 default empty

### 3.24 훈련 시스템 (V1.0 신규, 사용자 추가 요청)

**V0.5 동작:** GrowthSystem (매월 1일 자동 성장) 만. 유저 개입 없음.

**V1.0 변경 — 개인 + 그룹 훈련:**

#### 3.24.1 그룹 훈련 (Squad-level)
- `Tactic` 화면에 [훈련] 탭 추가.
- 포지션 그룹별 강도: GK / DF / MF / AT 의 훈련 강도 (Low / Medium / High).
- 강도 ↑ → 성장률 ×1.2 / fatigue 누적 ↑ / 부상 위험 ↑.
- 그룹별 stat 집중 (예: AT 그룹 → Finishing/Off the Ball 가중 / DF 그룹 → Tackling/Marking 가중).

#### 3.24.2 개인 훈련 (Player-level)
- PlayerProfile 에 [개인 훈련] 버튼.
- 1명당 1 stat 집중 (예: "Marcus Rashford의 Crossing 집중 훈련 4주").
- 효과: 해당 stat 성장률 ×1.5, 기간 4주.
- 동시 가능 인원 = `Club.facilities.trainingLevel` (1=2명, 5=6명, 10=10명).

#### 3.24.3 GrowthSystem 통합
- 매월 1일 호출 시 그룹/개인 훈련 modifier 적용.
- `algorithms.md` V1.0-4 신규 명세.

#### 3.24.4 영향 범위
- `Application/TrainingSystem.cs` 신규
- `Club.trainingDirective: TrainingDirective` 도메인 신규
- `Player.individualTraining: IndividualTraining?` 도메인 신규
- `GrowthSystem` 통합 (modifier 입력)
- TacticScene [훈련] 탭 (또는 Squad 별도 탭)
- PlayerProfile [개인 훈련] 버튼 + Modal

### 3.25 선수 비교 도구 (V1.0 신규, 사용자 추가 요청)

**V0.5 동작:** Squad 에서 개별 PlayerProfile 진입만.

**V1.0 변경 — 다중 선택 비교 패널:**

#### 3.25.1 Squad 선택 모드
- Squad 화면 우상단 [비교 모드] 토글.
- 토글 ON → 각 선수 row 에 체크박스. 2-3명 선택.
- [비교] 버튼 → SquadComparisonScene 진입.

#### 3.25.2 SquadComparisonScene
```
┌──────────────────────────────────────────┐
│ Marcus Rashford | Bukayo Saka | Cody Gakpo│
│   ST,LW          RW,ST         LW         │
│   25세           23세          25세       │
│   CA 158 PA 175  CA 162 PA 180 CA 155 PA170│
├──────────────────────────────────────────┤
│ Finishing    85 |  78  |  76              │
│ Pace         89 |  85  |  82              │
│ Dribbling    82 |  88  |  79              │
│ Crossing     65 |  78  |  72              │
│ ...                                       │
│ (49 stat 모두 나란히 + 색상 코딩)          │
├──────────────────────────────────────────┤
│ 시즌 통계: 출전 / 골 / 어시 / 평점        │
│ 성장 동향: 최근 3개월 화살표              │
│ 시장가 / 주급 / 계약 잔여                  │
└──────────────────────────────────────────┘
```

#### 3.25.3 영향 범위
- `Scenes/SquadComparisonScene.unity` 신규
- `SquadComparisonController.cs` 신규
- Squad 화면 [비교 모드] 토글 + 다중 선택 UI

### 3.26 선수 / 구단 이미지 (V1.0 — 이니셜만)

**V0.5 동작:** 이미지 부재. 텍스트만.

**V1.0 변경 — 이니셜 + 색상 배지 (사용자 합의, V1.x 정교화):**

#### 3.26.1 PlayerAvatar (50px 원형)
```
┌──┐
│MR│   ← Marcus Rashford 의 이니셜 "MR"
└──┘
   (배경색: 구단 primaryColor / 글자 흰색)
```

- `PersonalInfo.firstName + lastName` 첫 글자 추출.
- 클럽이 없으면 (FA) 회색 배경.
- `UI/PlayerAvatar.cs` 컴포넌트 + prefab.

#### 3.26.2 ClubBadge (50px 원형)
- 구단명 첫 글자 (2자: "MU" / "LI" / "AS").
- `Club.primaryColor` 배경 + `Club.secondaryColor` 글자.
- ClubGenerator 가 자동 색상 생성 (명성 기반 변동).

#### 3.26.3 적용 위치
- Squad / Transfer 검색 결과 / PlayerProfile / MatchReport / Standings / Schedule.
- 50px 통일.

#### 3.26.4 V1.x 확장 (Out of Scope)
- 실제 아바타 이미지 (얼굴 파츠 조합 / 이니셜 아트)
- 구단 로고 (방패 + 모티프)

#### 3.26.5 영향 범위
- `UI/PlayerAvatar.cs` + `UI/ClubBadge.cs` 신규
- `Domain/Club.primaryColor / secondaryColor: Color` 신규 (직렬화 시 hex string)
- ClubGenerator 색상 생성 단계 추가
- SaveMigration: V0.5 → V1.0 시 default 회색

### 3.27 Unity MCP 셋업 (Stage 0 — V1.0 첫 작업, 사용자 명시 우선순위)

**V0.5 동작:** Unity 작업 = docs/unity-ai/*.md 지시서 작성 → 사용자가 Unity AI Assistant 에 복붙.

**V1.0 변경:**

- **Stage 0 — Claude Code ↔ Unity Editor 직접 통신** 셋업.
- 상세: `docs/unity-mcp-setup.md` (전용 문서).
- 옵션 4단계 fallback (A: CoplayDev / B: Unity 공식 / C: IvanMurzak / D: 현 패턴 유지).
- 성공 시: 씬·prefab 작업 일부를 Claude Code 가 직접 수행. 사용자 복붙 단계 일부 생략.
- 실패 시: D fallback. V1.0 작업은 종전 패턴으로 진행 (Stage 0 가 블로킹 X).

**왜 Stage 0?**
- V1.0 의 **거의 모든 작업이 UI 폴리시 / 새 씬 생성 / 통합** — 씬 작업 효율이 전체 V1.0 일정에 결정적.
- 성공 시 V0.5 대비 작업 속도 2~3배 가능 (사용자 추정).
- 실패해도 손실 없음 (롤백 절차 명시).

**완료 조건 (`unity-mcp-setup.md` §8 체크리스트):**
- Smoke Test 통과 (MCPTest GameObject 생성 / Hierarchy 조회)
- 실프로젝트 호환 (DashboardScene Canvas 자식 읽기)
- 의도하지 않은 변경 없음

---

## 4. Stage 분할

> 의존성 순서 기준. Sub-PR 패턴 (Sub-A 명세 → Sub-B 구현 → Sub-C asset) V0.5 그대로.
>
> **V1.0 Stage 구조 (재구성, 2026-05-29)**: Stage 0 (Unity MCP) → Stage A (인프라) → Stage W (UI 표준 GlobalNav) → 본격 영역별 → Stage P (테스트) / Stage Q (빌드).
>
> **Save Migration**: V0.5 세이브 무효 (사용자 합의, Q8 V0.5 패턴 일관). 따라서 신규필드 default 보장만 하면 충분 — 별도 마이그레이션 코드 X.

### Stage 0: Unity MCP 셋업 (선행 — V1.0 첫 작업)
- 0.1 사전조건 검증 (Python / uv / Claude Code / Git)
- 0.2 옵션 A — CoplayDev/unity-mcp 설치 + Configure
- 0.3 Claude Code mcp add + Smoke Test (MCPTest GameObject)
- 0.4 실프로젝트 호환 검증 (DashboardScene Hierarchy)
- 0.5 옵션 B/C/D 결정 (옵션 A 실패 시) + Resolution log
- **블로킹 X** — 실패 시 옵션 D (현 unity-ai/*.md 패턴) 로 진행 가능. 상세 `docs/unity-mcp-setup.md`.

### Stage A: 인프라 / 도메인 확장 (선행)
- A.1 Inbox 도메인 + InboxRouter
- A.2 Player.physical 도메인 + PlayerGenerator 신체 조건
- A.3 Player.growthHistory + GrowthSystem 월별 스냅샷
- A.4 SaveMigration 인프라 (saveVersion = 3, V0.5 세이브 무효, NotSupportedException)
- A.5 Localization 신규 키 (매치 이벤트 확장 ~150 / 컵 / 신체 조건 / Inbox / Options / 사운드 / 통화)
- A.6 OptionsManager 골격 (PlayerPrefs 어댑터)
- A.7 SoundManager 골격 (AudioMixer + DontDestroyOnLoad)
- A.8 CurrencyFormatter 헬퍼 + GameBalanceSO.exchangeRates / currencySymbols

### Stage W: UI 표준 — GlobalNav (UI 일관성 기반)
- W.1 GlobalNavPrefab — TopBar + SideBar 구조 (§3.19)
- W.2 GlobalNavController (싱글톤, DontDestroyOnLoad, 인박스 슬라이드 패널 포함)
- W.3 모든 컨텐츠 씬 (11+) 에 GlobalNavPrefab 자동 주입
- W.4 각 씬 컨트롤러의 자체 [뒤로] / [저장] 버튼 제거 (중복 회피)
- W.5 SideBar 현재 씬 강조 / 클릭 무동작

### Stage X: Options 시스템
- X.1 OptionsScene 신규 + OptionsController
- X.2 사운드 슬라이더 3개 (Master/SFX/BGM) + AudioMixer 연동
- X.3 언어 HorizontalSelector (LocalizationSystem.SetLanguage 연동)
- X.4 통화 HorizontalSelector (CurrencyFormatter 와이어링)
- X.5 UI Scale HorizontalSelector (Canvas Scaler.scaleFactor 조정)
- X.6 자동 저장 Switch + 단축키 안내 Modal
- X.7 매월 1일 + 시즌 시작/종료 자동 저장 훅 (EventScheduler / SeasonEndProcessor 통합)

### Stage Y: 사운드 에셋 + 통합
- Y.1 무료 라이센스 BGM 3곡 + SFX 12종 수급
- Y.2 Assets/_Project/Audio/ 폴더 구조 + CREDITS.md
- Y.3 SoundManager.PlaySFX / PlayBGM 본격 wire-up
- Y.4 모든 UI 버튼 OnClick 에 SFX 호출
- Y.5 MatchSimulator → MatchTextController SFX 트리거

### Stage Z: 통화 통합
- Z.1 11+ UI 컨트롤러의 £X.XM 직박 → CurrencyFormatter.Format 교체
- Z.2 GameBalanceSO 환율 / 심볼 시드
- Z.3 도메인 직렬화 영향 0 검증 (GBP base 유지)

### Stage B: UX 패러다임 — Dashboard 인박스 / 자유도
- B.1 Dashboard 인박스 카테고리 탭 + 정렬 + 읽음 / 만료
- B.2 강제 씬 전환 폐기 (CounterOffer / YouthPromotion / TransferRequest 자동 라우팅 → InboxItem)
- B.3 보드 약속 / 매치 시작 = 기한 있는 항목 → 모달 잠금 유지
- B.4 만료 정책: 자동 만료 (효과 X) + 시즌 종료 시 읽은 항목 삭제 (Q1+Q2 합의)

### Stage C: Player Profile + Stats UI
- C.1 한 화면 4 카테고리 그리드 레이아웃
- C.2 stat 색상 코딩 (5단계 임계치)
- C.3 신체 조건 표시 (height / weight / preferredFoot / weakFootAbility)
- C.4 성장 동향 화살표 / 색

### Stage D: Squad 화면
- D.1 검색 회귀 fix
- D.2 성장 동향 시각화 (Stage C.4 와 짝)

### Stage E: YouthManagementScene 신규
- E.1 씬 + 컨트롤러
- E.2 다음 인스펙션 예고 (시설 기반 풀 사이즈 / PA 예측)
- E.3 1군 콜업 후보 목록
- E.4 Dashboard "유스" 버튼 분기 (날짜 도래 = YouthScene / 평상시 = YouthManagementScene)

### Stage F: Transfer 필터 패널
- F.1 CA 필터 폐기
- F.2 세부 stats / 주급 필터 추가
- F.3 필터 패널 UI 분리 (옵션 → 패널 → 적용 → listing)

### Stage G: Match Simulation 디테일 + 5-Zone 밸런싱
- G.1 이벤트 종류 확장 (패스 / 턴오버 / 스로인 / 프리킥 / 오프사이드 / GK 펀칭)
- G.2 신체 조건 / 멘탈리티 매치 반영 (Stage C.3 와 짝)
- G.3 선수 조합 시너지 (`TacticImpact` 확장, 10+ 카탈로그)
- G.4 전술 상성 (FormationMatchupSO)
- G.5 **5-Zone 골 빈도 재밸런싱 (P0 hotfix)** — avgGoalsPerMatch 2.7±0.3 수렴까지 튜닝 (§3.6.1)
- G.6 매치 텍스트 ~150 키 확장 + 표현 다양화 (§3.9, viewMode 폐기)

### Stage H: Tactic ↔ Lineup 통합 + 포메이션 도식
- H.1 TacticLineupScene 신규 (Tactic + Lineup 통합)
- H.2 포메이션 도식 + 드래그 배정
- H.3 Role / Duty 팝업 위젯 (dropdown 폐기)
- H.4 MatchPreviewScene 신규 (pre-match 점검 + 상대 라인업)
- H.5 시너지 / 매치업 표시 (Stage G.3 / G.4 연동)

### Stage I: Mentoring UI 재작업
- I.1 씬 prefab MUIP 재작업
- I.2 진행률 표시 / Mentor 추천
- I.3 안정화

### Stage J: MatchTextScene 완성 (viewMode 폐기 — §3.9)
- J.1 가속 (×1/×2/×4/스킵 + 단축키)
- J.2 모든 핵심 이벤트 텍스트 표시 (단일 모드, viewMode 폐기)
- J.3 결과 → §3.23 MatchResultDashboard 로 진입 (별도 결과 화면 X)
- J.4 상대 라인업 표시 (Stage H.4 연동)
- J.5 SFX 트리거 wire-up (Stage Y.5 연동)

### Stage AA: 매치 결과 대시보드 (V1.0 신규, §3.23)
- AA.1 MatchResult 도메인 확장 (zoneOccupancy / shotMap)
- AA.2 MatchSimulator 통계 누적 추가
- AA.3 MatchResultDashboard 씬 + 6 탭 (개요/평점/통계/히트맵/슛맵/이벤트)
- AA.4 히트맵 시각화 (5-zone color overlay)
- AA.5 슛맵 시각화 (좌표 점)
- AA.6 Schedule / Standings 에서 완료 매치 → 대시보드 진입 경로

### Stage AB: 훈련 시스템 (V1.0 신규, §3.24)
- AB.1 TrainingSystem 도메인 + Application
- AB.2 그룹 훈련 (포지션별 강도) — TacticScene [훈련] 탭
- AB.3 개인 훈련 (1 stat 4주) — PlayerProfile Modal
- AB.4 GrowthSystem 통합 (modifier 입력)
- AB.5 동시 인원 = trainingLevel 검증

### Stage AC: 선수 비교 도구 (V1.0 신규, §3.25)
- AC.1 SquadComparisonScene 신규 + Controller
- AC.2 Squad 비교 모드 토글 + 다중 선택
- AC.3 49 stat 나란히 + 색상 코딩
- AC.4 시즌 통계 / 성장 동향 / 시장가 비교

### Stage AD: 이미지 시스템 — 이니셜 (V1.0 신규, §3.26)
- AD.1 Club.primaryColor / secondaryColor 도메인 + ClubGenerator 색상 생성
- AD.2 PlayerAvatar / ClubBadge prefab 신규
- AD.3 적용 — Squad / Transfer / PlayerProfile / MatchReport / Standings / Schedule

### Stage K: PromiseInbox 진행률
- K.1 종류별 진행률 위젯
- K.2 카테고리 필터 (Active / Fulfilled / Broken)
- K.3 Dashboard 인박스 (Stage B) 연동

### Stage L: Facility UI + 밸런싱
- L.1 MUIP UI 갈아엎기
- L.2 효과 시각화 (Lv 차이 표시)
- L.3 효과 곡선 재산정 (체감 강화)

### Stage M: Standings + Schedule UI
- M.1 Standings MUIP UI
- M.2 Schedule 탭 분할 (리그 / 컵 / 통합)

### Stage N: Save 사용자 지정 (자동 저장은 Stage X.7 이전)
- N.1 슬롯명 입력 다이얼로그 — GlobalSavePanel (TopBar 저장 버튼)
- N.2 같은 시드 여러 시점 세이브 구분 가능

### Stage O: 컵 대회 (Stage Q 이월)
- O.1 Cup 도메인 + 대진표
- O.2 ScheduleGenerator 컵 라운드 삽입
- O.3 녹아웃 진행 + 우승 (managerReputation 가산)
- O.4 CupBracketScene UI
- O.5 AI 컵 로테이션 (선택)

### Stage P: 통합 테스트
- P.1 한 시즌 V1.0 통합 시나리오
- P.2 SaveMigration V0.5 → V1.0 라운드트립
- P.3 매치 텍스트 결정성
- P.4 사기 / 약속 / 컵 시나리오

### Stage Q: V1.0 빌드 + 보고서
- Q.1 v1.0-build-notes.md
- Q.2 v1.0-build-report.md
- Q.3 README + 스크린샷 + 포트폴리오 준비

---

## 5. Out of Scope (V1.0 명시적 미구현)

> V1.x 또는 영구 제외. V0.5 빌드 노트 §2 와 일관.

### V1.x 후순위
- 다중 리그 / 승강
- 미디어 / 기자회견 / 인터뷰
- Team Instructions (Tempo / Passing / Pressing / Defensive Line / Width — Mentality 7단계 + V1.0 시너지/상성 으로 사실상 대체)
- 코치 / 의료팀 / 스카우트 개별 Staff 도메인 (시설 등급으로 추상화 유지)
- **DOTween UI 애니메이션 (Q12 합의 — V1.x 확정, 2026-05-29)** — 정적 UI 로 V1.0 마감
- 폰트 폴리시 (Pretendard 등 미려한 폰트 검토 — 현 NotoSansKR 유지)
- 다국어 일/중/스 추가 (KO/EN 만 V1.0 유지)
- **선수 / 구단 실 이미지** — V1.0 = 이니셜 + 색상 배지 (§3.26). 실 아바타 / 로고는 V1.x
- **다중 컵 (EFL 컵 등)** — V1.0 = FA컵 단판 단일 (Q9 합의)
- **사용자 수정 가능 환율** — V1.0 = GBP base 고정 환율표 (사용자 합의)

### V1.x (영구 후순위)
- 유럽 대항전 (UCL / UEL / UECL)
- 국가대표 (A매치 위크 / 청소년 대표)
- 매니저 경력 (다른 구단 부임 — V1.0 = 경질 시 Game Over 유지)
- 라이벌 / 더비
- 보드 본격 인터랙션 (예산 요청 / 비전 / 야망 설정)
- 팬 / 광고 보드 / 스폰서십
- 라커룸 파벌 / 그룹

---

## 6. 영향 매트릭스

> Save Migration: V0.5 → V1.0 **무효** (Q8 V0.5 패턴 일관). 신규 게임만 V1.0. 따라서 모든 신규 필드 = default 보장만 하면 충분.

| 영역 | 도메인 변경 | 시드 영향 | 기존 docs 갱신 |
| --- | --- | --- | --- |
| Inbox (§3.1 / §3.18) | `Inbox / InboxItem` 신규 | X | data-flows / event-bus-catalog |
| Player.physical (§3.17) | `Player.physical: PhysicalAttributes` 신규 | PlayerGenerator 시드 확장 | algorithms #1 PlayerGen |
| growthHistory (§3.3) | `Player.growthHistory: List<StatSnapshot>` 신규 | X | algorithms V1.0 신규 |
| 매치 엔진 확장 (§3.6) | `MatchEventType` 항목 추가 + `zoneOccupancy / shotMap` | 매치 시드 행동 변경 → 결정성 유지 | algorithms V0.5-2 → V1.0-2 갱신 / event-bus |
| 시너지 + 상성 (§3.6.4 / §3.6.5) | `FormationMatchupSO` + SynergySO 신규 | TacticImpact 시드 확장 | algorithms V1.0-3 시너지 / 상성 명세 |
| Tactic + Lineup 통합 (§3.7) | `Tactic` 도메인 유지, 씬만 통합 | X | data-flows |
| Cup (§3.14) | `Cup / CupRound / CupTie` 신규 | ScheduleGenerator 컵 라운드 시드 | algorithms Cup 신규 / data-flows / class-diagram |
| 자동 저장 (§3.20.3) | X (SaveSystem + OptionsManager 행동) | X | data-flows |
| **GlobalNav (§3.19)** | X (UI 레이어만) | X | data-flows (씬 진입 시퀀스) |
| **Options (§3.20)** | X (PlayerPrefs) | X | (신규) options-spec 또는 inline |
| **Sound (§3.21)** | X (AudioMixer + SoundManager) | X | (신규) sound-spec 또는 inline |
| **Currency (§3.22)** | `GameBalanceSO.exchangeRates / currencySymbols` 시드 | X | algorithms (Format 헬퍼) |
| **MatchResultDashboard (§3.23)** | `MatchResult.zoneOccupancy / shotMap` 확장 | MatchSimulator 누적 추가 | algorithms V1.0-2 |
| **Training (§3.24)** | `Club.trainingDirective`, `Player.individualTraining` 신규 | X (스케줄 결정성) | algorithms V1.0-4 신규 |
| **선수 비교 (§3.25)** | X (UI only) | X | data-flows |
| **이미지 이니셜 (§3.26)** | `Club.primaryColor / secondaryColor` 신규 | ClubGenerator 시드 확장 | algorithms #5 ClubGen |
| **Unity MCP (§3.27)** | 0 (개발 인프라) | X | unity-mcp-setup.md (Stage 0) |

---

## 7. Open Questions — Resolution (2026-05-29 합의 완료)

> 모두 합의. §3 / §4 / §5 갱신 반영됨.

| Q | 영역 | 결정 | 적용 위치 |
| --- | --- | --- | --- |
| Q1 | 인박스 (§3.1) | **자동 만료 (효과 X)** — 기한 도래 시 자동 거절/수락 X, 사용자가 안 누르면 그냥 소멸. | §3.1 / Stage B.4 |
| Q2 | 인박스 (§3.1) | **시즌 종료 (5/15) 시 읽은 항목 자동 삭제**, 안 읽은 항목만 유지. 누적 최대치 = 무제한 (실제로는 시즌당 ~수십개 한정). | §3.1 / Stage B.4 |
| Q3 | Stats UI (§3.2) | **5단계 80 / 65 / 50 / 35** — Elite 80+ / Good 65-79 / Average 50-64 / Weak 35-49 / Poor -34. Hidden Attributes 동일 적용. | §3.2 / Stage C.2 |
| Q4 | 신체 조건 (§3.17) | **풀 스코프** — 헤더 (키 × 점프 + jumpingReach) + agility / 드리블 (키 작을수록 ↑) + pace + weakFoot (PK / 프리킥 / 윙어 인사이드 컷). v1.0-plan §3.17 그대로. | §3.17 / Stage G.2 |
| Q5 | Tactic (§3.7) | **Swap** — A 슬롯에 B 드롭 시 원래 A 선수가 B 슬롯으로 이동. FM 시리즈 표준. | §3.7 / Stage H.2 |
| Q6 | Tactic (§3.7) | **즉시 대기** — Continue 시 MatchPreviewScene 자동 진입 정지 → 유저 [매치 시작] 클릭해야 진행. 일정 지연 X. | §3.7 / Stage H.4 |
| Q7 | Match (§3.6 / §3.9) | **viewMode 폐기** — 단일 모드로 통일. 모든 핵심 이벤트 (Goal/KeyPass/Save/Card/Injury/Cross/Sub/Penalty/FreeKick/Corner) 텍스트로 노출. 사소한 패스 / 점유 갱신은 통계만, 텍스트 X. | §3.6.6 / §3.9 / Stage J |
| Q8 | 시너지 (§3.6) | **10+ 카탈로그** — 빅앤스몰 / 타겟+발마니 / 온볼이마이웨이 / 골니아 / 서프-스테파 / 윙백 듀얼 / 더블 피보테 / 트레자르테 / 펄스9 / 다이아몬드 미드 / 등. `algorithms.md` V1.0-3 명세. | §3.6.4 / Stage G.3 |
| Q9 | 컵 (§3.14) | **V1.0 = FA컵 단판 단일** — V0.5 원안 그대로. 다중 컵 (EFL) V1.x. 2-leg / 유럽대항전 영구 X. | §3.14 / §5 / Stage O |
| Q10 | 자동 저장 (§3.20.3) | **매월 1일 + 시즌 시작 (6/1) + 시즌 종료 (5/15)** — Options 에서 ON/OFF. 슬롯 = `autosave_<클럽>_<YYYY-MM>`. 최근 3개 순환. | §3.20.3 / Stage X.7 |
| Q11 | 일정 | **마감 없음** (V0.5 패턴 일관). Stage 단위 GitHub Issue + 보드 (#50) 추적. | §4 |
| Q12 | DOTween | **V1.x 미루기** — V1.0 = 정적 UI 로 마감. project-context.md "DOTween (V1.0 onwards)" 표현은 보강 필요 (V1.0 미적용). | §5 |
| Q-MIG | Save Migration | **V0.5 → V1.0 무효** — Q8 V0.5 패턴 일관. saveVersion 3 신규게임만. Migrator V1_0 = NotSupportedException. | Stage A.4 |

### 추가 결정 사항 (Open Questions 외, 2026-05-29 합의)

| 영역 | 결정 |
| --- | --- |
| **글로벌 네비** | TopBar + SideBar 영구 레이어 (§3.19) |
| **Options 범위** | 사운드 (Master/SFX/BGM 3 슬라이더) + 언어 (KO/EN) + 통화 (£/$/€/₩) + UI Scale + 자동 저장 + 단축키 안내. 디버그 모드 토글은 미포함 (isDebugMode 는 디버그 메뉴로). |
| **사운드 소스** | 무료 라이센스 에셋 (CC0/CC-BY). 출처 / 라이센스 명시 (CREDITS.md). BGM 3곡 + SFX 12종. |
| **통화 환율** | £ base 1.00 / $ 1.27 / € 1.16 / ₩ 1700 — GameBalanceSO 고정 상수. 사용자 수정 X. |
| **테마** | 다크 (#1A1A2E / #2A2A3E) 유지 + 강조색 추가 (#4A90D9 기존). 라이트 테마 V1.x. |
| **렌더 모드** | Screen Space — Overlay 유지 (모든 씬). |
| **해상도** | 1920×1080 가로 (포트폴리오 데스크탑 빌드 표준). Reference Resolution 1920×1080 / Match 0.5 / Scale With Screen Size. |
| **이미지 시스템 V1.0** | 이니셜 + 색상 배지만 (50px 원형). 실 아바타 / 로고 V1.x. |
| **V1.0 추가 시스템 (사용자 추가 요청)** | (1) 매치 후 결과 대시보드 — 히트맵 / 슛맵 / 평점 표 / 통계 시각화 (§3.23). (2) 훈련 시스템 — 개인 + 그룹 (§3.24). (3) 선수 비교 도구 — 2-3명 stat 나란히 (§3.25). |
| **Unity MCP** | Stage 0 = V1.0 첫 작업. CoplayDev/unity-mcp 1순위, fallback 4단계 (`unity-mcp-setup.md`). 실패해도 V1.0 진행 가능. |

---

## 8. Change Log

| Date | Change |
| --- | --- |
| 2026-05-29 | V1.0 plan 초안 작성. V0.5 빌드 노트 §2 / 빌드 리포트 §5.2 흡수. 13개 사용자 피드백 항목 → §2 → §3 18 영역으로 흡수. Stage A~Q 17 단계 분할. Open Questions 12 항목 발견. 컵 (V0.5 → V1.0 이월) + 통합 테스트 (이월) 포함. 다중 리그 / 미디어 / Team Instructions 등은 V1.x 유지. |
| 2026-05-29 | **V1.0 plan 보강** — 사용자 추가 결정사항 흡수. (1) §3.19 글로벌 네비 (TopBar+SideBar 영구 레이어) — 사용자 핵심 피드백 "씬 전환되도 기본 버튼 고정 위치" 해결. (2) §3.20 Options 신규 (Master/SFX/BGM/언어/통화/UI Scale/자동 저장/단축키). (3) §3.21 사운드 (AudioMixer + 무료 라이센스 에셋 + CREDITS.md). (4) §3.22 통화 (£/$/€/₩ 고정 환율 + GameBalanceSO 상수). (5) §3.23 매치 결과 대시보드 (히트맵/슛맵/통계 시각화). (6) §3.24 훈련 시스템 (개인 + 그룹). (7) §3.25 선수 비교 도구 (2-3명 stat). (8) §3.26 이니셜 + 색상 배지 (V1.x = 실 아바타). (9) §3.27 Unity MCP — Stage 0 첫 작업 (`docs/unity-mcp-setup.md` 별도). **§3.6 / §3.9 변경**: viewMode 폐기 — 모든 핵심 이벤트 텍스트 노출 + ×1~×4 가속만. 5-Zone 골 빈도 재밸런싱 (P0 — 2.7±0.3 수렴). 매치 텍스트 ~150 키 확장. **§4 Stage 재구성**: Stage 0 (MCP) → A (인프라) → W (GlobalNav) → X (Options) → Y (사운드) → Z (통화) → 기존 B-O → AA (MatchResult) → AB (훈련) → AC (비교) → AD (이미지) → 기존 P/Q. **§5 Out of Scope 갱신**: DOTween V1.x 확정, 폰트 폴리시 V1.x, 다중 컵 V1.x. **§6 영향 매트릭스 확장**: 8개 신규 영역 추가. **§7 Open Questions Resolution**: Q1-Q12 + Q-MIG 모두 결정. 추가 결정 사항 (네비/Options/사운드/통화/테마/렌더/해상도/이미지/MCP) 정리. |
