# V1.0 Tasks

V1.0 구현 작업 단위 분할. 의존성 순서대로 정렬되어 있으며, 위에서부터 차례로 작업한다.

각 작업은 **검증 가능한 완료 조건**을 가진다.

> **선행 문서:**
> - `docs/v1.0-plan.md` (광범위 명세 + Q1~Q12 결정)
> - `docs/design-decisions.md` #58~#65 (V1.0 결정사항)
> - `docs/algorithms.md` Part 3 V1.0-1 ~ V1.0-10 (V1.0 알고리즘 명세)
> - `docs/unity-mcp-setup.md` (Stage 0 상세)
> - `docs/unity-ai/muip-reference.md` (V1.0 보강본)
>
> **일정 (Q11 확정):** 마감 없음 — Stage 단위 GitHub Issue 일괄 생성 후 진행도는 보드 (#50) 추적.
>
> **Sub-PR 패턴 (V0.5 그대로):** Sub-A 명세 갱신 → Sub-B 구현 → Sub-C asset chore.
>
> **Save Migration**: V0.5 → V1.0 무효 (Q-MIG 합의). V1.0 신규 게임만. saveVersion = 3.

## 진행 상태 표기

- `[ ]` 미시작
- `[~]` 진행 중
- `[x]` 완료

---

## Stage 0: Unity MCP 셋업 (V1.0 첫 작업, 블로킹 X)

> 사용자 명시 우선순위. 성공 시 V0.5 대비 작업 속도 2~3배 (사용자 추정). 실패 시 옵션 D (현 unity-ai/*.md 패턴) 유지 — V1.0 진행에 블로킹 X.
> 상세: `docs/unity-mcp-setup.md`.

### Task 0.1: 사전 조건 검증
- [x] PowerShell 새 창에서 `python --version` 3.11+
- [x] `uv --version` 정상 (미설치 시 `irm https://astral.sh/uv/install.ps1 \| iex`)
- [x] `claude --version` 정상
- [x] `git --version` 정상
- **완료 조건:** 4개 모두 정상 출력 ✓ (2026-05-29 검증)

### Task 0.2: 옵션 A — CoplayDev/unity-mcp 설치 시도
- [x] Unity Package Manager → Add package from git URL: `https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#main`
- [x] Console 컴파일 에러 0 확인 (read_console error/warning 0건)
- [x] Window → MCP for Unity → Configure All Detected Clients 클릭
- [x] HTTP transport 연결 — 인스턴스 `minigame-project-gamjaHoo@39286a1e0139c24c` (Unity 6000.3.15f1)
- **완료 조건:** Auto-Setup 성공 + `Connected ✓` 표시 ✓ (2026-05-29)

### Task 0.3: 옵션 A Smoke Test
- [x] Claude Code 재시작 (현재 세션 종료 → 새 터미널)
- [x] ToolSearch + ListMcpResourcesTool → `mcp__UnityMCP__*` 시리즈 + 리소스 19종 노출
- [x] "Unity 에 빈 GameObject 'MCPTest' 생성" → TacticScene Hierarchy 에 즉시 추가 (instanceID -4818)
- [x] Unity Pending Connections — 자동 승인 (별도 조작 불필요)
- [x] "현재 씬 Hierarchy 보여줘" → 정확한 응답 (루트 4개 + Canvas 7 children)
- **완료 조건:** Smoke Test 통과 ✓ (2026-05-29)

### Task 0.4: 실프로젝트 호환 검증
- [x] TacticScene 활성 상태에서 "Canvas 자식 트리 + 각자 RectTransform" 조회 → 정확한 응답 (DashboardScene 갈음 — 본 검증 시 작업 씬 = TacticScene)
- [ ] 시험 Edit (예: TitleText.text 변경) → 본 세션 흐름에 미포함 (Stage W 진입 시 자연스럽게 검증)
- [x] `git status` — 세션 시작 시점과 100% 동일 (MCP 도구가 디스크 변경 0건)
- [x] 시험 GameObject (MCPTest) 제거 → Unity 깨끗
- **완료 조건:** 실프로젝트에 무해 동작 검증 ✓ (2026-05-29, 시험 Edit 만 미실시 — 비블로킹)

### Task 0.5: 옵션 A 실패 시 fallback 결정
- [x] 옵션 A 성공 → B/C/D 시도 불필요
- [x] Resolution log 를 `docs/unity-mcp-setup.md` Change Log 에 기록 (2026-05-29 row)
- [ ] `v1.0-plan.md` Change Log 에 옵션 A 채택 명시 (별도 chore)
- **완료 조건:** 사용 옵션 결정 + 명시 ✓ (**옵션 A 채택**, 2026-05-29)

### Task 0.6: GitHub Issue 생성
- [x] `chore/433-unity-mcp-setup` 브랜치 (#433)
- [x] `area:infra` 라벨 / V1.0 milestone / Type=Task
- [x] PR 본문에 사용 옵션 명시 (옵션 A — CoplayDev/unity-mcp)
- **완료 조건:** PR 머지 + 보드 추가

---

## Stage A: 인프라 / 도메인 확장 (선행)

> Localization / SaveMigration / 신규 도메인 클래스 / OptionsManager / SoundManager / CurrencyFormatter 스켈레톤.

### Task A.1: Inbox 도메인 + InboxRouter (Sub-A 명세 → Sub-B 구현)
- [x] `Domain/InboxItem.cs` 신규 (`algorithms.md` V1.0-7)
- [x] `Domain/InboxCategory` / `InboxPriority` / `InboxAction` enum
- [x] `GameState.inbox: List<InboxItem>` / `nextInboxId: int` 신규
- [x] `Application/InboxRouter.cs` — V0.5 EventBus 이벤트 ~10 흡수
- [x] `SeasonEndProcessor` 시즌 종료 시 inbox 정리 (안 읽은 것만 유지)
- [x] EditMode 테스트 — Inbox 직렬화 라운드트립 + Router wiring + 시즌 종료 정리
- **완료 조건:** V0.5 EventBus 이벤트 발화 시 InboxItem 자동 생성

### Task A.2: Player.physical 도메인 + PlayerGenerator 갱신
- [x] `Domain/PhysicalAttributes.cs` (height/weight/preferredFoot/weakFootAbility)
- [x] `Player.physical` 필드 추가
- [x] PlayerGenerator 신체 조건 추첨 단계 (포지션별 평균 / σ, `algorithms.md` V1.0-8)
- [x] EditMode 테스트 — T1 GK/CB 키 큰 분포 / T2 LW 키 작은 분포 / T3 PreferredFoot 70% Right
- **완료 조건:** 100명 생성 → 포지션별 키 평균 검증

### Task A.3: Player.growthHistory + GrowthSystem 월별 스냅샷
- [x] `Player.growthHistory: List<StatSnapshot>` 신규
- [x] GrowthSystem.Tick 매월 1일 호출 시 49 stat 스냅샷 저장 (V0.5-10 갱신)
- [x] 직전 3개월 stat 변화량 헬퍼
- [x] EditMode 테스트 — 3개월 진행 → growthHistory.Count == 3
- **완료 조건:** 시즌 진행 후 Squad 성장 동향 화살표 표시 가능 (Stage D.2)

### Task A.4: SaveMigration 인프라 (V0.5 → V1.0 무효)
- [x] `GameState.saveVersion = 3` (V1.0)
- [x] `MigratorV0_5_to_V1_0` 골격 — `NotSupportedException` (Q-MIG 합의)
- [x] V0.5 세이브 로드 시 명확한 에러 메시지
- [x] EditMode 테스트 — V0.5 세이브 로드 시 예외 / V1.0 신규 세이브 정상
- **완료 조건:** V0.5 세이브 호환 명시적 차단

### Task A.5: Localization 신규 키 (~150 매치 이벤트 + Options + 인박스 + 등)
- [x] 매치 이벤트 ~150 키 (KO + EN) — `algorithms.md` V1.0-2 카탈로그
- [x] Options 키 (Master / SFX / BGM / 언어 / 통화 / UI Scale / 자동 저장 / 단축키)
- [x] Inbox 카테고리 / 우선순위 / 알림 키
- [x] Currency 심볼 / 단위 키
- [x] 시너지 카탈로그 키 (10종)
- [x] FA Cup 라운드 / 우승 키
- [x] 훈련 시스템 키 (그룹 / 개인 / 강도)
- [x] `FM-Lite/Seed/Generate V1.0 Localization` 메뉴 갱신
- **완료 조건:** 언어 토글 시 모든 V1.0 신규 텍스트 영/한 전환 정상

### Task A.6: OptionsManager 골격
- [x] `Application/OptionsManager.cs` static (PlayerPrefs 어댑터)
- [x] 10 옵션 키 (`FMLite.Options.Master` 등)
- [x] 게임 시작 시 시스템 언어 / 기본값 로드
- [x] EditMode 테스트 — Get/Set 라운드트립 / default 값 / 키 충돌 없음
- **완료 조건:** PlayerPrefs 저장 / 로드 정상

### Task A.7: SoundManager 골격
- [x] `Application/SoundManager.cs` (DontDestroyOnLoad, GameManager 패턴)
- [ ] `Assets/_Project/Audio/MasterMixer.mixer` 신규 (Master / SFX / BGM) — Stage Y.1 로 이관
- [x] PlaySFX(SfxId) / PlayBGM(BgmId) API
- [x] AudioMixer.SetFloat 변환 헬퍼 (Mathf.Log10 × 20)
- [x] Sound 파일 미배치 시 silent fallback
- [x] EditMode 테스트 — Manager 인스턴스화 / API 호출 / 메모리 누수 X
- **완료 조건:** SoundManager API 동작 (실제 사운드 X, 시그니처만)

### Task A.8: CurrencyFormatter + GameBalanceSO 환율 / 심볼
- [x] `Utils/CurrencyFormatter.cs` 신규 (`algorithms.md` V1.0-5)
- [x] `GameBalanceSO.exchangeRates / currencySymbols` 정적 사전 (V1.0: CurrencyFormatter 자체 보유, V1.x GameBalanceSO 외부화)
- [x] M / K / B 자동 단위 (KRW 의 경우)
- [x] EditMode 테스트 T1-T8 (4 통화 × 2 단위)
- **완료 조건:** `CurrencyFormatter.Format(12_500_000)` → 통화별 정확 출력

---

## Stage W: UI 표준 — GlobalNav (V1.0 핵심 — 사용자 우선순위)

> 사용자 핵심 피드백 "씬 전환되도 기본 버튼 고정 위치" 해결. 모든 컨텐츠 씬에 자동 주입.
>
> **생명주기 (2026-06-01 정정):** 명세 모순 (DDOL 싱글톤 ↔ 모든 씬 baked-in) → **씬별 인스턴스** 채택 (DontDestroyOnLoad 아님). `design-decisions.md #58` / `v1.0-plan §3.19.5–6` 참조. 구현은 이 모델 기준.
>
> **Sub-PR:** Sub-A 명세 갱신 (#416) → Sub-B 코드 (GlobalNavController + GlobalSavePanel + 테스트) → Sub-C prefab + 11+ 씬 주입.

### Task W.1: GlobalNavPrefab 디자인
- [x] `Assets/Imported/FMLite UI/Prefabs/GlobalNavPrefab.prefab` 신규 (nested repo `6d2c922`)
- [x] TopBar (80px, top-stretch) — 좌 5 element (뒤로/날짜/자금/토큰/인박스+배지) + 우 3 element (옵션/저장/홈) (§3.19.1)
- [x] SideBar (200px, left-stretch, TopBar 아래) — **10 메인 메뉴 버튼** (§3.19.2 "9" 는 miscount, 명시 리스트 10개 채택)
- [x] InboxPanel (우측 슬라이드, 비활성 시작) + HomeConfirmModal
- [x] Canvas Override Sorting + sortingOrder = 100
- [x] MUIP Basic-Outline 버튼 + NotoSansKR 폰트 (이모지 금지)
- **완료 조건:** prefab 인스턴스 화면에서 정상 표시 ✅ (prefab 스테이지 검증)

### Task W.2: GlobalNavController (씬별 인스턴스)
- [x] `UI/GlobalNavController.cs` 신규 (**씬별 인스턴스 — DDOL 아님**, #58)
- [x] TopBar element 데이터 바인딩 (날짜 / 자금 / 토큰 / 인박스 배지)
- [x] DayAdvancedEvent 구독 → 자동 갱신
- [x] Inbox 카운트 (안 읽은 항목) 배지 표시
- [x] [뒤로] / [옵션] / [저장] / [홈] 버튼 wire-up
- [x] InboxPanel 슬라이드 토글 (V1.0 정적, 즉시 표시)
- **완료 조건:** 모든 씬에서 GlobalNav 정보 정확 표시 ✅

### Task W.3: 11+ 컨텐츠 씬에 GlobalNavPrefab 자동 주입
- [x] DashboardScene / SquadScene / TacticScene / LineupScene / TransferScene / ScheduleScene / StandingsScene / FacilityScene / YouthScene / MentoringScene / PromiseInboxScene 에 prefab 인스턴스 추가 (11씬, 구조 검증)
- [x] 씬별 인스턴스 모델 (Awake 활성, GameManager 재조회)
- [x] 각 씬의 자체 [뒤로] / [저장] 버튼 제거 (중복 회피) — 11씬 구 BackButton/SaveButton/TopBar 비활성화 (#423, 삭제 대신 SetActive false 로 NRE 회피)
- [ ] DashboardController.savePanel → GlobalSavePanel 데드 코드 제거 — **후속** (현재 null-guard 무해, GlobalSavePanel 로 기능 일원화 완료)
- **완료 조건:** 11+ 씬 모두 GlobalNav 주입 ✅ / 구 버튼 정리는 후속

### Task W.4: SideBar 현재 씬 강조
- [x] 현재 씬 메뉴 버튼 = accent (#4A90D9) 강조 (활성 background tint)
- [x] 현재 씬 클릭 = 무동작 (interactable false + NavigateTo guard)
- [x] EditMode 테스트 — `GetMenuIndex` 매핑 (Sub-B 15개 테스트에 포함)
- **완료 조건:** 모든 씬에서 강조 정확 ✅

### Task W.5: GlobalSavePanel (모달)
- [x] 둥근 패널 모달 (슬롯 리스트 + 새 슬롯/닫기) — `GlobalSavePanel.cs` 컴포넌트 + SaveSlotItem prefab 연결
- [ ] 슬롯명 입력 (TMP_InputField) — **Stage N.1 로 이관** (현재 자동 슬롯명)
- [x] 어디서든 TopBar [저장] 클릭 → 표시
- **완료 조건:** 모든 씬에서 저장 가능 ✅ (슬롯명 커스텀은 N.1)

---

## Stage X: Options 시스템

### Task X.1: OptionsScene 신규
- [x] `Scenes/OptionsScene.unity` 신규 (#429)
- [x] TopBar 만 (SideBar 없음 — §3.19.4 예외)
- [x] [뒤로] = 직전 씬 (PlayerPrefs) / [저장] = OptionsManager.Save
- [x] EditorBuildSettings 등록
- **완료 조건:** OptionsScene 진입 / 복귀 정상 ✅ (GlobalNav [옵션]→OptionsScene)

### Task X.2: 사운드 슬라이더 3개 (Master/SFX/BGM)
- [x] MUIP Slider Standard × 3 (min0/max100/wholeNumbers)
- [x] OptionsController — value 변경 시 `SoundManager.SetMixerVolume` (AudioMixer asset 은 Y.1, silent fallback)
- [x] 변환 헬퍼 (`SoundManager.VolumeToDb` = Mathf.Log10 × 20)
- [x] [저장] 시 PlayerPrefs (OptionsManager.Save) — OnDisable 자동저장은 미구현 (저장 버튼으로 명시)
- **완료 조건:** 슬라이더 조정 시 OptionsManager + mixer 반영 ✅ (실제 소리는 Y.1)

### Task X.3: 언어 HorizontalSelector
- [x] MUIP HorizontalSelector (KO / EN)
- [x] LocalizationSystem.SetLanguage 연동
- [ ] 즉시 반영 (OptionsScene 자체 텍스트 즉시 재로컬라이즈는 후속 — 현재 SetLanguage 만)
- **완료 조건:** 토글 시 언어 설정 변경 ✅ / UI 즉시 갱신은 후속

### Task X.4: 통화 HorizontalSelector
- [x] HorizontalSelector (£ / $ / € / ₩)
- [x] OptionsManager.Currency 갱신
- [x] TopBar 자금 즉시 재계산 (`GlobalNavController.Instance?.RefreshMoney`) — Options 엔 GlobalNav 없어 복귀 시 반영
- **완료 조건:** 통화 변경 시 TopBar 표시 변경 ✅

### Task X.5: UI Scale HorizontalSelector
- [x] HorizontalSelector (90% / 100% / 110% / 125%)
- [x] CanvasScaler **referenceResolution 역비례 조정** (ScaleWithScreenSize 모드 — scaleFactor 대신, 명세 보정)
- [x] 즉시 반영 (ApplyUiScale 전 캔버스 순회)
- **완료 조건:** Scale 변경 시 UI 확대/축소 ✅

### Task X.6: 자동 저장 Switch + 단축키 안내 Modal
- [x] MUIP Switch (ON / OFF — OnEvents/OffEvents)
- [x] OptionsManager.AutoSave
- [x] [단축키 안내] 버튼 → Modal (정적 표: 가속 1/2/3/4, 저장 Ctrl+S, 뒤로 Esc, Continue Space)
- **완료 조건:** 자동 저장 토글 / 안내 모달 표시 정상 ✅

### Task X.7: 자동 저장 트리거 통합
- [x] **DailyProcessor** 매월 1일 → AutoSave ON 시 자동 저장 (EventScheduler 아님 — Day==1 훅은 DailyProcessor)
- [x] SeasonEndProcessor 5/15 → 자동 저장 ("season_end")
- [x] NewSeasonProcessor 6/1 → 자동 저장 ("new_season")
- [x] 슬롯명: `autosave_<clubName>_<YYYY-MM>` / `autosave_<clubName>_<seasonYear>_<event>`
- [x] 최근 3개 순환 (`SelectToDelete` — savedAt 내림차순 skip 3)
- [x] EditMode 테스트 7개 (슬롯명 / Sanitize / 삭제 대상 선정) — `AutoSaveSystem.cs` (#431)
- **비고:** `AutoSaveSystem` (Application) — Application→Persistence asmdef 참조 추가 (순환 없음). AutoSave OFF 면 no-op (기존 게임루프 테스트 무영향).
- **완료 조건:** 시즌 진행 시 자동 슬롯 정확

---

## Stage Y: 사운드 에셋 + 통합

### Task Y.1: 무료 라이센스 BGM / SFX 수급
- [ ] BGM 3곡 (MainMenu / Dashboard / Match) — freesound.org / Pixabay / OpenGameArt
- [ ] SFX 12종 (button_click 등) — 같은 소스
- [ ] 각 파일 = CC0 또는 CC-BY 라이센스 확인
- [ ] `Assets/_Project/Audio/Music/*.ogg` + `Assets/_Project/Audio/SFX/*.wav`
- **완료 조건:** 15개 파일 확보 + 라이센스 명확

### Task Y.2: Audio 폴더 구조 + CREDITS.md
- [ ] `Assets/_Project/Audio/CREDITS.md` — 각 파일의 출처 / 작가 / 라이센스 / 변경 사항
- [ ] README.md V1.0 갱신 시 사운드 크레딧 섹션 추가
- **완료 조건:** 모든 파일 출처 추적 가능

### Task Y.3: SoundManager 본격 wire-up
- [ ] PlaySFX(SfxId) / PlayBGM(BgmId) 구현
- [ ] crossfade 로직 (씬 전환 시 BGM 부드러운 전환)
- [ ] OptionsManager.Volume 값과 AudioMixer 연동
- **완료 조건:** SoundManager.PlaySFX(SfxId.Goal) → 즉시 재생

### Task Y.4: 모든 UI 버튼 OnClick 에 SFX 호출
- [ ] 모든 ButtonManager / Button OnClick listener 에 `SoundManager.PlaySFX(SfxId.ButtonClick)` 추가
- [ ] 통합 헬퍼 — `MUIPButtonClickHelper` 또는 GlobalNavController 가 일괄 wire-up
- **완료 조건:** 모든 버튼 클릭 시 click SFX 들림

### Task Y.5: MatchSimulator / MatchTextController SFX 트리거
- [ ] MatchTextController 가 Match.events 재생 중 이벤트별 SFX 호출
- [ ] Goal → SfxId.Goal / Card.Yellow → SfxId.CardYellow / etc
- [ ] BGM_Match 자동 재생 (씬 진입 시)
- **완료 조건:** MatchTextScene 진입 시 SFX 정확 트리거

---

## Stage Z: 통화 통합

### Task Z.1: UI 컨트롤러 화폐 직박 → CurrencyFormatter 교체
- [x] GlobalNavController (TopBar 자금) — Sub-B 완료
- [x] TransferController (오퍼 listing 금액) — 직박 교체 / market_value_fmt(Localization 키)는 follow-up
- [x] SeasonSummaryController (재정 결산)
- [x] NegotiationController / NegotiationOfferItem (오퍼 금액)
- [x] TransferPlayerItem (시장가 — 명단∈ 정확 금액)
- [x] FacilityController / PlayerProfileController / TransferController(market_value) — Localization £ 키 통화중립화 + 호출부 CurrencyFormatter (#437/#438) + LocalizationData.asset 재생성 (#442) ✅
- **완료 조건:** 모든 화폐 표시 통화 변환 ✅

### Task Z.2: GameBalanceSO 환율 / 심볼 시드
- [x] V1.0 미적용 (N/A) — CurrencyFormatter 가 환율/심볼 자체 보유 (design-decisions #61 — V1.x 에 GameBalanceSO 외부화)
- **완료 조건:** V1.0 = CurrencyFormatter 내장 (시드 N/A) ✅

### Task Z.3: 도메인 영향 0 검증
- [x] EditMode 테스트 — GameState 직렬화 라운드트립에서 amount 가 GBP base 유지 (CurrencyIntegrationTests, #435/#436)
- [x] 다른 통화 설정에서도 도메인 값 변함 X 확인 (Currency_IsDisplayOnly_DomainAmountUnchanged)
- **완료 조건:** 직렬화 / 도메인 데이터에 통화 영향 0 ✅

---

## Stage B: UX 패러다임 — Dashboard 인박스 / 자유도

### Task B.1: 인박스 패널 UI (Dashboard + TopBar 슬라이드 통합)
- [x] DashboardController 의 in-memory message 리스트 → GameState.inbox 활용 — `InboxPanelController.Refresh()` 가 `GameManager.State.inbox` 직독 (#447)
- [x] InboxPanel UI = TopBar [인박스] 클릭 시 우측 슬라이드 (어디서든 동일) — `GlobalNavController.ToggleInboxPanel` → `Refresh()` (#447)
- [x] 카테고리 탭 (전체 + Match / Transfer / Morale / Board / Youth / Cup / Award) — 4×2 Grid, 런타임 로컬라이즈 (#447)
- [x] 안 읽음 강조 (읽음 #2A2A3E → 안 읽음 #3A3A4E) + 카테고리 색 스트라이프 + 기한 D-N/만료 — `InboxEntryView` (#447)
- [x] 클릭 → InboxAction (OpenScene / OpenDialog / None) + isRead=true — `InboxPanelController.OnEntryClicked` (OpenDialog 는 B.2/B.3) (#447)
- [x] 우선순위 정렬 (RequiresAction → High → Medium → Low) + 카테고리 필터 + 안읽음 카운트 — `InboxPanelController` 순수 로직 + EditMode 6개 (#439). UI 렌더(슬라이드/탭/강조/클릭)는 후속 MCP
- [x] 타이틀 렌더 — `InboxTextResolver` (ID→이름 state 해석, 사용자 결정) + named-ID 키 이름기반 migrate (#443). ⚠️ LocalizationData.asset 재생성 필요
- **완료 조건:** Dashboard / 다른 씬에서 동일하게 인박스 접근 ✅ (행-기한 키 `inbox_deadline_*` 추가 → LocalizationData.asset 재생성 필요)

### Task B.2: 강제 씬 전환 폐기
- [x] CounterOffer 도착 시 자동 NegotiationScene 라우팅 폐기 → InboxItem 으로 (RequiresAction + 기한 7일) — `DashboardController.OnContinueClicked` 라우트 제거 (InboxRouter 가 흡수) (#451)
- [x] YouthIntakeAvailableEvent 강제 YouthScene 전환 폐기 → InboxItem 만 (Q3) — `_youthIntakePending`/`OnYouthIntakeAvailable`/`Update` 제거 (#451). YouthPromotionSuggestedEvent 도 InboxRouter 가 InboxItem 변환 (Q9 유저 승인 패턴 유지)
- [x] TransferRequest → InboxItem + Dialog 옵션 (현 V0.5 유지) — `OnTransferRequest` 미변경 (#451)
- **완료 조건:** Continue 시 강제 전환 X — 인박스 알림(RequiresAction)만 ✅ (SeasonSummary 5/15 / MatchText 유저 매치 결과 전환은 정상 흐름 유지)

### Task B.3: 기한 있는 항목 모달 잠금 유지
- [x] 보드 약속 (시즌 시작 시 미응답) = 모달 잠금 유지 — `DashboardController.CheckBoardMeeting` (PendingReview → `boardMeetingPanel.Show`) 그대로 유지, B.2 에서 미제거 (#451)
- [x] 매치 시작 = MatchPreviewScene 자동 진입 정지 (Q6) — MatchPreviewScene 미존재(매치 전 프리뷰 자동 진입 없음), 매치는 시뮬 후 결과 MatchText 로만 이동 → 이미 충족 (#451)
- **완료 조건:** 기한 있는 결정(보드 약속)만 강제 처리, 나머지 자유 ✅

### Task B.4: 인박스 정책 (Q1+Q2 합의)
- [x] 기한 도래 시 → 자동 처리 X, 인박스에서 사라지지 않음 (`isRead=false` 라도 비활성 표시) — `InboxEntryView`: 만료(`IsExpired`) 시 타이틀/스트라이프 흐리게 + 클릭 비활성 (#451)
- [x] SeasonEndProcessor 시즌 종료 시 inbox 정리 — 읽은 것만 삭제 (A.1 와 짝) — `SeasonEndProcessor.CleanInbox` 이미 구현 (A.1 Sub-B)
- [x] EditMode 테스트 — 시즌 종료 시 inbox 정확 정리 — `InboxTests.T8` (A.1) + `IsExpired` 4개 (#451)
- **완료 조건:** 인박스 누적 정책 동작 ✅ (Stage B 완료)

---

## Stage C: Player Profile + Stats UI

### Task C.1: 한 화면 4 카테고리 그리드 레이아웃
- [x] PlayerProfileController 전면 재작업 — 4 TMP 블록 → 카테고리 GridLayout + StatRowView 인스턴스화 (#455 Sub-B)
- [x] 4 카테고리 (Tech / Mental / Phys / GK) — **2×2 폐지 → FM식 풀-높이 컬럼** (사용자 결정, Sub-C). GK 는 GK 포지션만 (outfield 3컬럼 36 / GK 4컬럼 49)
- [x] 49 stat 한 화면 (스크롤 없음) — 상단 stat 컬럼 밴드 + 하단 info 스트립, CanvasScaler Expand 로 1920×1080 항상 fit
- **완료 조건:** 1920×1080 에서 한 화면 정상 표시 (FM식 컬럼, 폰트 24)

### Task C.2: Stat 색상 코딩 (Q3 합의)
- [x] 80+ = #2ECC71 (Elite) / 65-79 = #82E08A (Good) / 50-64 = #BBBBBB (Average) / 35-49 = #F39C12 (Weak) / -34 = #E74C3C (Poor) — `StatColorCoding.Classify`/`GradeColor` (#455 Sub-B) + EditMode T1/T2
- [x] Hidden Attributes 동일 적용 — `StatColorCoding` 값 기반 (Hidden 도 동일 Classify)
- [~] Tooltip (호버 시 등급 명) — `StatRowView` 가 `TooltipContent.description` 설정 (코드), 씬 Tooltip prefab 배치는 Sub-C — `algorithms.md` V1.0-12 / muip-reference §11
- **완료 조건:** stat 강약 한눈에 색상으로 구분 (Sub-C 씬 검증)

### Task C.3: 신체 조건 표시
- [x] height / weight / preferredFoot / weakFootAbility (별점 1-5) — `PlayerProfileController.BuildPhysicalCondition` (#455 Sub-B)
- [~] PlayerProfile 헤더에 추가 — 코드 완료, 헤더 TMP 필드 와이어링은 Sub-C
- **완료 조건:** Player.physical 정확 표시 (Sub-C 씬 검증)

### Task C.4: 성장 동향 화살표 / 색
- [x] Player.growthHistory 활용 — 최근 3개월 stat 변화량 (`GrowthSystem.GetStatChange`, A.3)
- [x] +2 이상 = 진녹 ↑ / +1 = 녹 ↗ / 0 = 회 → / -1 = 주황 ↘ / -2 이하 = 빨강 ↓ — `StatColorCoding.Trend*` (#455 Sub-B) + EditMode T4/T5
- [x] 각 stat 옆에 표시 — `StatRowView` arrowText (행마다)
- **완료 조건:** 성장 추세 시각화 (Sub-C 씬 검증)

---

## Stage D: Squad 화면

### Task D.1: 검색 제거 (회귀 fix → 폐지, 사용자 결정 #459)
- [x] 스쿼드 내 검색 불필요 → SquadController 검색 코드 + SquadScene `SquadSearchPanel` 완전 제거
- [x] 프로필 뒤로가기 씬 캐싱 (`PreviousScene` 진입 시 저장 → 스쿼드/전술 등 호출 씬 복귀)
- **완료 조건:** 검색 UI 제거 + 어디서 들어와도 뒤로가기 정상 복귀 ✅

### Task D.2: 성장 동향 시각화 (Stage C.4 와 짝) + CA 재계산 (#459)
- [x] Squad 선수 row 에 CA 변화 화살표/색 (StatColorCoding.Trend) — PlayerListItem CAChange
- [x] CA 변화량 (직전 3개월) — `CaCalculator.GetCaChange`
- [x] **CA 재계산 도입** (앵커 + RelevantMean) — CA 고정 문제 해소, t=0 밸런스 점프 0 (algorithms.md V1.0-13)
- [x] 성장 체감 튜닝: growthBaseChance 0.01→0.06 + 출전 기반 성장 보너스 (FM식)
- **완료 조건:** Squad 에서 한눈에 성장 추세 파악 ✅ (시즌 진행 시 화살표) / 스쿼드 화면 MUIP 폴리싱은 후속(item 2)

---

## Stage E: YouthManagementScene 신규

### Task E.1: 씬 + 컨트롤러
- [x] `Scenes/YouthManagementScene.unity` 신규
- [x] `UI/YouthManagementController.cs`
- [x] 다음 인스펙션 예고 + 현 유스 + 콜업 후보 + Mentoring 요약
- **완료 조건:** 평시 유스 정보 일관 표시

### Task E.2: 다음 인스펙션 예고
- [x] 다음 인스펙션 날짜 (6/15 또는 1/15) + D-N
- [x] 시설 등급 기반 예측 풀 사이즈 / 영입 가능 수 (PA 비공개 → 풀+영입수로 표시)
- **완료 조건:** 예고 정보 정확

### Task E.3: 1군 콜업 후보 목록
- [x] 자동 트리거된 18세+CA70% 선수 표시
- [x] PlayerProfile 진입 가능 (PlayerListItem 행 클릭)
- **완료 조건:** V0.5 콜업 메커닉 활용

### Task E.4: Dashboard "유스" 버튼 분기
- [x] 정해진 날짜 (6/15 / 1/15) = YouthScene (인스펙션)
- [x] 평상시 = YouthManagementScene
- **완료 조건:** 사용자 피드백 2.4 해결

---

## Stage F: Transfer 필터 패널

### Task F.1: CA 필터 폐기 (스카우트 시스템 일관성)
- [x] TransferController 의 CA 필터 UI 제거 — minCA/maxCA 입력 GameObject 삭제 (#472)
- [x] 자기 구단 선수 (Squad) 는 정확 CA 표시 (변경 없음 — TransferPlayerItem 가시성 분기 유지)
- **완료 조건:** Transfer 검색에서 CA 필터 X ✅

### Task F.2: 세부 stats / 주급 필터 추가
- [x] Stats 임계 필터 (Pace ≥ N / Finishing ≥ M 등) — `TransferSearchFilter.statThresholds` (fieldPath→최소값, AND) + `StatCatalog` (Domain, 49 stat 단일 카탈로그) (#472)
- [x] 주급 범위 (min / max) — `TransferSearchFilter.minWage/maxWage`
- [x] Not-Scouted 선수는 정성적 라벨만 표시 (TransferPlayerItem 가시성 분기 유지 — 필터는 실수치 기준)
- [x] EditMode 테스트 — T14 (stat 임계 AND) / T15 (주급 범위)
- **완료 조건:** 새 필터 동작 ✅

### Task F.3: 필터 패널 UI 분리
- [x] Transfer 화면 = 검색창(MUIP Input) + [검색]/[필터] 버튼 + 결과 listing + "이적 시장" 타이틀 (#472)
- [x] [필터] 클릭 → ModalWindow (포지션/나이/국적/트레잇/세부 stats/주급/시장가/계약) — 사용자 결정: ModalWindow
- [x] [적용] → 모달 닫힘 + listing 갱신 / [닫기] = 취소
- **완료 조건:** UI 인라인 → 패널 분리 완료 ✅ (드롭다운=TMP_Dropdown, 입력=MUIP Input Field 다크, 버튼=plain 한글)

### Task F.4: 이적 협상 후속 결함 (#469 에서 이월)
- [x] **#2 활성 오퍼 표시** — 모든 오퍼 listing (`TransferController.RefreshActiveOffers` / Negotiation / PlayerNegotiation) 이 `offer.amount`(제시 금액) 표시 확인. 시장가 표시하는 곳 없음 → T17 로 불변식 고정 (#472)
- [x] **#1 저가 수락 버그** — 분석 결과 **일반 경로(`SubmitOffer`→`AiRespondToOffer`)는 극저가를 정상 Rejected** (ratio≈0 < `aiMockingThreshold` 0.85). FA 경로(`SubmitFreeAgentContract`)는 어디서도 호출 안 됨(unreachable). → 로직 결함 아님, T16 (극저가 500 → Rejected, seed 무관 결정적) 로 동작 고정. 밸런스(#170 기본 오퍼 1.30×)는 **현행 유지** (사용자 결정).
- **완료 조건:** 극저가 오퍼 거절 확인 + 활성 오퍼가 제시 금액 표시 ✅ (Stage F 완료)

---

## Stage G: Match Simulation 디테일 + 5-Zone 밸런싱

> **재설계 (2026-06-04, #474 / design-decisions #70):** G.5 = 블런트 4-파라미터 튜닝 폐기 → **xG 찬스-퀄리티 레이어**. 추가로 **평점 시스템 전면 재설계**(FM 정합, 사용자 요청) 포함. PR 분할: **PR1**(#474) = xG 레이어 + G.5 + G.1 + G.2 + 평점 재설계 + MatchResult shotMap/zoneOccupancy(AA.1/AA.2 선당김). **PR2** = G.3 + G.4. **PR3** = G.6 텍스트. 상세: `algorithms.md` V1.0-1.

### Task G.1: 이벤트 종류 확장
- [ ] 패스 / 턴오버 / 스로인 / 프리킥 / 오프사이드 / GK 펀칭 — MatchEventType 추가
- [ ] MatchSimulator 5-zone resolution 에 통합
- **완료 조건:** 매치 결과에 신규 이벤트 종류 발생

### Task G.2: 신체 조건 / 멘탈리티 매치 반영
- [ ] 헤더 (키 × 점프 + jumpingReach)
- [ ] agility (키 작을수록 ↑)
- [ ] pace + weakFootAbility
- [ ] PK / FK 발 일치 보너스
- [ ] `algorithms.md` V1.0-8 명세 그대로
- **완료 조건:** EditMode 테스트 — 키 큰 선수 헤더골 우세 / 키 작은 선수 드리블 우세

### Task G.3: 선수 조합 시너지 (10+ 카탈로그)
- [x] `Domain/ScriptableObjects/SynergySO.cs` + SynergyCondition (List<Position>/minCount/sameNat/stat-AND 확장) + 10 시드 (`Generate V1.0 Synergy+Matchup` 메뉴 — Sub-C, 사용자 실행)
- [x] `TacticImpact.ComputeSynergies` (`algorithms.md` V1.0-3) — 조건 평가 + stat 파서
- [x] MatchSimulator 통합 — teamMod=Π(시너지)×매치업, **xG 비율 적용**(blanket Eff 폭주 회피) + rng 결정성 수정
- [~] MatchPreviewScene 활성 시너지 표시 — **Stage H.4/H.5 이관** (씬 미존재)
- [x] EditMode 테스트 (#478) — T1~T4 검출(stat/멀티AND/자국/회귀가드) + T6 통합 골·xG 우세
- **완료 조건:** 시너지 자동 검출 + 매치 영향 ✅ (T6: 시너지 1.5 → 홈 골+50%/xG+50%)

### Task G.4: 전술 상성 (FormationMatchupSO)
- [x] `Domain/ScriptableObjects/FormationMatchupSO.cs` + MatchupEntry + Get
- [x] 6×6 = 36 entry 매트릭스 (`algorithms.md` V1.0-9) — 시드 생성기
- [x] MatchSimulator 통합 — teamMod 에 매치업 합산 (xG 비율)
- [x] EditMode 테스트 — T5 Get(정의/미정의/동일) + T7 상성 우세 골·xG 검증
- **완료 조건:** 포메이션별 상성 매치 결과 영향 ✅

### Task G.5: 골 빈도 재밸런싱 — xG 찬스-퀄리티 레이어 (#474)
- [ ] xG 모델 구현 — chanceType별 baseXG, 골=xG×finishMod×gkMod (`algorithms.md` V1.0-1)
- [ ] E[goals]≈ΣxG 로 baseXG/슛빈도 직접 산정 (팀당 ΣxG≈1.35 → 2.7/경기)
- [ ] 측정 하네스 (200~380매치 avg/std/ΣxG) → 사용자 Test Runner 1~2회 미세조정
- [ ] `GameBalanceSO` xG 파라미터 시드 갱신 (Sub-C asset chore)
- [x] EditMode 테스트 갱신 (V0.5 1.0~6.0 → V1.0 T3 2.2~3.3). 측정 T21: 평균골 2.76 / std 1.62 / PK 0.26경기 ✓
- **완료 조건:** avg 2.7±0.3 ✓ (실측 2.76) / std ≈ Poisson(~1.6, 실제 축구 정합 — 초안 "<1.5" 정정)
- **+ 평점 재설계** (사용자 요청, #70): 포지션 가중·패스성공률·DF 무실점공유·xG 보정(clinical/빅찬스미스). `algorithms.md` V1.0-1 평점 subsection.

### Task G.6: 매치 텍스트 ~150 키 변형 + 시드 선택 (#481)
- [x] LocalizationSO 매치 이벤트 변형 키 (KO+EN) — A.5 에서 추가됨 (`match_event_{family}_{n}`, ~146키, 28 패밀리)
- [x] MatchSimulator 시드 기반 변형 선택 (`Vk`=matchSeed+eventSeq, gameplay rng 미소비) + SubstitutionAI sub 변형 + MakeArgs `{player}`/`{gk}` 어휘 정합 (`algorithms.md` V1.0-2)
- [~] viewMode 폐기 — **Stage J.2 이관** (MatchTextScene UI)
- [x] EditMode T28 — 같은 시드 동일 표현 시퀀스 + 변형 키 형식 + 골 변형 다양성(≥2종)
- **완료 조건:** 같은 시드 동일 표현 시퀀스 + 다양성 ✅ (Stage G 완료, viewMode 만 J.2)

---

## Stage H: Tactic ↔ Lineup 통합 + 포메이션 도식

### Task H.1: TacticLineupScene 신규 (통합) (#486)
- [x] `Scenes/TacticLineupScene.unity` 신규 — 3탭 골격 + 전술 탭
- [x] 기존 TacticScene / LineupScene 폐기 — 씬 파일 + 전용 컨트롤러 4종(Tactic/Lineup/SlotRow) 삭제, 빌드세팅·네비 정리 (#495, Stage H 마무리). AutoLineup 은 유지
- [x] 3 탭 (Tactic / Lineup / SetPieces) — ButtonManager 탭 + 패널 토글 (WindowManager 대신 SetTabCurrent CG 리셋, MUIP 하이라이트 잔상 회피)
- **완료 조건:** 통합 씬 진입 / 탭 전환 정상 ✅

### Task H.2: 포메이션 도식 + 드래그 배정 (Q5 = Swap) (#488)
- [x] 피치 도식 — `FormationPitchLayout` displayName 파싱 + 좌우정렬 런타임 좌표(6 포메이션, 에셋 변경 0). EditMode 5/5
- [x] 11 슬롯 = 도식 위 점 (`PitchSlotView`, 포지션 적합 색)
- [x] 우측 패널에 Squad 목록 (전체, 배정자 흐리게, 미배정·가용만 드래그)
- [x] 드래그 & 드롭 슬롯 배정 (Swap — 슬롯↔슬롯 교환 / 점유 시 밀린 선수 벤치 복귀 / 슬롯→벤치 미배정)
- **완료 조건:** 드래그 배정 정상 동작 ✅

### Task H.3: Role / Duty 팝업 위젯 (dropdown 폐기) (#489)
- [x] 슬롯 클릭 → 팝업 (`RoleDutyPopup` — 포지션 호환 Role 버튼 리스트 + Duty 3버튼, 현재 선택 강조)
- [x] V0.5 RoleDropdown 영역 침범 문제 해결 — Canvas 최상위 오버레이(클리핑 없음) + 배경 클릭 닫기
- **완료 조건:** 팝업 정확 표시 / 슬롯 갱신(`포지션 · 공/지/수`) ✅ (Duty 라벨 한글 하드코딩 — 로컬라이즈 후속)

### Task H.4: MatchPreviewScene 신규 (pre-match 점검) (#491)
- [x] `Scenes/MatchPreviewScene.unity` 신규 (빌드세팅 등록) — EventSystem 은 InputSystemChecker(Input System 패키지 정합)
- [x] Continue 시 매치일 도래 → MatchPreviewScene 자동 진입 (Q6) — `BackgroundSimulator.SimulateDay` 가 유저 매치 스킵 + 온디맨드 `SimulateUserMatch`. `DashboardController.TryRouteToMatchPreview`(진입 선행 + 진행 후 체크로 매치일 스킵 방지). EditMode T7/T8
- [x] 라인업(SelectStartingEleven 프리뷰 — 시뮬과 동일 시드) + 전술 + Mentality + 상대 라인업 표시
- [~] 활성 시너지 수 표시 (TacticImpact.ComputeSynergies) — **상세 목록/매치업 보너스/Tooltip 은 H.5**
- [x] [매치 시작] = SimulateUserMatch → MatchTextScene / [라인업 수정] = TacticLineupScene
- [x] 게이트: 출전 가능 11명 미만 시 [매치 시작] 차단 + 경고 (H.6 HasValidLineup 취지)
- [x] **네비 통합** (사용자 요청): GlobalNav SideBar `라인업` 버튼 폐기(10→9) + `전술` → TacticLineupScene. GlobalNavPrefab + DashboardController 핸들러 정합. GlobalNavTests 9개로 갱신
- **완료 조건:** 매치 직전 점검 화면 정상 ✅ (시너지 상세는 H.5)

### Task H.5: 시너지 / 매치업 표시 (#493)
- [x] MatchPreviewScene 활성 시너지 **목록**(이름 — 효과설명) + 포메이션 매치업 보너스(+N%/-N%/대등) — `MatchPreviewController.BuildInfoText` (GameDatabase.FormationMatchup.Get + TacticImpact.ComputeSynergies). 시너지 키는 A.5 시드 활용
- [~] Tooltip → **인라인 효과설명으로 대체** (프리뷰 전용 화면, 정보 밀도 낮아 hover 불필요). MUIP 툴팁은 필요 시 후속
- [x] **duty 로컬라이즈** (H.3 이월): duty_attack/support/defend(+_short) + matchup_label 시드 → RoleDutyPopup(런타임 라벨) + LineupTabController(슬롯 마커) Localization 전환. KO/EN 토글 검증
- [x] RoleDutyPopup 타이틀↔Role 리스트 겹침 fix (RoleScroll top 56→84)
- **완료 조건:** pre-match 정보 풍부 ✅ (역할명 한글화는 PlayerRoleSO.nameKey 필드 신설 필요 — 별도 후속)

### Task H.6: 라인업 선정 로직 재작성 — 포메이션 기반 + AI 노이즈 (#483)
- [x] `LineupSelector` 신규 (Application) — 유저 배정 슬롯 우선 + 빈 슬롯 포메이션 정합 자동 채움. CA-top11 포지션무시 폴백 폐기 (`algorithms.md` V1.0-14 / design-decisions #71)
- [x] **AI 팀**: 포메이션(`FormationSO.slotPositions`) 정합 — 포지션 적합(주>보조>같은라인) + CA + `lineupNoiseSigma` 노이즈. 무포메이션 시 top-CA 폴백(편향 없음)
- [x] **포메이션별 squadConfig 시드** (#483 사용자 결정) — 스쿼드가 formation 라인 분배 따름 (3-5-2=CB↑·풀백0, 4-3-3=윙어·중미↑, 5-3-2=WB·CB5). GenerateFormations + formationStyle 명시 (재생성 보존). **Formation 에셋 재생성 chore 필요**
- [x] `GameBalanceSO.lineupNoiseSigma` + 포지션 적합(주/보조/같은라인) 판정 + `HasValidLineup`(게이트용, H.4 배선)
- [x] EditMode `LineupSelectorTests` (T1 포지션정합>CA / T2 부상·정지 제외 / T3 무포메이션 top-CA / T4 HasValidLineup / T5 결정성). 구 StartingEleven_TopByCA/Suspended 제거(obsolete)
- [~] **유저 팀 라인업 미지정 매치 차단** — `HasValidLineup` 준비됨, 게이트 배선은 **MatchPreviewScene(H.4)** 도착 시 (지금은 AI 자동 폴백)
- **완료 조건:** AI 포메이션 정합 라인업(노이즈) ✅ / squadConfig 정합 ✅ / 유저 강제 게이트는 H.4

---

## Stage I: Mentoring UI 재작업

### Task I.1: MentoringScene prefab MUIP 재작업
- [ ] V0.5 미완 UI 안정화
- [ ] Mentor / Mentee 선택 UI 정비
- **완료 조건:** 씬 진입 / 그룹 생성 / 해체 안정

### Task I.2: 진행률 표시
- [ ] Mentor → Mentee 각 Hidden Attribute 수렴 진행률 (MUIP ProgressBar)
- [ ] 월별 변화량 표시
- **완료 조건:** Mentee Hidden 변화 시각화

### Task I.3: Mentor 추천
- [ ] leadership + age + 계약 잔여 기반 자동 추천 (V0.5 CaptainSystem 패턴)
- [ ] [추천 Mentor 자동 선택] 버튼
- **완료 조건:** 추천 정확

---

## Stage J: MatchTextScene 완성 (viewMode 폐기)

### Task J.1: 가속 (×1/×2/×4/스킵) ✅
- [x] 4 버튼 + 단축키 (1/2/3/4, InputSystem)
- [x] 진행 ProgressBar (분 단위, 추가시간 45+/90+)
- **완료 조건:** 가속 정확 동작 ✅ (#498)

### Task J.2: 모든 핵심 이벤트 텍스트 표시 ✅
- [x] viewMode 폐기 (단일 모드)
- [x] Match.events 시간 순 표시 + 사소한 이벤트 필터 (`MatchEventDisplay.ShouldShowText`)
- **완료 조건:** 핵심 이벤트 모두 텍스트로 노출 ✅ (#498)

### Task J.3: 결과 → §3.23 MatchResultDashboard 진입
- [~] **Stage AA 이관** — V1.0 현재 인라인 결과 유지, MatchResultDashboard 진입은 Stage AA 구현 시
- **완료 조건:** Stage AA 와 연동 (AA 대기)

### Task J.4: 상대 라인업 표시 (Stage H.4 연동) ✅
- [x] pre-match 상대 라인업 패널 MatchTextScene 에서 접근
- **완료 조건:** 라인업 확인 가능 ✅ (#498)

### Task J.5: SFX 트리거 wire-up (Stage Y.5 연동) ✅
- [x] 매치 이벤트별 SFX/BGM wire-up (silent fallback — 실제 사운드 에셋은 Stage Y)
- **완료 조건:** SFX 트리거 배선 ✅ (#498) / 실제 사운드는 Stage Y

---

## Stage K: PromiseInbox 진행률

### Task K.1: 종류별 진행률 위젯
- [ ] PlaytimeAgreement: "출전시간 35% / 목표 50%" ProgressBar
- [ ] TransferIn: "해당 포지션 영입 0/1" + D-day
- [ ] Renewal: D-day
- [ ] TransferOut: D-day
- **완료 조건:** 진행률 시각화 동작

### Task K.2: 카테고리 필터 (Active / Fulfilled / Broken)
- [ ] Toggle Group Panel 로 필터
- **완료 조건:** 필터 동작

### Task K.3: Dashboard 인박스 연동
- [ ] Stage B.1 InboxPanel 에서 Promise 카테고리 클릭 → PromiseInboxScene 진입
- **완료 조건:** 인박스 → 세부 화면 연결

---

## Stage L: Facility UI + 밸런싱

### Task L.1: MUIP UI 갈아엎기
- [ ] FacilityScene UI 재작업
- [ ] 시설별 아이콘 + 등급 시각화 (스타 / 바)
- [ ] 진행 중 업그레이드 ProgressBar
- **완료 조건:** UI 폴리시

### Task L.2: 효과 시각화 (Lv 차이 표시)
- [ ] 다음 등급 효과 비교 (예: "성장률 +X% → +Y%")
- [ ] Tooltip 으로 상세 설명
- **완료 조건:** 등급 차이 한눈에

### Task L.3: 효과 곡선 재산정
- [ ] Training: Lv10 = 평범 선수도 ace 성장 가능
- [ ] Medical: Lv10 = 부상 회복 50% 단축
- [ ] Scout: Lv10 = 사실상 전체 정확 / Lv1 = 자기 리그 일부만
- [ ] `GameBalanceSO` 외부화 곡선 갱신
- **완료 조건:** 시설 효과 체감 향상

---

## Stage M: Standings + Schedule UI

### Task M.1: Standings MUIP UI
- [ ] 행 색상 / 강조 / 변동 화살표
- [ ] 자기 구단 row 강조
- **완료 조건:** 시각 향상

### Task M.2: Schedule 탭 분할 (리그 / 컵 / 통합)
- [ ] MUIP WindowManager 로 3 탭
- [ ] 컵 (Stage O) 도입 후 활성
- **완료 조건:** 탭 동작

---

## Stage N: Save 사용자 지정

### Task N.1: 슬롯명 입력 다이얼로그
- [ ] GlobalSavePanel (W.5) 의 [신규 슬롯] 클릭 → TMP_InputField 다이얼로그
- [ ] 기본값 = 자동 형식 (`slot_<클럽>_<yyMMdd_HHmm>`)
- [ ] 같은 시드 여러 시점 세이브 구분 가능
- **완료 조건:** 슬롯명 사용자 입력 가능

---

## Stage O: 컵 대회 (V0.5 → V1.0 이월)

### Task O.1: Cup 도메인 + 대진표
- [ ] `Domain/Cup.cs` + `CupRound.cs` + `CupTie.cs`
- [ ] `GameState.cups: List<Cup>` 신규
- [ ] 대진표 생성 알고리즘 (`algorithms.md` V1.0-10)
- [ ] 시드 결정성 (`state.randomSeed ^ "FACup".GetHashCode()`)
- **완료 조건:** Cup 직렬화 라운드트립

### Task O.2: ScheduleGenerator 컵 라운드 삽입
- [ ] 매월 마지막 수요일 (외부화 `cupRoundDayOfWeek`)
- [ ] 32강 / 16강 / 8강 / 4강 / 결승 일정
- [ ] `match.allowsExtraTime = true` (I.11 연장 발동)
- **완료 조건:** 컵 매치 정상 진행

### Task O.3: 녹아웃 진행 + 우승
- [ ] DailyProcessor 가 라운드 종료 감지 → 다음 라운드 생성
- [ ] 결승 → 우승 클럽 + managerReputation +10
- [ ] CupRoundCompletedEvent / CupWonEvent 발행
- **완료 조건:** 컵 1회 완주

### Task O.4: CupBracketScene UI
- [ ] `Scenes/CupBracketScene.unity` 신규
- [ ] 대진표 시각화 (라운드별 진행)
- [ ] Schedule 탭 (Stage M.2) 에서 진입
- **완료 조건:** 대진표 표시

### Task O.5: AI 컵 로테이션 (선택)
- [ ] AI 컵 매치 라인업 다양성 (V1.x 정교화 가능)
- **완료 조건:** 컵 매치 라인업 다양성

---

## Stage AA: 매치 결과 대시보드 (V1.0 신규)

### Task AA.1: MatchResult 도메인 확장
- [ ] `MatchResult.zoneOccupancy: int[5]` 신규
- [ ] `MatchResult.shotMap: List<ShotPin>` 신규
- [ ] `ShotPin { x, y, outcome (Goal/Saved/Off) }`
- **완료 조건:** 신규 필드 직렬화 라운드트립

### Task AA.2: MatchSimulator 통계 누적
- [ ] 5-zone 전이 시 zoneOccupancy 누적
- [ ] 슛 발생 시 shotMap 추가
- **완료 조건:** 시즌 진행 후 데이터 확인

### Task AA.3: MatchResultDashboard 씬 + 6 탭
- [ ] `Scenes/MatchResultDashboard.unity` 신규
- [ ] WindowManager 로 6 탭 (개요 / 평점 / 통계 / 히트맵 / 슛맵 / 이벤트)
- **완료 조건:** 탭 전환 정상

### Task AA.4: 히트맵 시각화
- [ ] 5-zone color overlay (Home red / Away blue)
- [ ] zoneOccupancy 비율 → alpha
- **완료 조건:** 점유 분포 시각화

### Task AA.5: 슛맵 시각화
- [ ] 슛 위치 점 (Goal = 큰 / Saved = 회색 / Off = 작은)
- [ ] Home / Away 색상 구분
- **완료 조건:** 슛 위치 시각화

### Task AA.6: 진입 경로
- [ ] MatchTextScene 종료 시 [결과 보기] 버튼
- [ ] Schedule / Standings 에서 완료 매치 클릭
- **완료 조건:** 모든 경로에서 진입 가능

---

## Stage AB: 훈련 시스템

### Task AB.1: TrainingSystem 도메인 + Application
- [ ] `Domain/Club.trainingDirective` (TrainingIntensity gk/df/mf/at)
- [ ] `Domain/Player.individualTraining` (targetStat, start, end)
- [ ] `Application/TrainingSystem.cs` 신규
- [ ] `algorithms.md` V1.0-4 의사코드 구현
- **완료 조건:** TrainingSystem.ComputeModifier 정확

### Task AB.2: 그룹 훈련 — TacticScene [훈련] 탭
- [ ] WindowManager 새 탭 (Tactic / Lineup / SetPieces / Training)
- [ ] Toggle Group Panel × 4 (GK/DF/MF/AT 강도)
- **완료 조건:** 그룹 강도 저장 / GrowthSystem 반영

### Task AB.3: 개인 훈련 — PlayerProfile Modal
- [ ] [개인 훈련] 버튼 → ModalWindow
- [ ] stat 선택 (Dropdown) + 시작일 (자동)
- [ ] 동시 인원 = trainingLevel 검증 (초과 시 차단)
- **완료 조건:** 개인 훈련 발주 정상

### Task AB.4: GrowthSystem 통합
- [ ] 매월 1일 Tick 시 TrainingSystem.GetGroupModifier / individualTraining 적용
- [ ] `algorithms.md` V1.0-4 흐름 그대로
- **완료 조건:** 훈련에 따른 성장률 변화 검증

### Task AB.5: 동시 인원 = trainingLevel 검증
- [ ] EditMode 테스트 — Lv1 클럽 = 2명 / Lv5 = 6명 / Lv10 = 10명 제한
- **완료 조건:** 인원 초과 시 차단

---

## Stage AC: 선수 비교 도구

### Task AC.1: SquadComparisonScene 신규
- [ ] `Scenes/SquadComparisonScene.unity` 신규
- [ ] `SquadComparisonController.cs`
- **완료 조건:** 씬 진입 가능

### Task AC.2: Squad 비교 모드 토글
- [ ] SquadController 에 [비교 모드] Switch
- [ ] 토글 ON → 각 선수 row 에 체크박스 (2-3명)
- [ ] [비교] 버튼 → SquadComparisonScene 진입 (PlayerPrefs 로 ID 전달)
- **완료 조건:** 비교 모드 정상

### Task AC.3: 49 stat 나란히 + 색상 코딩
- [ ] 2-3 열 × 49 행 그리드
- [ ] Stage C.2 색상 코딩 활용
- [ ] 최고 값 강조 (테두리)
- **완료 조건:** 직관적 비교

### Task AC.4: 시즌 통계 / 성장 동향 / 시장가
- [ ] 출전 / 골 / 어시 / 평점 비교
- [ ] 성장 동향 화살표 (Stage C.4)
- [ ] 시장가 / 주급 / 계약 잔여
- **완료 조건:** 종합 비교

---

## Stage AD: 이미지 시스템 (이니셜)

### Task AD.1: Club.primaryColor / secondaryColor + ClubGenerator
- [ ] `Domain/Club.primaryColor / secondaryColor: string` 신규 (hex)
- [ ] ClubGenerator 색상 생성 단계 추가 (`algorithms.md` V1.0-6.3)
- [ ] 명성 기반 색조 다양화
- **완료 조건:** 20 구단 색상 다양성 검증

### Task AD.2: PlayerAvatar / ClubBadge prefab
- [ ] `UI/PlayerAvatar.cs` + prefab
- [ ] `UI/ClubBadge.cs` + prefab
- [ ] 이니셜 + 색상 배지 (50px circle)
- **완료 조건:** 모든 선수 / 구단에서 정상 표시

### Task AD.3: 적용 — Squad / Transfer / PlayerProfile / MatchReport / Standings / Schedule
- [ ] 각 화면의 선수 row / 구단 표시에 PlayerAvatar / ClubBadge 통합
- **완료 조건:** 일관된 이미지 노출

---

## Stage P: 통합 테스트

### Task P.1: 한 시즌 V1.0 통합 시나리오
- [ ] V1.0 시스템 통합 (매치 + 사기 + Promise + 시상 + 컵 + 훈련 + 시너지)
- [ ] 한 시즌 완주 + 5/15 시즌 종료 + 6/1 신규 시즌
- **완료 조건:** 한 시즌 V1.0 전체 통합 동작

### Task P.2: SaveMigration V0.5 무효 검증
- [ ] V0.5 세이브 로드 시도 → NotSupportedException
- [ ] V1.0 신규 세이브 라운드트립
- **완료 조건:** 명확한 차단 + V1.0 정상

### Task P.3: 매치 텍스트 결정성
- [ ] 같은 시드 매치 → 같은 텍스트 시퀀스
- [ ] 5-Zone 밸런싱 후 결정성 유지 확인
- **완료 조건:** 결정성 검증

### Task P.4: 사기 / 약속 / 컵 시나리오
- [ ] PlaytimeAgreement 약속 → 출전 미달 → Happiness → TransferRequest → InboxItem
- [ ] FA 컵 1회 완주
- [ ] 훈련 시스템 효과
- **완료 조건:** EditMode T 통과

### Task P.5: Unity MCP 통합 (Stage 0 결과에 따라)
- [ ] 옵션 A/B/C 성공 시 — V1.0 작업 중 MCP 도구 활용 사례 기록
- [ ] 옵션 D — 기존 unity-ai 패턴 유지 (변화 없음)
- **완료 조건:** Stage 0 결정 일관

---

## Stage Q: V1.0 빌드 + 보고서

### Task Q.1: v1.0-build-notes.md
- [ ] 빌드 노트 작성 (V0.5 패턴 그대로)
- [ ] V1.0 구현된 기능 / 임시 처리 / 플레이 정보
- **완료 조건:** 빌드 노트 작성

### Task Q.2: v1.0-build-report.md
- [ ] 빌드 환경 / 산출물 / 테스트 시나리오 / 발견 이슈
- [ ] 사용자 플레이테스트 피드백 흡수
- **완료 조건:** 빌드 보고서 작성

### Task Q.3: README + 스크린샷 + 포트폴리오 준비
- [ ] README.md V1.0 버전 갱신
- [ ] 스크린샷 (GlobalNav / OptionsScene / MatchResultDashboard / 등)
- [ ] 사운드 크레딧 섹션
- [ ] 포트폴리오 비디오 (옵션)
- **완료 조건:** 포트폴리오 준비 완료

---

## Stage R: 플레이테스트 핫픽스 (V1.0 — 사용자 우선순위 최상)

> 2026-06-05 플레이테스트 피드백 18항목 트리아지 결과. **대부분 기존 미완 Stage(Y/I/AA/AB/AC/AD/K/L/M/N/O)보다 우선** (플레이 직결, 사용자 명시). 근거: `design-decisions.md #72~#77` / `algorithms.md` V1.0-11.5·15·16·17·18 / `event-bus-catalog.md #76`. Sub-PR 패턴(명세 본 PR 완료 → 구현 → 에셋 chore) 유지.

### Task R.1: 슈퍼유망주 동적 라벨 (#72)
- [ ] `SeedV10Data` trait id=14 effects 비움 (에셋 chore)
- [ ] `PlayerGenerator.SelectTraits` 랜덤 풀에서 id=14 제외
- [ ] `GrowthSystem.Tick` 슈퍼유망주 재평가(부여/회수, V1.0-11.5)
- [ ] `GameBalanceSO.superProspectMinCA`(110) / `superProspectMaxAge`(21)
- [ ] EditMode — 21세&CA≥110 부여 / 22세 회수 / CA<110 회수 / 노장 미부여
- **완료 조건:** 노장에 슈퍼유망주 X, 어린 고CA에 부여, 만 22세 회수, 효과 없음

### Task R.2: Trait 가시성 3-tier + 검색 폐지 (#73)
- [ ] `TraitSO.visibility` enum + 20 trait 분류 (에셋 chore)
- [ ] `ScoutingSystem` `revealedTraitIds` 채우기 (scoutLevel 비례, 자기팀=100)
- [ ] `PlayerProfileController.BuildTraitsText` 게이팅 (Concealed 제외, 타팀=revealedTraitIds)
- [ ] `TransferController` trait 드롭다운 + `TransferSystem` `filter.traitId` 제거
- [ ] Localization "추가 정찰 필요" 키
- [ ] EditMode — Concealed 미노출 / 자기팀 전체 / 미정찰 0 / scoutLevel 비례
- **완료 조건:** 늦깎이/조숙형 안 보임, 검색에 trait 없음, 스카우팅별 노출 차등

### Task R.3: 재정 밸런싱 — 주급 차감 + 수입/시설 재스케일 (#74)
- [ ] `WageSystem`(또는 FinanceSystem 월처리) — Day==1 주급 월 차감 (전 구단)
- [ ] `FinanceSystem` 수입 재스케일 + 분산 (TV↑, matchday 홈경기/월 분산)
- [ ] `FacilityLevelSO` 비용 곡선 상향 (에셋 chore)
- [ ] `GameBalanceSO` 재정 필드 재산정 (에셋 chore)
- [ ] 측정 하네스 (시즌 완주 후 구단별 순현금흐름) → 사용자 Test Runner 1~2회 튜닝
- **완료 조건:** 중위 구단 시즌당 본전±(성적 따라 흑/적자), 돈으로 전원 영입 불가, 주급으로 자금 감소 확인

### Task R.4: 명성 기반 선수 수락 (#75)
- [ ] `TransferSystem.ComputePlayerAcceptChance` 명성격차 항 추가
- [ ] `GameBalanceSO.repGapWeight` / `repGapMaxPenalty` / CA→rep 매핑
- [ ] `EstimatePlayerAcceptChance` 미리보기 정합
- [ ] EditMode — 약체 구단 저임금 거절 / 고임금 보상 시 수락 / 명성 충분 시 영향 0
- **완료 조건:** 명성 낮은 구단 오퍼 = 거절 또는 큰 요구, 임금 보상 시 영입 가능

### Task R.5: 인박스 확장 (#76)
- [ ] `InboxCategory.League` enum 끝 append
- [ ] Tier1 라우팅 — PlayerInjured(Match) / PlayerInjuryRecovered(Match) / PlayerStatChanged(Youth, 유스 한정) / 시상(Award)
- [ ] Tier2 신규 이벤트 4종 정의+발화+라우팅 — PlayerUnhappy / PlayerFatigued / StandingsChanged / ContractExpiring
- [ ] Localization 신규 알림 키
- [ ] EditMode — InboxRouter 신규 이벤트 → InboxItem 생성 + 카테고리/우선순위
- **완료 조건:** 부상/불만/피로/순위변동/부상복귀/계약만료/유스성장/시상 알림이 인박스에 표시

### Task R.6: 글로벌 선수명 링크 + 프로필 가시성 게이팅 (#77-1)
- [ ] `PlayerNameLinkController` 일괄 부착 (Transfer/MatchText/MatchPreview/Standings/SeasonSummary/Negotiation 등) — 드롭다운/필터 내 이름 **제외**
- [ ] `PlayerProfileController` 자기팀/타팀 분기 + 가시성 게이팅(R.2) + PreviousScene 복귀
- **완료 조건:** 모든 씬에서 아무 선수 이름 클릭 → 프로필 진입 (타팀 = 스카우팅 게이팅)

### Task R.7: 프로필 액션 — 재계약 / 콜업 (#77-2)
- [ ] 자기팀 선수 [재계약] 버튼 → `RenewContract` 모달 (로직 기구현)
- [ ] [콜업] 버튼 = **유스 선수 한정** 노출 → `YouthSystem.PromotePlayer`
- **완료 조건:** 정보창에서 재계약 가능 / 콜업 버튼은 유스에게만

### Task R.8: 전역 단축키 (#77-3)
- [ ] `GlobalNavController` — Esc=뒤로 / Ctrl+S=저장 (컨텐츠 씬), Space=Continue (Dashboard 한정)
- [ ] 매치씬 1/2/3/4 가속 유지
- **완료 조건:** 단축키 동작 + Options 단축키 안내 표와 일치

### Task R.9: 보드 약속 이행 보상 (#77-4)
- [ ] `BoardSystem` 이행 경로 — `boardConfidence` + `managerReputation` 가산
- [ ] `GameBalanceSO.boardPromiseFulfillReward`
- [ ] EditMode — 이행 시 confidence/reputation ↑
- **완료 조건:** 이사회 요구 성공 시 신임도 상승

### Task R.10: WB/AM squad 생성 (#77-5)
- [ ] `FormationConfig` WB/AM 1급 슬롯 편입 (구조 보존)
- [ ] `SeedV10Data.GenerateFormations` + Formation 에셋 재생성 (chore)
- [ ] `ClubGenerator.BuildSquadComposition` 반영
- [ ] EditMode — WB/AM 쓰는 포메이션 스쿼드에 해당 포지션 생성
- **완료 조건:** WB/AM 포지션 선수 정상 생성

### Task R.11: 리그 개인 리더보드 (#77-6)
- [ ] `SeasonAwardSystem.BuildLeagueStats` 시즌중 조회 public 쿼리 일반화(`LeaderboardSystem`)
- [ ] 득점/도움/평점/GK 클린시트/출전 + 최소 출전수 필터
- [ ] StandingsScene 탭/섹션 (자국 선수 강조)
- **완료 조건:** 시즌 중 득점왕/도움왕/평점/클린시트/출전 순위 표시

### Task R.12: 협상 씬 버그 + 스크롤 (항목 8) ✅
- [x] **근본원인** = Viewport `Mask` 그래픽 Image alpha=0 → `cullTransparentMesh` 컬링으로 스텐실 미기록 → 자식(오퍼 행) 클립(안 보임), `Mask`는 레이캐스트 비클립이라 클릭만 통과. → alpha 0→1(showMaskGraphic=false라 비표시, 스텐실만) 수정 (두 씬)
- [x] 동적 생성 직후 `LayoutRebuilder.ForceRebuildLayoutImmediate` (클릭해야 뜨던 복불복 해소) + `_state` null 시에도 EmptyLabel 표시
- [x] NegotiationOfferItem 프리팹 LayoutElement(높이80)+top-stretch 앵커 (별도 에셋 repo)
- **완료 조건:** 씬 진입 즉시 오퍼 행 렌더링 ✅ (#505, 사용자 Play 확인) / 전반 UI 폴리싱(다크/MUIP)은 후속

### Task R.13: 부상 복귀 텍스트 조건부 (항목 11)
- [ ] 프로필은 이미 조건부 → 다른 화면/문자열 점검 (비부상 시 복귀 문구 노출 지점 탐색·수정)
- **완료 조건:** 부상 상태에서만 복귀예정 표시

---

## Stage S: 씬 폴리싱 (V1.0, MUIP)

> 항목 10·12. **§0 공통 토큰**: 다크 `#1A1A2E`/`#2A2A3E` + accent `#4A90D9`, NotoSansKR(이모지 금지), 1920×1080 ScaleWithScreenSize. **MUIP 함정 회피**(ButtonManager 텍스트 리셋→plain+한글 / CustomDropdown→TMP_Dropdown / UIManagerInputField 제거 / 새 EventSystem InputSystemChecker). 시각 세부는 구현 시 scene_view 검증. **의존성**: ClubSelect/SeasonSummary 배지·아바타는 Stage AD 선행 자연(텍스트 먼저), S.5 클럽 베스트는 R.11 의존.

### Task S.1: DashboardScene 폴리싱 ✅
- [x] 중복 제거 — 카드 내 date/token 숨김(GlobalNav TopBar 일원화)
- [x] InfoPanel 중앙 카드(폭1040, ContentSizeFitter+패딩40/32) + 다음경기 헤더(굵게)/상세 위계 + Continue accent 둥근버튼 + 텍스트 가독성(부상 빨강/사기 주황) + 빈 매치상세·스쿼드경보 `SetActive` 접기(DashboardController) + `dashboard_no_record`/`_injury_fmt`/`_morale_warning_fmt` 로컬라이즈 키 추가
- **완료 조건:** GlobalNav 비가림, 컨텐츠 영역 MUIP 정돈 ✅ (#503, 사용자 Play 확인) / 다이얼로그 ModalWindow 통일은 후속

### Task S.2: MainMenuScene 폴리싱 ✅
- [x] 다크 배경(#1A1A2E) + 타이틀/서브타이틀(NotoSansKR) + New/Load MUIP Rounded Filled 버튼(New=accent #4A90D9 / Load=dark) 중앙 스택
- [ ] (후속) Options/Quit 버튼 = `MainMenuController` 핸들러 추가 필요
- **완료 조건:** 메인 메뉴 MUIP 폴리싱 ✅ (#501, 사용자 Play 확인)

### Task S.3: ClubSelectScene 폴리싱
- [ ] 구단 리스트 카드 + 명성 티어 시각화 + 선택 강조 + confirm 패널 MUIP
- [ ] (선택) 재정등급/시설 미리보기. 배지는 Stage AD 후속
- **완료 조건:** 구단 선택 MUIP 폴리싱

### Task S.4: GachaScene 폴리싱
- [ ] 4라인 티어 카드 + 별점/색상(C.2 색표) + ACE 강조 + 리롤 토큰 위젯 + 확정 accent
- [ ] (정보 보강) 라인별 대표/ACE 선수명 노출(티어는 #14 유지)
- **완료 조건:** 스탯 리롤 MUIP 폴리싱

### Task S.5: SeasonSummaryScene 폴리싱 + 정보 보강 (항목 12)
- [ ] 4섹션(순위/수상/재정/보드) 카드화 + 다크/accent
- [ ] 정보 추가 — 시즌 성적(W-D-L/승점/득실), 최종 순위 미니표, 클럽 베스트(득점/도움/평점, R.11 연계), 약속 이행/결렬 요약, 성장 유망주, 다음 시즌 목표
- **완료 조건:** 폴리싱 + 정보부족 해소

### Task S.6: UIManagerInputField NRE 핫픽스 (항목 3)
- [ ] Transfer 필터 패널 등 입력칸의 `UIManagerInputField` 제거 (또는 texts 할당)
- **완료 조건:** 필터 패널 열 때 NRE 없음 (※ R 단계와 무관히 즉시 핫픽스 가능)

---

## Stage 완료 후

V1.0 완성 시 상태:
- ✅ Unity MCP 셋업 (Stage 0 — 옵션 A/B/C 성공 시 / 또는 D fallback)
- ✅ 글로벌 네비 — TopBar + SideBar 영구 레이어 (모든 씬 일관 UI)
- ✅ Options 시스템 (사운드 + 언어 + 통화 + UI Scale + 자동 저장)
- ✅ 사운드 시스템 (BGM 3 + SFX 12, 무료 라이센스, CREDITS.md)
- ✅ 통화 시스템 (4 통화, GBP base 고정 환율)
- ✅ 인박스 자유도 (FM 식 피드, 강제 씬 전환 폐기)
- ✅ Stat 색상 코딩 (80/65/50/35) + 한 화면 레이아웃
- ✅ 신체 조건 + 매치 영향 (헤더/agility/pace/PK)
- ✅ Tactic ↔ Lineup 통합 + 포메이션 도식 드래그
- ✅ 매치 결과 대시보드 (히트맵 / 슛맵 / 통계)
- ✅ 매치 텍스트 ~150 키 + 가속 ×1~×4
- ✅ 5-Zone 골 빈도 밸런싱 (2.7±0.3)
- ✅ 선수 조합 시너지 10+ + 전술 상성
- ✅ 훈련 시스템 (개인 + 그룹)
- ✅ 선수 비교 도구
- ✅ 이미지 (이니셜 + 색상 배지)
- ✅ FA Cup 단판 단일
- ✅ Stat 49 + Hidden / Trait / Promise / Scout / AI 영입 / Tactic / Mentoring / 시상 / 보드 / 재정 (V0.5 그대로)
- ❌ 다중 리그 / 승강 (V1.x)
- ❌ 미디어 / 기자회견 (V1.x)
- ❌ Team Instructions (V1.x — Mentality 7단계 + 시너지/상성 으로 사실상 대체)
- ❌ Staff (코치 / 의료 / 스카우트 인사) (V1.x)
- ❌ DOTween UI 애니메이션 (V1.x)
- ❌ 폰트 폴리시 / 라이트 테마 (V1.x)
- ❌ 실 선수/구단 이미지 (V1.x — 이니셜은 도입)
- ❌ 다중 컵 EFL (V1.x)
- ❌ 다국어 일/중/스 (V1.x)

---

## 작업 시 주의사항

### 의존성 순서 지키기
Stage 0 (MCP — 선행 권장 단, 블로킹 X) → Stage A (인프라) → Stage W (UI 표준) → 영역별 (X/Y/Z/B-O/AA-AD) → Stage P (테스트) / Stage Q (빌드).

### 각 Task 완료 조건 검증
EditMode / PlayMode 테스트 통과 + Unity 에디터 동작 확인. 통과 못 한 채 다음으로 넘어가지 않기.

### 명세 참조 의무
- 각 Stage 의 명세는 `docs/v1.0-plan.md` §3 + `docs/algorithms.md` Part 3 V1.0-N + `docs/design-decisions.md` #58~#65.
- 작업 중 명세 모순 발견 → 즉시 사용자에게 보고 후 결정 → 명세 갱신.

### Sub-PR 패턴
V0.5 패턴 (Sub-A 명세 → Sub-B 구현 → Sub-C asset chore) 유지. 큰 Task 는 Sub-PR 분할.

### Unity MCP 의존도
- Stage 0 옵션 A/B/C 성공 시 — Claude Code 가 씬·prefab 작업 직접 수행 가능. docs/unity-ai/*.md 작성 비도 감소.
- Stage 0 옵션 D 시 — V0.5 패턴 유지 (docs/unity-ai/*.md 작성 → 사용자 Unity AI Assistant 복붙).
- 두 경우 모두 V1.0 진행 가능.

### 발견 사항 기록
- 새 결정 필요 → `design-decisions.md` #66+ 추가
- 명세 미비 → `algorithms.md` Part 3 갱신 + `QUESTION:` 마커
- TBD 해결 → 해당 문서 갱신

---

## Change Log

| Date | Change |
| --- | --- |
| 2026-05-29 | V1.0 작업 분할 초안 작성. `docs/v1.0-plan.md` §4 Stage 구조 (0 / A / W / X / Y / Z / B-O / AA-AD / P / Q) 기반. Sub-PR 패턴 V0.5 그대로. Q1~Q12 + Q-MIG 결정 반영. Stage 0 (Unity MCP) 우선 시도 / 실패 시 옵션 D fallback. Stage A (인프라) 완료 후 W (UI 표준) 진입 정책. 일정 마감 없음 (Q11). |
| 2026-05-29 | **Stage 0 완료 — 옵션 A (CoplayDev) 채택**. Task 0.1 ~ 0.4 검증 통과 (0.4 시험 Edit 1건만 미실시, 비블로킹). UnityMCP HTTP transport 정상 연결 + Smoke Test (MCPTest 생성/조회/삭제) + git status 변화 없음 검증. 상세는 `docs/unity-mcp-setup.md` Change Log + §5.12–5.14 신규 함정 3건. Task 0.6 (GitHub Issue / PR) 별도 진행. **Stage A 진입 가능**. |
| 2026-06-01 | **Stage W 후속 — 씬 정리 + 컨텐츠 재배치**. (#423/#424) 11씬 구 BackButton/SaveButton/TopBar 비활성화 (NRE 회피 위해 삭제 X). (#425) 각 씬 활성 컨텐츠를 `ContentRoot` (inset 200,-80) 로 이동 — GlobalNav TopBar/SideBar 비가림. Background 전체 유지, dialog/팝업(TransferRequestDialog/BoardMeetingPanel/OfferPanel 등)은 Canvas 직계 유지(오버레이). "적당히" — 픽셀 정밀화는 씬 전면 재작업(B~D) 시. |
| 2026-06-01 | **Stage W Sub-B/C — GlobalNav 구현 + 주입**. Sub-B (#420 머지): `GlobalNavController` + `GlobalSavePanel` + W.4 강조 로직 + EditMode 15개. Sub-C1 (#421): GlobalNavPrefab (MUIP TopBar 8요소 + SideBar 10버튼 + InboxPanel + HomeConfirmModal + GlobalSavePanel, NotoSansKR) → nested Imported repo 커밋 (`6d2c922`). Sub-C2: 11개 컨텐츠 씬 GlobalNav 주입 + DashboardScene 구 SideMenu/SavePanel/InboxPanel 제거. **함정**: MCP `manage_gameobject` 로 스케일된 캔버스 아래 생성 시 localScale 2.5 버그 → 1로 교정. MCP play 모드 진입이 브리지 크래시 유발 → edit 모드 scene_view 검증으로 전환. **후속**: 나머지 10씬 구 버튼 정리 + DashboardController 데드 코드 제거 (모든 씬 재정리). |
| 2026-06-01 | **Stage B.1 UI — 인박스 패널 prefab (#447)**. `InboxEntryView` 신규 (행: 타이틀=`InboxTextResolver` / 안읽음 #3A3A4E / 카테고리 색 스트라이프 / 기한 D-N·만료). `InboxPanelController` MonoBehaviour 파트 (4×2 탭 Grid + ScrollView + empty label + `Refresh()` = state.inbox 필터→정렬→행 생성 + 클릭 시 isRead + InboxAction 라우팅). `GlobalNavController.ToggleInboxPanel`→`Refresh()`. GlobalNavPrefab InboxPanel 구성 + `InboxEntryPrefab` 신규 → nested Imported repo 커밋. **결정**: 탭은 plain UI Button (transition=None) — MUIP `UIManagerButton` 가 색 override 해 강조 토글과 충돌하므로 회피. 탭 라벨은 런타임 로컬라이즈 (`filter_all` + `inbox_category_*`). **함정**: edit-모드 prefab-stage scene_view 캡처에서 TMP/레이아웃 미빌드로 행이 안 보임 → 런타임 정상 (수치 검증). 행-기한 키 `inbox_deadline_days_fmt`/`inbox_deadline_expired` 추가 → ⚠️ LocalizationData.asset 재생성 필요. EditMode 559개 통과. |
| 2026-06-01 | **Stage W 진입 — Sub-A 명세 갱신 (#416)**. GlobalNav 생명주기 모순 (DDOL 싱글톤 ↔ 모든 씬 baked-in) 발견 → **씬별 인스턴스** 모델 확정 (사용자 결정). `design-decisions #58` 정정 + `v1.0-plan §3.19.5–6` + `class-diagram` Presentation Layer 신규 + `data-flows §8` GlobalNav 씬 진입 흐름 + `muip-reference §17` 정합. TopBar 아이콘 이모지 금지 함정 (NotoSansKR 미지원) 기록. Stage W Sub-PR: Sub-A(명세) → Sub-B(코드+테스트) → Sub-C(prefab+11+씬 주입). |
| 2026-06-04 | **Stage F 완료 — Transfer 필터 패널 + 협상 결함 검증 (#472)**. F.1 CA 필터 UI 폐기. F.2 `TransferSearchFilter.statThresholds`(fieldPath→최소값, AND) + `minWage/maxWage` + `StatCatalog`(Domain, 49 stat fieldPath/labelKey/accessor 단일 카탈로그) + `SearchPlayers` 필터 + EditMode T14/T15. F.3 필터 패널 분리 — 메인=검색창(MUIP Input)+[검색]/[필터]+"이적 시장" 타이틀, [필터]→ModalWindow(사용자 결정). F.4 #2 활성오퍼 제시금액 표시 확인(T17), #1 극저가 수락 = 일반 경로는 이미 Rejected(ratio<0.85), FA 경로 unreachable → 로직 결함 아님, T16 으로 고정, 밸런스 현행 유지(사용자 결정). **UI 함정**: MUIP CustomDropdown 이 모달 내 세로 레이아웃에서 펼침 겹침/갱신 깨짐 → **Unity TMP_Dropdown** 으로 (팝업 자체 캔버스). MUIP ButtonManager 가 런타임에 텍스트를 "BUTTON" 으로 리셋(+OpenSans) → **plain 버튼+한글 TMP 라벨**. MUIP Input Field 는 다크 재색상(Filled/Background) 후 사용. MUIP Input placeholder 텍스트 override 가 프리팹/UIManager 에 복원됨 → 국적 placeholder GameObject **삭제**로 해결. scene_view 캡처가 색/내용 변경 리렌더 지연 → 데이터 검증 + 사용자 플레이테스트로 확인. EditMode 17/17 (TransferSystemTests). |
| 2026-06-05 | **Stage R(플레이테스트 핫픽스) + Stage S(씬 폴리싱) 신설**. 2026-06-05 플레이테스트 피드백 18항목 트리아지(코드 조사 5클러스터 + FM 웹 리서치) → `design-decisions #72~#77` / `algorithms.md` V1.0-11.5·15·16·17·18 / `event-bus-catalog #76` 반영 후 작업 분할. **Stage R (R.1~R.13)**: 슈퍼유망주 동적라벨 / Trait 가시성+검색폐지 / 재정 밸런싱(주급차감+수입정상화+시설비) / 명성기반 선수수락 / 인박스 확장 / 글로벌 선수명 링크+가시성게이팅 / 프로필 재계약·콜업(유스한정) / 전역 단축키 / 보드 이행 보상 / WB·AM squad / 리그 리더보드 / 협상씬 버그+스크롤 / 부상복귀 텍스트. **Stage S (S.1~S.6)**: Dashboard/MainMenu/ClubSelect/Gacha/SeasonSummary MUIP 폴리싱 + SeasonSummary 정보보강 + UIManagerInputField NRE 핫픽스. **우선순위**: Stage R 대부분이 기존 미완 Stage보다 우선(플레이 직결, 사용자 명시). |
