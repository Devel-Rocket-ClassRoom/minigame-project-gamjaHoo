# V1.0 Tasks

V1.0 구현 작업 단위 분할. 의존성 순서대로 정렬되어 있으며, 위에서부터 차례로 작업한다.

각 작업은 **검증 가능한 완료 조건**을 가진다.

> **선행 문서:**
> - `docs/v1.0-plan.md` (광범위 명세 + Q1~Q12 결정)
> - `docs/design-decisions.md` #39~#52 (V1.0 결정사항)
> - `docs/algorithms.md` Part 2 V1.0-1 ~ V1.0-9 (V1.0 알고리즘 명세)
>
> **일정 (Q11 확정):** 마감 없음 — Stage 단위 GitHub Issue 일괄 생성 후 진행도는 보드 (#50) 추적.
>
> **Sub-PR 패턴 (V0.1 그대로):** Sub-A 명세 갱신 → Sub-B 구현 → Sub-C asset chore.

## 진행 상태 표기

- `[ ]` 미시작
- `[~]` 진행 중
- `[x]` 완료

---

## Stage A: 인프라 + 데이터 마이그레이션 (선행)

> Localization / String Table / SaveMigration 인프라 + 신규 도메인 클래스 스켈레톤. 다른 Stage 의 빌딩 블록.

### Task A.1: SaveMigration 인프라
- [x] `Persistence/SaveMigration.cs` 신규 — `Migrate(state, targetVersion)` 골격
- [x] `IMigrator` 인터페이스 + `Migrators` Dictionary
- [x] `GameState.saveVersion: int` 필드 추가 (디폴트 2 = V1.0)
- [x] `SaveSystem.Load` 통합 — 로드 후 마이그레이션 호출
- [x] `MigratorV1_0` 골격 (Q8 = `NotSupportedException`)
- [x] EditMode 테스트 — V0.1 세이브 로드 시 예외 / V1.0 세이브 정상 로드 / 가상 Migrator 라운드트립
- **완료 조건:** V0.1 세이브 로드 시 명확한 에러 + V1.0 신규 세이브 정상 로드 ✅

### Task A.2: Localization 시스템 + LocalizationSO
- [x] `Application/LocalizationSystem.cs` static — `Get(key, args)` / `CurrentLanguage` / `SetLanguage`
- [x] `Domain/ScriptableObjects/LocalizationSO.cs` — key → multi-language 매핑 (KO / EN)
- [x] `enum Language { Korean, English }`
- [x] CSV / JSON 임포트 헬퍼 (Editor)
- [x] 초기 시스템 언어 감지
- **완료 조건:** `Localization.Get("reroll_button")` 호출 시 현재 언어 텍스트 반환 ✅

### Task A.3: 기존 UI 한글 → key 추출
- [x] 11 씬 (MainMenu / ClubSelect / Gacha / Dashboard / Squad / PlayerProfile / Youth / Transfer / Schedule / Standings / Facility) 전수 한글 추출
- [x] `LocalizationSO` 인스턴스 채움 (~150-200 키 추정)
- [x] UI 컨트롤러 코드 — 직박 한글 → `Localization.Get(key)` 호출 교체
- **완료 조건:** 언어 토글 시 UI 텍스트 영/한 전환 정상 ✅

### Task A.4: 신규 도메인 클래스 스켈레톤
- [x] `HiddenAttributes.cs` (9 필드)
- [x] `Promise.cs` + `PromiseType / PromiseStatus` enum
- [x] `MentoringGroup.cs`
- [x] `Tactic.cs` + `TacticSlot.cs` + `Duty` enum + `Mentality` enum
- [x] `ScoutReport.cs` + `CaPaEstimate.cs` + `HiddenAttributesPartial.cs`
- [x] `SeasonAward.cs` + `AwardType` enum + `BoardPromise.cs` + `SeasonHistory.cs`
- [x] `TraitEffect.cs` + `TraitEffectType` enum
- [x] `Player.parentClubId / loanEndDate` 신규 필드 (Loan)
- [x] `Player.hiddenAttrs` 필드 추가
- [x] `PlayerState.happiness / suspendedMatches` 신규
- [x] `Contract.signingBonus / loyaltyBonus / appearanceBonus / goalBonus` 신규
- [x] `Club.tactic / scoutingKnowledge / season.mentoringGroups / season.captainPlayerId / season.viceCaptainPlayerId / season.boardPromises / season.dressingRoomMood` 신규
- [x] `Facilities` 8 필드 분리 (Scout / Training / YouthCoach / YouthRecruitment / YouthFacility / Medical / Stadium / Gym)
- [x] `TransferOffer.isLoan / loanFee / loanWageShare / loanEndDate / loanOption / counterAmount / negotiationRound / releaseClauseActivated` 신규
- [x] `OfferStatus.CounterOffer / Negotiating` enum 추가
- [x] `PlayerMatchStat` 확장 (shots / passes / tackles / interceptions / keyPasses / foulsCommitted / foulsSuffered)
- [x] `GameState.activePromises / activeAwards / managerReputation / nextPromiseId / nextAwardId` 신규
- **완료 조건:** 모든 클래스 `[Serializable]` + 컴파일 OK + 직렬화 라운드트립 EditMode 테스트 통과 ✅

### Task A.5: 신규 ScriptableObject 카탈로그 정의
- [x] `FormationSO.cs` + 6 카탈로그 정의 (4-4-2 / 4-3-3 / 3-5-2 / 4-2-3-1 / 4-4-1-1 / 5-3-2)
- [x] `PlayerRoleSO.cs` + 40 카탈로그 (포지션별 3-4 Role)
- [x] `GameBalanceSO.mentalityModifiers[7]` 외부화 (shot/pressure multiplier × 7단계)
- [x] `InjuryTypeSO.cs` 신규 + 15 카탈로그 채움
- [x] `FacilityType` enum 8종 확장 (Task A.4 완료)
- [x] `FacilityLevelSO` 카탈로그 — 8 type × 10 level = 80 asset (SeedV10Data.cs)
- **완료 조건:** Unity 메뉴 `FM-Lite/Seed/Generate V1.0 Data` 실행 시 80+ asset 자동 생성 ✅

---

## Stage B: 선수 능력치 재정의 (§3.1)

> `algorithms.md` V1.0-1 PlayerGen V1.0 적용. Stats 49 + 1-100 + Hidden Attributes.

### Task B.1: Stats 카테고리 재정의 (49)
- [x] `Stats.cs` 의 4 카테고리 클래스 재작성 (Technical 14 / Mental 14 / Physical 8 / Goalkeeping 13)
- [x] 신규 7 필드 추가 (Marking / Technique / LongThrows / Bravery / Flair / FirstTouchGk / PassingGk / PunchingTendency)
- [x] `ApplyToAll` 헬퍼 갱신
- [x] EditMode 테스트 갱신 (StatsTests 4 → 카테고리별 갱신)
- **완료 조건:** Stats 49 필드 / 직렬화 라운드트립 OK

### Task B.2: 스케일 1-100 + GameBalanceSO 재산정
- [x] `GameBalanceSO` stat 외부화 25 필드 1-100 기준 재산정 (statMeanAtCAFloor 5→25, statStdDev 2→10 등)
- [x] `ClampStat` 1-100 으로 변경
- [x] 시드 자산 (`Balance/GameBalance.asset`) Unity reimport
- **완료 조건:** PlayerGenerator 가 1-100 stats 정상 생성 / `algorithms.md` #1 의 Test Scenarios T1-T7 변경 (1-100 기준 임계치)

### Task B.3: Hidden Attributes 도입
- [x] `Player.hiddenAttrs` 필드 적용 (Task A.4 완료 후)
- [x] PlayerGenerator 5단계 Hidden 추첨 추가 (`algorithms.md` V1.0-1 의사코드)
- [x] `GameBalanceSO.hiddenAttrMean / hiddenAttrStdDev` 추가
- [x] 트레잇 가산점 (BigMatch → pressureHandling +20 등)
- [x] EditMode 테스트 (T6 Hidden 분포 / T7 트레잇 가산점)
- **완료 조건:** 100명 생성 → Hidden 9 필드 평균 ~50, std ~15

### Task B.4: Absolute / Relative 메타
- [x] `Utils/StatMetadata.cs` 신규 — stat name → Absolute/Relative 매핑 (하드코딩)
- [x] 10 Absolute stats 식별 (Determination / Work Rate / Leadership / Flair / Bravery / Aggression / Concentration / Natural Fitness / Composure / Decisions)
- **완료 조건:** `StatMetadata.IsAbsolute("determination") == true` / `IsAbsolute("passing") == false`

### Task B.5: UI 항상 세부 스탯 노출
- [x] `PlayerProfileController.cs` 갱신 — `isDebugMode` 무관 자기 구단 선수 stats 정확 수치 노출
- [x] 타 구단 선수 → Stage E 전까지 임시로 정확 수치 노출
- [x] Squad 화면 stat 카테고리별 표시 갱신 (PlayerProfile 진입 경로 — CA 수치 표시 유지)
- [x] 스탯 컷오프 상수 제거 (StatElite=17 등 삭제, 가챠 TierGrade enum 별도 유지)
- [x] B.1 신규 필드 UI 반영 + Localization 키 추가 (marking/technique/longThrows/bravery/flair/firstTouchGk/passingGk/punchingTendency)
- **완료 조건:** 가챠 화면만 5단계 티어, 나머지는 정확 수치 1-100

---

## Stage C: 트레잇 재정의 (§3.2)

### Task C.1: TraitSO 효과 시스템
- [x] `TraitEffect.cs` + `TraitEffectType` enum (`MatchModifier / GrowthRateModifier / InjuryRateModifier / MoralePropensity / MarketValueModifier`)
- [x] `TraitSO.effects: List<TraitEffect>` 필드 추가
- **완료 조건:** TraitSO 인스펙터에서 effects 편집 가능

### Task C.2: 신규 트레잇 정의
- [x] V1.0 카탈로그 14개 신규 (클러치 / 무리한패스 / 와이드플레이 / 자국인우대 / 유리몸 / 철인 / 멘탈약자 / 슈퍼유망주 / 멀티포지션 / 골결정력 / 수비형윙백 / 정신적리더 / 페널티스페셜리스트 / 프리킥마이스터)
- [x] 각 trait `effects` 매핑 (기존 6개 포함 총 20개)
- [x] `SeedV10Data.GenerateTraitsV10()` 추가 (GenerateAll 에서 호출)
- **완료 조건:** ~20 TraitSO asset, 각 effects 채워짐

### Task C.3: 충돌 그룹 확장
- [x] Group 2 (Durability — 유리몸 / 철인) 신규
- [x] Group 3 (PressureMentality — 빅매치형 / 멘탈약자) 신규
- [x] TraitSO.exclusionGroupId 갱신
- **완료 조건:** PlayerGenerator 4단계 트레잇 추첨 시 같은 그룹 X

---

## Stage D: 시설 시스템 (§3.10)

### Task D.1: FacilityType 8종 + FacilityLevelSO 카탈로그 80개
- [x] `enum FacilityType` 8종 확장 (Task A.5 와 짝)
- [x] `FacilityLevelSO` 80 asset (8 type × 10 level)
- [x] 비용 곡선 `baseCost × pow(level, 2.5)` 적용
- [x] 효과 필드 정의 (Training.growthRateBonus / Medical.recoverySpeedBonus / Stadium.incomeMultiplier 등)
- **완료 조건:** FacilityLevelSO 80 asset 자동 생성 (Seed 스크립트) + 인스펙터 정확

### Task D.2: Facilities 도메인 8 필드 분리
- [x] V0.1 `Facilities.scoutLevel / trainingLevel / youthLevel` → V1.0 8 필드
- [x] `FacilityUpgrade.cs` 신규 + `Facilities.activeUpgrades` 추가
- [x] 기본값 모두 1
- **완료 조건:** ClubGenerator 새 구단 생성 시 8 필드 채움

### Task D.3: FacilitySystem 병렬 업그레이드
- [x] `FacilitySystem.StartUpgrade` — 동일 타입 차단만 유지, N개 동시 발주 가능
- [x] `Facilities.activeUpgrades: List<FacilityUpgrade>` (V0.1 3 필드 → 가변 리스트)
- [x] `FacilitySystem.GetLevel/SetLevel` 8종 확장
- [x] `FacilitySystem.ProcessUpgrades` 다중 업그레이드 처리
- **완료 조건:** 자금만 있으면 N개 동시 발주 가능

### Task D.4: 시설 효과 본격 도입 ✅ (#195 머지 — Sub-A #286 / Sub-B #287)
- [x] **Training** → Player Growth System 신규 (GrowthSystem.cs / 매월 1일 / 2단계 모델 +1/+2/+3)
- [x] **Medical** → InjurySystem.ComputeRecoveryDays + ComputeInjuryRate (floor 0.5) + ProcessRecovery (DailyProcessor 통합)
- [~] **Stadium** → Stage M.6 의존 (D.4 책임 X — 명세 #53 에 분리)
- [x] **Gym** → GrowthSystem 피지컬 보정 + InjurySystem 회복 보조
- [~] **Scout** → Stage E.2 의존
- [~] **YouthCoach / YouthRecruitment / YouthFacility** → Stage L.1-3 의존
- **완료 조건:** Training + Medical + Gym 직접 효과 적용 ✅ / Stadium·Scout·Youth* 은 후속 Stage 의존 ✅

### Task D.5: FacilityScene UI 갱신 (진행 중 — #196)
- [x] 8 시설 표시 (FacilityRowController 동적 생성)
- [x] 등급 1/10 + 다음 등급 효과 + 비용 (FacilityEffectFormatter)
- [x] 진행 중 업그레이드 목록 (다중)
- **완료 조건:** UI 에서 8 시설 정상 표시 + 발주 가능 ✅ (코드 + prefab + 씬 작업 완료, PR 진행 중)

---

## Stage E: 스카우트 시스템 (§3.7)

### Task E.1: ScoutReport 도메인 + Club.scoutingKnowledge ✅ (#197 — PR #289)
- [x] Task A.4 의 신규 클래스 활용 (ScoutReport / CaPaEstimate / HiddenAttributesPartial 이미 정의됨)
- [x] `Club.scoutingKnowledge` Dictionary 초기화 로직 (default `new Dictionary`)
- [x] Newtonsoft.Json 직렬화 라운드트립 테스트 (ScoutReportTests 6 케이스)
- **완료 조건:** ScoutReport 직렬화 라운드트립 OK ✅

### Task E.2: ScoutingSystem 명단 관리 ✅ (#198)
- [x] `Application/ScoutingSystem.cs` 신규 — `UpdateKnowledge(state, balance)`
- [x] 자기 구단 → 자동 (senior + youth / scoutLevel 100 / 정확 estimate)
- [x] 외부 명단 — Scout 시설 등급 기반 무작위 (단일 리그 단순화)
- [x] 매주 누적 — `scoutWeeklyLevelGain` (5) / margin 감소 / estimate 실제값 수렴
- [x] `DailyProcessor` 매주 월요일 호출
- [x] 외부화: `scoutWeeklyLevelGain` + `FacilityLevelSO(Scout).scoutingListSize / caAccuracyMargin`
- [x] EditMode 테스트 T1~T7
- **완료 조건:** 시설 Lv5 클럽 → 명단 ~3000명, Lv1 클럽 → ~50명 ✅ (단일 리그라 후보 수 제약은 V2.0+ 다중 리그 도입 시 확장)

### Task E.3: TransferSystem.SearchPlayers 가시성 분기 ✅ (#199)
- [x] `ScoutingVisibility.cs` 헬퍼 — `IsScouted / GetTier / GetTierLocalizationKey` (UI 표시 책임 분리)
- [x] 정성적 라벨 5단계 — VeryHigh / High / Average / Low / VeryLow (Localization 5 키 추가)
- [x] `requireScouted` 폐기 — 코드에 없었음 (명세상 폐기, SearchPlayers 시그니처 그대로)
- [x] 디버그 모드 무관 모두 정확 노출 (`IsScouted(..., isDebugMode: true)`)
- [x] EditMode 테스트 ScoutingVisibilityTests 13 케이스
- **완료 조건:** 가시성 헬퍼 구현 + 라벨 5단계 정의 ✅. UI 표시 분기는 E.4 책임.

### Task E.4: UI Transfer 검색 결과 표시 ✅ (#200 — 텍스트 정성화만, 회색조/자물쇠 V1.x)
- [x] TransferPlayerItem.Setup 에 userClub 추가 + ScoutingVisibility 활용 가시성 분기
- [x] 명단 ∈ = 정확 수치 (CA "145" / Market "£12.5M")
- [x] 명단 ∉ = 정성적 라벨 (CA "높음" / Market "중간") via Localization 5 키 (E.3 추가됨)
- [~] 회색조 + 자물쇠 아이콘 = **V1.x** (사용자 결정 — prefab 추가 부담 회피)
- [x] EditMode 테스트 TransferPlayerItemVisibilityTests 4 케이스
- **완료 조건:** UI 명단 가시성 차이 명확 (텍스트 라벨로) ✅. 회색조/자물쇠 시각적 강조 = V1.x 보강.

---

## Stage F: AI 구단 영입 (§3.7.5, §3.8.1)

### Task F.1: CpuTransferAi 구현 ✅ (#201)
- [x] `Application/CpuTransferAi.cs` 신규 — `Run(state, balance)`
- [x] 5 트리거 (`algorithms.md` V1.0-5) — 트리거 1/3/5 실제 작동 / 2 (CoreInjury) 는 Stage I 매치 엔진 의존 / 4 (PromiseRisk) 는 Stage M.5 boardPromises 의존 (안전 분기)
- [x] 후보 선수 추첨 (CA 가중) + `TransferSystem.SubmitOffer` 호출
- [x] 외부화: `aiWeaknessRatioThreshold / aiCoreInjuryWeeksThreshold / aiSavingsThreshold / aiOfferAmountRandomMin / aiOfferAmountRandomMax / aiBudgetRatio / aiContractFaThresholdDays`
- [x] EditMode 테스트 T1~T6 (빈 state / userClub 제외 / 약점 트리거 / 후보 없음 / 결정성 / 자금 부족)
- **완료 조건:** EditMode T1~T6 통과 ✅

### Task F.2: EventScheduler 통합 ✅ (#202)
- [x] 매주 월요일 `CpuTransferAi.Run` 호출 (DailyProcessor 에 통합 — ScoutingSystem 다음 / 명단 활용)
- **완료 조건:** 시즌 진행 시 AI 구단 오퍼 자연 발생 (state.activeOffers 갱신) ✅

---

## Stage G: 사기 / 약속 시스템 (§3.4)

### Task G.1: MoraleSystem 변동 트리거 ✅ (#203)
- [x] `Application/MoraleSystem.cs` 신규 — `Tick / OnMatchFinished / OnTransferCompleted / OnContractRenewed / OnPromiseFulfilled / OnPromiseBroken / OnInterview`
- [x] 변동 매트릭스 (`algorithms.md` V1.0-6) — 매치 결과 / 평점 / Hidden 보정 / 약속 미이행 / TransferRequest 발행
- [x] Hidden Attributes 보정 — professionalism (변동폭 ±30%) / loyalty (충격 완화 ±50%). ambition 은 G.2 출전시간 미달 trigger 도입 시 활용.
- [x] `MatchPostProcessor` 통합 — V0.1 미구현 사기 갱신 본격 도입
- [x] `DailyProcessor` 통합 — Tick 호출 (매일 50 수렴)
- [~] **T8 (라커룸 분위기 < 30 → 폼 -5) 는 G.3 책임으로 이연** — `Club.season.dressingRoomMood` 갱신 + MatchSimulator 입력은 G.3 / Stage I 매치 엔진과 짝.
- **완료 조건:** T1~T7 통과 (T8 는 G.3) ✅

### Task G.2: PromiseSystem 4종 + 면담 (Sub-A #204 ✅ / Sub-B 진행 중 #300)
- [x] `Application/PromiseSystem.cs` 신규 — `CheckProgress(state, balance)` + `Create*` 헬퍼 4종 (Sub-A)
- [x] Promise 4 종류 평가 로직 (PlaytimeAgreement / TransferIn / Renewal / TransferOut) — Promise.cs 도메인은 A.4 완료 (Sub-A)
- [x] `GameState.activePromises / nextPromiseId` — A.4 완료, Create 헬퍼 사용 (Sub-A)
- [x] `DailyProcessor` 통합 — 매주 월요일 `CheckProgress` 호출 (Sub-A)
- [x] MoraleSystem.OnInterview Promise* 본격 wire-up (Sub-A)
- [x] seasonAppearances tracking — PlaytimeAgreement 평가 기반 (Sub-A)
- [x] PromiseCreatedEvent / PromiseFulfilledEvent / PromiseBrokenEvent 3종 (Sub-A)
- [x] **면담 dialog UI** (PlayerProfile [면담] 버튼 + InterviewDialogController 4 멘트) (Sub-B)
- [x] **Dashboard 인박스** (5 이벤트 구독, in-memory 메시지 리스트) (Sub-B)
- [x] **PromiseDeadlineApproachingEvent** (30일 임박, Promise.deadlineNotified 플래그) (Sub-B)
- [x] Localization 키 15개 (면담 4 + 인박스 5 + Promise 타입 4 + 기타 2) (Sub-B)
- **완료 조건:** Promise 생성 → deadline 도래 → 자동 status 확정 → 사기 변동 + 이벤트 발행 + 인박스 알림 + 면담 dialog 동작 ✅ (Sub-A T1-T13 / Sub-B T14-T15)

### Task G.3: 라커룸 분위기 ✅ (#205)
- [x] `Club.season.dressingRoomMood` 갱신 (월 1회) — `MoraleSystem.UpdateDressingRoomMood` + `MoraleSystem.Tick` 매월 1일 호출
- [x] 캡틴 leadership 가산점 — `captain.stats.mental.leadership × dressingRoomCaptainLeadershipBonus (0.2)`
- [x] < 30 → 시즌 폼 -5 보정 — `MatchSimulator.MoodFactor` 0.95f 곱 (strength 5% 감소 ≈ 폼 -5)
- **완료 조건:** EditMode T8 (UpdateDressingRoomMood 5 케이스 + MatchSimulator 통합) ✅

### Task G.4: TransferRequest 자동 트리거 ✅ (#206)
- [x] Happiness < 20 → `TransferRequestEvent` 발행 — G.1 (`MoraleSystem.OnPromiseBroken`)
- [x] Dashboard 인박스 표시 — G.2 Sub-B (`DashboardController.OnTransferRequest` → `PushInbox`)
- [x] 유저 응답 (수락 / 거절 / 면담) — `TransferRequestDialogController` 신규 (3 버튼 + 자기구단 한정 auto-show). 수락=`transferListed=true` / 거절=dismiss / 면담=PlayerProfileScene 진입
- [x] Localization 6 키 (dialog title / message / accept / reject / interview / close)
- **완료 조건:** Happiness 강제 < 20 → 자동 알림 + 다이얼로그 응답 가능 ✅

---

## Stage H: 계약 시스템 (§3.3)

### Task H.1: RenewContract + AI 응답
- [ ] `TransferSystem.RenewContract` 메서드 신규 (`algorithms.md` V1.0-3.1)
- [ ] AI 응답 로직 (loyalty / ambition / 잔여 기간 가산점)
- [ ] 사기 회복 (contractRenewalMoraleBoost / contractRenewalHappinessBoost)
- [ ] `ContractRenewedEvent / ContractRenewalRejectedEvent` 신규
- **완료 조건:** 주급 ×1.5 재계약 → 거의 무조건 수락 / morale +15 / happiness +25

### Task H.2: Contract 옵션 확장
- [ ] `Contract.signingBonus / loyaltyBonus / appearanceBonus / goalBonus / releaseClause`
- [ ] UI 협상 화면에서 입력
- [ ] release clause 활성화 — 오퍼 amount ≥ clause → 강제 Accepted
- **완료 조건:** release clause 시나리오 검증

### Task H.3: FA 시장 (잔여 6개월)
- [ ] `TransferSystem.SubmitFreeAgentContract` 신규
- [ ] 잔여 6개월 이내 선수에게 타 구단 직접 계약 제안 (이적료 0)
- **완료 조건:** EditMode T (FA 영입 시나리오)

---

## Stage I: 매치 엔진 재작성 (§3.5) — V1.0 최대 작업

### Task I.1: 분 단위 이벤트 엔진 골격
- [ ] `MatchSimulator.Simulate` 인터페이스 유지 / 내부 재작성
- [ ] 분 단위 step 루프 (1~90)
- [ ] 시드 결정성 (`algorithms.md` V1.0-2 1단계)
- **완료 조건:** T1 결정성 통과

### Task I.2: 이벤트 종류 정의 + 발생 확률
- [ ] Shot / Save / Foul / Card / Injury / Substitution / Pass / Cross / KeyPass / OffsidesCalled / Goal / Assist
- [ ] 발생 공식 (`algorithms.md` V1.0-2 이벤트 표)
- [ ] stat 직접 참조 (finishing × composure 등)
- **완료 조건:** EditMode 시뮬 시 이벤트 분포 명세 범위

### Task I.3: 부상 / 카드 / 정지 시스템
- [ ] `InjuryTypeSO` 카탈로그 ~15 카탈로그 활용
- [ ] 부상 발생 → `InjuryInfo` + `PlayerInjuredEvent`
- [ ] 옐로 5장 → suspendedMatches=1
- [ ] 레드 → suspendedMatches 1-3
- [ ] starting11 선정 시 injury / suspendedMatches 제외
- **완료 조건:** T2 부상 빈도 / T3 카드 누적 / T4 정지 검증

### Task I.4: 평점 시스템
- [ ] 이벤트별 가산점 (`algorithms.md` V1.0-2 평점 표)
- [ ] PlayerMatchStat.rating 본격 채움
- **완료 조건:** T5 평점 계산 정확

### Task I.5: 텍스트 이벤트 (유저 매치 한정)
- [ ] `Match.events` 본격 채움 (유저 매치)
- [ ] 핵심 이벤트만 (~15-20)
- [ ] `MatchEvent.textKey / textArgs` String Table 활용
- [ ] LocalizationSO 에 매치 이벤트 텍스트 ~30 키 추가
- **완료 조건:** T9 텍스트 이벤트 분량 검증

### Task I.6: SubstitutionAI
- [ ] `Application/SubstitutionAI.cs` 신규 — `DecideSubstitution(matchState, balance)`
- [ ] fatigue 70+ / Injury / 스코어 상황 기반
- [ ] PlayerMatchStat.minutesPlayed 가변
- **완료 조건:** EditMode 매치 시뮬 시 자동 교체 발생

### Task I.7: SimulateLite (비활성 경량)
- [ ] `MatchSimulator.SimulateLite` 분리
- [ ] V0.1 단순 Poisson + 라인 가중 (기존 V0.1 로직 재활용)
- [ ] `BackgroundSimulator.SimulateDay` 분기 (활성/비활성)
- [ ] `MatchPostProcessor.Process(..., publishEvent: false)` 옵션
- **완료 조건:** T4 SimulateLite events 비움 / playerStats 22명만

### Task I.8: 외부 영향 (form / morale / fatigue)
- [ ] effectiveCA 곱셈 보정 적용
- [ ] formCoefficient / moraleCoefficient / fatigueCoefficient 외부화
- **완료 조건:** T7 외부 영향 검증

### Task I.9: strengthExponent 폐기
- [ ] `GameBalanceSO.strengthExponent` 제거
- [ ] `algorithms.md` #33 보강 → 폐기 표시
- [ ] 기존 외부화 자산 reimport
- **완료 조건:** strengthExponent 참조 0건 + 매치 결과 결정성 유지

---

## Stage J: 전술 시스템 (§3.6)

### Task J.1: FormationSO 추출 + 카탈로그
- [ ] `GameBalanceSO.formation: FormationConfig` nested → `FormationSO` 추출
- [ ] 5-6 카탈로그 (4-4-2 / 4-3-3 / 3-5-2 / 4-2-3-1 / 4-4-1-1 / 5-3-2)
- [ ] `LeagueConfigSO.availableFormations: List<FormationSO>` 검토
- **완료 조건:** 5-6 FormationSO asset

### Task J.2: PlayerRoleSO 카탈로그 + Tactic 도메인
- [ ] PlayerRoleSO ~40 카탈로그 (Task A.5 와 짝)
- [ ] 각 Role 의 `eventModifiers` 채움 (Poacher.Shot=1.5 등)
- [ ] `Tactic` 도메인 `Club.tactic` 본격 활용
- [ ] 자동 Tactic 생성 (`ClubGenerator` 가 디폴트 4-4-2 + 자동 Role 배정)
- **완료 조건:** Tactic 직렬화 라운드트립 + 매치 시뮬 입력 가능

### Task J.3: Mentality + Team Instructions
- [ ] `enum Mentality` 7단계
- [ ] `GameBalanceSO.mentalityModifiers[7]` 외부화
- [ ] **Team Instructions 는 V1.x** — V1.0 미구현 (확인용 placeholder 만)
- **완료 조건:** Mentality 7단계 매치 시뮬 입력 정상

### Task J.4: TacticImpact 시스템
- [ ] `Application/TacticImpact.cs` 신규 — `ComputeEventWeight(tactic, playerId, state, eventType, balance)`
- [ ] Role × Duty × Mentality × Stat 곱셈
- [ ] `MatchSimulator.Simulate` 통합 — 이벤트 주체 선수 추첨 시 호출
- **완료 조건:** T1~T4 (`algorithms.md` V1.0-7 Test)

### Task J.5: LineupScene + TacticScene
- [ ] `Scenes/TacticScene.unity` 신규 — Formation 선택 + 11 슬롯 Role / Duty 편집 + Mentality
- [ ] `Scenes/LineupScene.unity` 신규 — 11명 수동 배정 (Tactic.slots 의 assignedPlayerId)
- [ ] 자동 라인업 버튼 (Role 호환 + top CA + 폼/사기 가산 + 부상/정지 제외)
- [ ] Set Pieces 담당자 지정 UI
- **완료 조건:** 사용자가 Tactic 편집 → MatchSimulator 가 적용

### Task J.6: 캡틴 / 부캡틴
- [ ] `Club.season.captainPlayerId / viceCaptainPlayerId`
- [ ] 자동 (leadership + age + 계약 잔여) / 수동 변경
- [ ] 라커룸 분위기 +5 가산점
- [ ] Squad 화면 [캡틴 임명] 버튼
- **완료 조건:** 캡틴 변경 가능 + 라커룸 효과 검증

### Task J.7: 가챠 시 포메이션 랜덤화
- [ ] `StartingSquadGacha` 또는 `ClubGenerator` 가 포메이션 추첨
- [ ] 빅클럽 / 약체 가중치 차이
- [ ] ClubGen 분배표 = 추첨된 FormationSO 기준
- **완료 조건:** 20 구단 포메이션 다양성 검증 (EditMode T)

---

## Stage K: 협상 / 임대 시스템 (§3.8)

### Task K.1: CounterOffer + 다중 라운드
- [ ] `OfferStatus.CounterOffer` 활성화
- [ ] `AiRespondToOffer` 4분기 (Accepted / CounterOffer / Rejected / Mocking)
- [ ] `TransferSystem.RespondToCounterOffer` 메서드
- [ ] maxNegotiationRounds (3) 외부화
- **완료 조건:** CounterOffer 시나리오 검증

### Task K.2: 선수 개인 협상 (Negotiating)
- [ ] AI 판매 Accepted → `OfferStatus.Negotiating` 단계 도입
- [ ] 선수 응답 로직 (loyalty / ambition / 주급 / 출전시간 약속)
- [ ] `EstimateInitialWage` 헬퍼
- **완료 조건:** EditMode T (loyalty 80 vs 20 거절률 차이)

### Task K.3: 임대 시스템 (Loan)
- [ ] `TransferOffer.isLoan / loanFee / loanWageShare / loanEndDate`
- [ ] `Player.parentClubId / loanEndDate` (Task A.4 와 짝)
- [ ] `TransferSystem.SubmitLoanOffer`
- [ ] CompleteTransfer 분기 (임대 vs 영구)
- [ ] DailyProcessor 임대 종료 자동 복귀
- [ ] `LoanReturnedEvent` 신규
- **완료 조건:** 임대 → 종료 → 자동 복귀 라운드트립

### Task K.4: 트랜스퍼 리스트 활성화
- [ ] `Player.state.transferListed` 본격 활용
- [ ] 자동 트리거 (Happiness < 20 → TransferRequestEvent → 유저 승인 → transferListed)
- [ ] CalculateMarketValue × 0.7 (할인)
- [ ] CpuTransferAi 우선순위 ↑
- **완료 조건:** TransferRequest → 유저 수락 → transferListed → AI 영입 가시화

### Task K.5: NegotiationScene UI
- [ ] 협상 진행 화면 (라운드 / amount / status 표시)
- [ ] CounterOffer 응답 옵션
- [ ] 선수 협상 단계 표시
- **완료 조건:** UI 협상 전 흐름 동작

---

## Stage L: 유스 시스템 갱신 (§3.9)

### Task L.1: 유스 CA 캡 + PA 분포 조정
- [ ] `GameBalanceSO.youthMinCa = 30 / youthMaxCa = 95 / youthCaGapMean = 60`
- [ ] `YouthSystem.GenerateIntake` 갱신 — CA 캡 적용 (`algorithms.md` V1.0-4)
- [ ] PlayerGenerator `forceCa / forcePa` 파라미터 추가
- **완료 조건:** EditMode T (유스 100명 생성 → 모든 CA ≤ 95)

### Task L.2: 풀 전체 영입 + 시설 인원 제한
- [ ] `YouthSystem.SignPlayers` 갱신 — maxSign 검증 (시설 등급 비례)
- [ ] UI YouthScene "전체 선택" 버튼
- **완료 조건:** Lv5 시설 클럽 = 풀 전체 영입 가능 / Lv1 = 풀 33% 만

### Task L.3: 유스 시설 분리 (Coach / Recruitment / Facility) ⚠️ 회귀 버그 진행 중
- [ ] Task A.5 / D.1 의 분리된 시설 활용
- [ ] YouthCoach → 평균 PA + 고급 트레잇 가중치
- [ ] YouthRecruitment → 풀 사이즈 + 인스펙션 빈도 (Lv7+ = 보조 인스펙션 추가 검토)
- [ ] YouthFacility → 유스 성장률 + 1군 콜업 적응
- **완료 조건:** 각 시설 효과 분리 검증

**⚠️ 회귀 버그 (2026-05-26 발견)**: V1.0 D.1 (FacilityLevelSO 시설별 필드 분리) 머지 후 `YouthSystem.cs` + `YouthPoolController.cs` 가 V0.1 패턴 유지 (FacilityType.YouthCoach 에서 `youthPoolSize` + `youthAvgPA` 둘 다 읽으려 시도) → V10 시드는 `youthPoolSize` 를 YouthRecruitment 에만 / `youthAvgPABonus` 를 YouthCoach 에만 분배. **결과: 유스 인스펙션 발동 시 후보 0명, 리롤 / 영입 확정 버튼 비활성** (게임 핵심 사이클 차단). Stage L.3 진입 시 다음 코드 수정 필수:

- [YouthSystem.PopulateCandidates:187](Assets/_Project/Scripts/Application/YouthSystem.cs#L187) — `FacilityType.YouthCoach` → `FacilityType.YouthRecruitment` 로 `youthPoolSize` 조회 변경. `youthAvgPA` 계산은 YouthCoach 의 `youthAvgPABonus` + balance 기본값 결합.
- [YouthSystem.SampleYouthPA:263](Assets/_Project/Scripts/Application/YouthSystem.cs#L263) — `facility.youthAvgPA` 참조 갱신 (위와 동일).
- [YouthPoolController.Refresh:141](Assets/_Project/Scripts/UI/YouthPoolController.cs#L141) — UI 의 facility 조회도 YouthRecruitment 로 변경 + `_userClub.facilities.youthRecruitmentLevel` 사용.

### Task L.4: Mentoring 시스템
- [ ] `Application/MentoringSystem.cs` 신규 — `RunMentoring(state, balance)`
- [ ] Hidden Attributes 수렴 (월 1회)
- [ ] `MentoringGroup` 도메인 활용
- [ ] DailyProcessor 매월 1일 호출
- [ ] `MentoringScene` UI (그룹 만들기 / 해체)
- **완료 조건:** Mentor 80 + Mentee 20 → 6개월 후 mentee professionalism +30 정도

### Task L.5: 1군 콜업 자동 트리거 + 유저 승인
- [ ] `YouthSystem.CheckPromotionCandidates`
- [ ] `YouthPromotionSuggestedEvent` 발행
- [ ] Dashboard 인박스 + PlayerProfile [1군 승격] / [거절]
- **완료 조건:** 18세 + CA ≥ 클럽 평균 70% → 자동 알림 + 유저 응답

### Task L.6: 미영입 후보 → AI 다른 구단 영입
- [ ] `YouthSignedByOtherEvent` 신규
- [ ] `SignPlayers` 갱신 — rejected 일부 AI 영입 (확률)
- **완료 조건:** 미영입 후보 ~30% AI 다른 구단 영입 / `rejectedPlayerIds` 보존

### Task L.7: 라운드별 포지션 가중치
- [ ] `youthPositionWeightVolatility` 외부화 + SamplePositionWeights 헬퍼
- **완료 조건:** 인스펙션마다 포지션 분포 다양성

---

## Stage M: 시즌 시스템 확장 (§3.11)

### Task M.1: Player.career 시즌 통계 저장
- [ ] `SeasonEndProcessor.Run` 신규 단계 — 모든 선수 시즌 통계 → `Player.career.Add(...)`
- [ ] `SeasonStat` 필드 확장 (yellowCards / redCards / minutesPlayed / competition)
- [ ] PlayerProfile UI 시즌별 커리어 표시
- **완료 조건:** 1 시즌 완주 후 `Player.career.Count > 0`

### Task M.2: 시상 시스템
- [ ] `Application/SeasonAwardSystem.cs` 신규 — `ComputeSeasonAwards(state, balance)`
- [ ] AwardType 7종 (LeagueMVP / TopScorer / TopAssist / YoungPlayer / BestEleven / GoldenGlove / ManagerOfSeason)
- [ ] `state.activeAwards / nextAwardId`
- [ ] `League.history: List<SeasonHistory>`
- [ ] 수상 시 morale / happiness 가산
- [ ] `AwardWonEvent` 발행
- **완료 조건:** EditMode T1~T6 (`algorithms.md` V1.0-9)

### Task M.3: 월간 어워드
- [ ] `SeasonAwardSystem.ComputeMonthlyAwards(state, balance)`
- [ ] `DailyProcessor` 매월 1일 호출
- [ ] Manager / Player of the Month
- [ ] boardConfidence +5 / morale +10
- **완료 조건:** 월 1회 어워드 발행 + 효과 적용

### Task M.4: 보드 평가 + 경질
- [ ] `Club.season.boardConfidence` 본격 변동
- [ ] 매월 평가 (목표 - 실제 순위 × multiplier)
- [ ] 매치 평가 (패배 -2 / 빅매치 패배 -5 / 승리 +1)
- [ ] < 30 → `BoardWarningEvent` / < 10 → `ManagerSackedEvent`
- [ ] V1.0 = Game Over (V1.x 다른 구단 부임)
- **완료 조건:** boardConfidence 강제 < 10 → Game Over 화면

### Task M.5: 보드 약속 (시즌 시작)
- [ ] `BoardPromise` 도메인 / `BoardPromiseType` enum
- [ ] `Club.season.boardPromises`
- [ ] `NewSeasonProcessor.Run` 신규 단계 — 보드 약속 생성 + 매니저 수락/거절 UI
- [ ] 거절 시 boardConfidence -10
- **완료 조건:** 새 시즌 시작 시 보드 약속 UI 표시

### Task M.6: 재정 결산
- [ ] `SeasonEndProcessor.Run` 신규 단계
- [ ] 입장료 (`baseStadiumIncome × stadiumLevel × clubReputation × homeMatches`)
- [ ] TV 중계권 (`baseTvFee × clubReputation × leaguePosition`)
- [ ] 상금 (리그 순위별 차등)
- [ ] `Club.finance.money / transferBudget / wageBudget` 갱신
- **완료 조건:** 시즌 종료 후 클럽 자금 변동 검증

### Task M.7: Match 데이터 압축
- [ ] `SeasonEndProcessor.Run` 신규 단계
- [ ] 직전 시즌 외 Match `events / playerStats` 비움
- [ ] 우승 / 강등 / 시상 정보만 보존 (`League.history`)
- **완료 조건:** 3 시즌 플레이 후 세이브 파일 크기 ↓ 검증

### Task M.8: 매니저 평판
- [ ] `GameState.managerReputation`
- [ ] 변동 트리거 (우승 +20 / 승격 +15 / 보드 약속 이행 +5 / 경질 -30 / 월간 매니저 +5)
- [ ] boardConfidence 가산 효과
- **완료 조건:** 우승 후 managerReputation +20 검증

### Task M.9: SeasonSummaryScene
- [ ] 시즌 종료 시 자동 표시
- [ ] 시상 / 보드 평가 / 재정 결산 / 다음 시즌 목표
- **완료 조건:** 5/15 도래 시 자동 표시

---

## Stage N: UI 인터랙티브 + 신규 씬 (§3.14)

### Task N.1: PlayerNameLinkController
- [ ] `UI/PlayerNameLinkController.cs` 신규 — TMP_Text + Button + playerId
- [ ] 클릭 → PlayerProfile 씬 + PlayerPrefs("SelectedPlayerId") + PlayerPrefs("PreviousScene")
- [ ] Standings / Schedule / Transfer / PlayerProfile 시즌 통계 / 매치 결과 / 라커룸 모든 선수 이름 클릭 가능
- **완료 조건:** 모든 씬에서 선수 이름 → PlayerProfile 점프 + 뒤로가기

### Task N.2: Dashboard 인박스 + 다음 매치 정보
- [ ] 인박스 패널 (Promise / Board / 매치 알림 / TransferRequest / YouthPromotion)
- [ ] 다음 매치 정보 (상대 폼 + 직전 결과 + 상호 전적)
- [ ] 사기 요약 (불만 선수 목록)
- [ ] 부상 / 정지 명단
- **완료 조건:** Dashboard 종합 정보 표시

### Task N.3: MatchTextScene
- [ ] `Scenes/MatchTextScene.unity` 신규
- [ ] Match.events 분 순서 표시
- [ ] 가속 ×1 / ×2 / ×4 / 스킵 (Q6)
- [ ] 결과 화면 (스코어 + 득점자 + 평점)
- **완료 조건:** 유저 매치 시 자동 진입 + 가속 동작

### Task N.4: NegotiationScene
- [ ] Stage K.5 참조
- **완료 조건:** Stage K 통합

### Task N.5: PromiseInboxScene (또는 Dashboard 통합)
- [ ] active promises 목록
- [ ] 진행률 표시 (예: 출전시간 35% / 목표 50%)
- **완료 조건:** Promise 진행 UI 동작

### Task N.6: Squad / Transfer 검색 / 필터 강화
- [ ] Transfer 검색: 포지션 / CA 범위 → + 연령 / 국적 / 트레잇 / 시장가 / 계약 잔여
- [ ] Squad 검색: 자기 구단 한정
- **완료 조건:** 7-8 필터 옵션 동작

### Task N.7: 신규 씬 등록
- [ ] EditorBuildSettings 에 LineupScene / TacticScene / MatchTextScene / NegotiationScene / MentoringScene / SeasonSummaryScene / PromiseInboxScene 추가
- **완료 조건:** 모든 씬 빌드 포함

---

## Stage O: 통합 테스트

### Task O.1: IntegrationTests 갱신
- [ ] V1.0 시스템 통합 시나리오 (한 시즌 완주 + 사기 변동 + Promise 사이클 + 매치 텍스트 + 시상)
- [ ] 기존 T1~T3 (V0.1) 보강
- **완료 조건:** 한 시즌 V1.0 시스템 전체 통합 동작

### Task O.2: SaveMigration 라운드트립 테스트
- [ ] V1.0 세이브 라운드트립 (모든 신규 필드)
- [ ] V0.1 세이브 로드 시도 → 예외 검증
- **완료 조건:** 라운드트립 정확 + V0.1 무효 메시지

### Task O.3: 매치 텍스트 이벤트 결정성 테스트
- [ ] 같은 시드 매치 → 같은 텍스트 시퀀스
- **완료 조건:** 결정성 검증

### Task O.4: 사기 / 약속 시스템 시나리오 테스트
- [ ] PlaytimeAgreement 약속 → 출전 미달 → Happiness 변동 → TransferRequest 발행
- [ ] loyalty 80 vs 20 변동폭 차이
- **완료 조건:** EditMode T 통과

---

## Stage P: V1.0 빌드 + 보고서

### Task P.1: V1.0 빌드 노트
- [ ] `docs/v1.0-build-notes.md` 신규 (V0.1 패턴 그대로)
- **완료 조건:** 빌드 노트 작성

### Task P.2: V1.0 빌드 보고서
- [ ] `docs/v1.0-build-report.md` 신규
- [ ] 빌드 환경 / 산출물 / 테스트 시나리오 / 발견 이슈
- **완료 조건:** 빌드 보고서 작성

### Task P.3: README + 스크린샷
- [ ] README.md V1.0 버전 갱신
- [ ] 스크린샷 (메인 메뉴 / 매치 텍스트 / 시즌 시상 등)
- **완료 조건:** 포트폴리오 준비 완료

---

## Stage 완료 후

V1.0 완성 시 상태:
- ✅ 분 단위 매치 이벤트 시뮬레이션 (텍스트 이벤트, 유저 매치 한정)
- ✅ 부상 / 카드 / 정지 시스템
- ✅ 평점 시스템
- ✅ Tactic (Formation + Role + Duty + Mentality + Set Pieces)
- ✅ 사기 / 행복도 / Promise 시스템 + 면담
- ✅ 스카우트 명단 (이분법) + AI 영입 (필요 기반 트리거)
- ✅ 협상 (CounterOffer + 선수 협상 + 임대) + 상시 재계약 + FA
- ✅ 유스 (CA 캡 + 시설 분리 + Mentoring + 1군 콜업 자동)
- ✅ 시설 8종 × 10단계 + 병렬 업그레이드
- ✅ 시즌 시상 + 보드 평가 + 재정 결산 + 매니저 평판
- ✅ Stats 49 (FM 1:1) × 1-100 + Hidden Attributes
- ✅ Localization (영/한)
- ✅ Save Migration 인프라
- ❌ 컵 대회 (V2.0+)
- ❌ 다중 리그 / 승강 (V2.0+)
- ❌ 미디어 / 기자회견 (V1.x)
- ❌ Team Instructions (V1.x)
- ❌ 코치 / 의료팀 / 스카우트 개별 인사 (V1.x)
- ❌ 자동 저장 정교화 (V1.x)
- ❌ DOTween UI 애니메이션 (V1.x)
- ❌ 선수 / 구단 이미지 (V1.x)

---

## 작업 시 주의사항

### 의존성 순서 지키기
Stage A 가 모든 신규 도메인 클래스 / SO / Localization 인프라의 빌딩 블록. 우선 완료 후 B+ 진입.

### 각 Task 완료 조건 검증
EditMode / PlayMode 테스트 통과 + Unity 에디터 동작 확인. 통과 못 한 채 다음으로 넘어가지 않기.

### 명세 참조 의무
- 각 Stage 의 명세는 `docs/v1.0-plan.md` §3 + `docs/algorithms.md` Part 2 V1.0-N + `docs/design-decisions.md` #39~#52.
- 작업 중 명세 모순 발견 → 즉시 사용자에게 보고 후 결정 → 명세 갱신.

### Sub-PR 패턴
V0.1 패턴 (Sub-A 명세 → Sub-B 구현 → Sub-C asset chore) 유지. 큰 Task 는 Sub-PR 분할.

### 발견 사항 기록
- 새 결정 필요 → `design-decisions.md` #53~ 추가
- 명세 미비 → `algorithms.md` Part 2 갱신 + `QUESTION:` 마커
- TBD 해결 → 해당 문서 갱신

---

## Change Log

| Date | Change |
| --- | --- |
| 2026-05-22 | V1.0 작업 분할 초안 작성. `docs/v1.0-plan.md` §4 Stage A~P 골격 기반. Sub-PR 패턴 V0.1 그대로. Q1~Q12 결정 반영. Stage A (인프라) 우선 완료 후 B+ 진입 정책. 일정 마감 없음 (Q11). |
| 2026-05-26 | Stage D.4 Sub-A 명세 완료. 스코프 한정 — Training + Medical + Gym 만 (Stadium / Scout / Youth* 은 후속 Stage M.6 / E.2 / L 의존). `algorithms.md` V1.0-10 Player Growth System (신규) + V1.0-11 Injury Recovery (신규) + `design-decisions.md` #53 + `v1.0-plan.md` §3.10.5 보강. Sub-B 구현 후속. |
| 2026-05-26 | D.4 Sub-A 명세 보강 — 성장 size 분포 도입. 초안 `+1` 단위 단조 → 사용자 지적 반영. 2단계 모델 (발생 확률 + size 추첨 +1/+2/+3). `growthBaseChance 0.05 → 0.01` (빈도 1/5), `growthSizeWeights [75,20,5]` / peak youth `[60,30,10]`. 평범 선수 1년 stat 합산 ~5 / wonderkid ~12. FM 표준 일치. |
| 2026-05-26 | D.4 Sub-B 구현 완료 — GrowthSystem.cs + InjurySystem.cs + StatMetadata.IsPhysical + DailyProcessor 통합 + PlayerEvents (PlayerStatChangedEvent / PlayerInjuryRecoveredEvent) + event-bus-catalog 등록 + EditMode 테스트 17개 통과. PR #287 머지. 이슈 #195 close. |
| 2026-05-26 | D.5 코드 + prefab + 씬 작업 완료. FacilityRowController (단순 row) + FacilityController (동적 8 시설 생성, LayoutRebuilder 강제) + FacilityEffectFormatter (시설별 효과 문자열) + SeedV10LocalizationData 17 키 추가 + FacilityEffectFormatterTests 10 케이스. FacilityRow.prefab 신규 + FacilityScene 의 MainContent / FacilityList LayoutElement 적용. PR 진행 중 (이슈 #196). |
| 2026-05-26 | 이슈 정리 — V1.0 일괄 생성 이슈 외 별도 생성된 8개 (#274 / #276 / #278~280 / #282~284) duplicate comment + close, 기존 일괄 생성 이슈 7개 (#187~191 / #193~194) completed comment + close. 추적 정리 (`scripts/cleanup-duplicate-issues.ps1`). 권한 부족으로 실제 삭제 X, close + comment 로 대체. |
| 2026-05-26 | E.1 PR #289 머지 (#197 close) — ScoutReport / CaPaEstimate / HiddenAttributesPartial 직렬화 라운드트립 검증 (Newtonsoft.Json). 도메인 자체는 A.4 에 이미 정의됨. Tests/EditMode asmdef 에 Newtonsoft.Json.dll precompiledReference 추가. |
| 2026-05-26 | E.2 — ScoutingSystem 신규 (`algorithms.md` V1.0-12). 매주 월요일 호출. 자기 구단 자동 + 외부 명단 시설 등급 기반 무작위 + 매주 누적 + margin 감소. DailyProcessor 통합 + GameBalanceSO 신규 1 필드 + EditMode 7 케이스. |
| 2026-05-26 | E.3 — ScoutingVisibility 헬퍼 (Application). IsScouted (디버그 모드 포함) / GetTier (5단계 라벨, ratio 기반) / GetTierLocalizationKey. Localization 5 키 추가 (very_high / high / average / low / very_low). EditMode 13 케이스. UI 표시 분기는 E.4 책임. |
| 2026-05-26 | E.4 — TransferPlayerItem 가시성 분기. Setup 에 userClub 추가, CA / Market value 명단 ∈ 정확 / ∉ 정성적 라벨. 회색조/자물쇠 시각화는 V1.x 미루기 (사용자 결정 — prefab 추가 부담 회피). EditMode 4 케이스. TransferController.OnSearchClicked 호출 일치. |
| 2026-05-26 | F.1+F.2 — CpuTransferAi 신규 (`algorithms.md` V1.0-5). 5 트리거 (약점 라인 / 부상자 / FA 임박 / 보드 약속 / 자금 여유) 우선순위 1>2>3>4>5. CA 가중 후보 추첨 + SubmitOffer (시장가 ×1.20~1.40). 트리거 2 (부상) / 4 (보드 약속) 는 도메인 미구현이라 안전 분기 (Stage I / M.5 도입 시 본격 활성). DailyProcessor 매주 월요일 ScoutingSystem 다음 호출. GameBalanceSO 신규 7 외부화 필드. EditMode 6 케이스. |
| 2026-05-26 | G.1 — MoraleSystem 신규 (`algorithms.md` V1.0-6). 7 메서드 (Tick / OnMatchFinished / OnTransferCompleted / OnContractRenewed / OnPromiseFulfilled / OnPromiseBroken / OnInterview) + Hidden 보정 헬퍼 (professionalism / loyalty). TransferRequestEvent 신규 발행 (Happiness < 20). InterviewType enum 신규. DailyProcessor Tick + MatchPostProcessor OnMatchFinished 통합. GameBalanceSO 신규 19 외부화 필드. EditMode T1-T7 + 5 보조 케이스. T8 (라커룸 분위기 < 30) 은 G.3 이연. OnPromiseFulfilled / OnPromiseBroken 핸들러는 본격 구현 (호출자 = G.2 PromiseSystem.CheckProgress 도입 시 wire-up). OnInterview Praise/Criticize 본격 / Promise* 는 G.2 의존 스텁. |
| 2026-05-26 | G.2 Sub-A — PromiseSystem 신규 (`algorithms.md` V1.0-6 / `design-decisions.md` #43). CheckProgress 4종 평가 (PlaytimeAgreement = 시즌 매치 출전 비율 / TransferIn = 약속 후 영입 카운트 / Renewal = contract.startDate ≥ madeAt / TransferOut = currentClubId 변경) + Create 헬퍼 4종 + Deadline 계산 (다음 시즌 종료 / 시즌 개막 / 활성화 기간 종료). MoraleSystem.OnInterview Promise* 본격 (G.1 스텁 해제 → PlaytimeAgreement / Renewal Promise 생성). seasonAppearances tracking (MatchPostProcessor 증가 + NewSeasonProcessor 리셋). DailyProcessor 매주 월요일 CheckProgress 호출 (ScoutingSystem / CpuTransferAi 다음). PromiseEvents.cs 3종 (Created / Fulfilled / Broken) + GameBalanceSO 9 외부화 필드. EditMode T1-T13 (4종 × Active/Fulfilled/Broken + 면담 wire). Sub-B (면담 UI / Dashboard 인박스 / 30일 임박 알림) 후속 PR. |
| 2026-05-26 | G.2 Sub-B — 면담 UI + Dashboard 인박스 + 30일 임박 알림. Promise.deadlineNotified 플래그 + PromiseDeadlineApproachingEvent 신규 (PromiseSystem.CheckProgress 가 임계 진입 시 1회 발행). InterviewDialogController.cs 신규 (4 버튼 → MoraleSystem.OnInterview). PlayerProfileController [면담] 버튼 (own-club 선수만 활성). DashboardController 인박스 패널 (5 이벤트 구독: Promise* / DeadlineApproaching / TransferRequest) + InboxItem.cs. Localization 15 키 (면담 4 + 인박스 5 + Promise 타입 4 + 기타 2). EditMode T14-T15. Unity Editor 작업 (사용자): InterviewDialogPrefab / InboxItemPrefab / scene wire / LocalizationSO 시드 자산 갱신 — chore PR 분리. |
| 2026-05-26 | G.3 + G.4 — Stage G 완결. **G.3**: MoraleSystem.UpdateDressingRoomMood 신규 (1군 happiness 평균 + 캡틴 leadership × 0.2 가산). MoraleSystem.Tick 매월 1일 호출. MatchSimulator MoodFactor 신규 — `dressingRoomMood < dressingRoomMoodLowThreshold (30)` 시 strength 곱셈 `dressingRoomLowMoodStrengthFactor (0.95)` (≈ 폼 -5 효과). 외부화 3 필드. EditMode 5 케이스 (평균 / 캡틴 가산 / 빈 squad / null season / clamp). **G.4**: TransferRequestDialogController.cs 신규 (3 버튼 = 수락 / 거절 / 면담, V1.0 단순). DashboardController.OnTransferRequest auto-show (자기 구단 선수 한정). Localization 6 키 (dialog title / message / accept / reject / interview / close). G.4 의 다른 항목들 (Event 발행 / 인박스) 은 G.1 / G.2 Sub-B 에서 이미 완료. Unity Editor 자산 (TransferRequestDialogPrefab + DashboardScene wire + Localization asset regen) 은 chore PR 분리. |
